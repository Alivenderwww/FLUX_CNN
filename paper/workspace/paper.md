# 面向端侧流式计算场景的卷积加速器设计

**Convolutional Accelerator Design for Edge-Side Streaming Computation Scenarios**

---

> 作者：[TBD: 学生姓名]
> 学号：[TBD]
> 学院：[TBD]
> 专业：集成电路设计与集成系统
> 指导教师：[TBD]
> 完成日期：[TBD: 答辩前定稿日]

---

## 摘要

随着视频监控、工业视觉、消费电子与自动驾驶辅助感知等应用对端侧智能推理的需求快速增长，面向资源受限终端的卷积神经网络（Convolutional Neural Network，CNN）加速器已成为人工智能芯片设计的重要研究方向。端侧流式计算场景下，输入数据持续到达、单帧推理时延受限、片上存储与功耗预算紧张，对加速器的吞吐稳定性、调度灵活性与资源效率提出综合挑战。本文以"微观寄存器配置 + 宏观指令流调度"的软硬协同分层调度为核心命题，设计并实现了卷积加速器FLUX_CNN。在数据通路上，加速核采用16×16的INT8乘加阵列与三级寄存器堆构成的固定阵列，配合权重静止、激活值滑窗复用与输出通道广播三层数据流复用策略；在控制结构上，核内由6层嵌套自循环有限状态机完成单层全部调度，核间由任务描述符（Task Descriptor）链表完成多层串接与多核协同。针对小通道场景下处理单元（Processing Element，PE）的低利用率问题，本文在编译器侧提出Y维折叠（Ky-fold）与空间到深度（Space-to-Depth，S2D）两种重映射方法。针对FPGA验证平台上数字信号处理单元（Digital Signal Processor，DSP）资源的有效利用，本文进一步提出一种利用OC-broadcast数据流中相邻列共享激活值特性、把两个INT8乘法合并到单个DSP48E1块中的硬件无关映射方案。多核扩展层采用W维切片配合跨核SRAM直送，避免外部双倍速率同步动态随机存储器（Double Data Rate SDRAM，DDR）中转。在Xilinx Kintex-7 XC7K325T-2平台上完成了22个端到端测试用例与ResNet11整网验证。实测结果表明，$N=4$配置下DSP实现版本的布线后最高时钟频率达到143.8 MHz，ResNet11整网在100 MHz假设下达到526 FPS，相对$N=1$含S2D基线取得3.13倍多核加速比。本文工作为端侧定制化卷积加速器提供一种可参考的硬件实现范式。

**关键词**：FLUX_CNN；卷积神经网络加速器；软硬协同分层调度；端侧流式计算；FPGA验证

## Abstract

The rapid adoption of video surveillance, industrial inspection, consumer electronics and driver-assistance perception drives the demand for convolutional neural network (CNN) accelerators on resource-constrained edge devices. Under edge-side streaming computation scenarios, continuously arriving inputs, tight per-frame latency, limited on-chip storage and a strict power envelope jointly challenge the throughput stability, scheduling flexibility and resource efficiency of accelerators. This paper proposes a CNN accelerator FLUX_CNN, organized around a software-hardware co-scheduled hierarchical scheme that combines micro-level register configuration with macro-level instruction-stream scheduling. The datapath of one core consists of a fixed 16×16 INT8 multiply-accumulate (MAC) array and a three-level register file, integrated with a composite dataflow that combines weight stationary, sliding-window activation reuse and output-channel broadcast. The control structure delegates the per-layer schedule to a six-level nested self-iterating finite state machine inside one core, and delegates multi-layer chaining and multi-core coordination to task descriptor lists outside the core. To address the low processing element (PE) utilization in layers with small channel counts, this paper proposes two compiler-side remapping methods, namely Ky-folding and space-to-depth (S2D). To exploit digital signal processor (DSP) blocks on the FPGA verification platform, this paper further proposes a hardware-agnostic mapping scheme that packs two INT8 multiplications into one DSP48E1 block, leveraging the structural property that adjacent columns share the same activation vector under output-channel broadcast. The multi-core extension layer adopts W-dimension slicing together with inter-core SRAM forwarding, eliminating round-trips through external double data rate SDRAM (DDR). Twenty-two end-to-end test cases and a complete ResNet11 inference are validated on a Xilinx Kintex-7 XC7K325T-2 platform. Post-routing results show that the $N=4$ DSP implementation reaches 143.8 MHz, the ResNet11 network achieves 526 FPS at an assumed 100 MHz, and the four-core configuration delivers a 3.13× speedup over the single-core S2D reference configuration. The presented work offers a reference design path for customized CNN accelerators in edge-side streaming computation scenarios.

**Key Words**: FLUX_CNN; CNN Accelerator; Software-Hardware Co-Scheduling; Edge-Side Streaming Computation; FPGA Verification

---

## 主要符号表

| 符号 | 代表意义 | 单位或定义 |
|---|---|---|
| FPGA | 现场可编程门阵列 | Field Programmable Gate Array |
| ASIC | 专用集成电路 | Application Specific Integrated Circuit |
| SoC | 片上系统 | System on Chip |
| CNN | 卷积神经网络 | Convolutional Neural Network |
| DNN | 深度神经网络 | Deep Neural Network |
| PE | 处理单元 | Processing Element |
| MAC | 乘加运算 | Multiply-Accumulate |
| LUT | 查找表 | Look-Up Table（FPGA 基本单元） |
| DSP | 数字信号处理单元 | Digital Signal Processor（FPGA 基本单元） |
| BRAM | 块随机存储器 | Block Random Access Memory |
| FF | 触发器 | Flip-Flop |
| AXI | 高级可扩展接口 | Advanced eXtensible Interface |
| DMA | 直接内存访问 | Direct Memory Access |
| SDP | 标量数据通路 | Scalar Data Path |
| RTL | 寄存器传输级 | Register Transfer Level |
| SRAM | 静态随机存储器 | Static Random Access Memory |
| DDR | 双倍速率同步动态随机存储器 | Double Data Rate SDRAM |
| FSM | 有限状态机 | Finite State Machine |
| WS | 权重静止 | Weight Stationary |
| OS | 输出静止 | Output Stationary |
| RS | 行静止 | Row Stationary |
| INT8 | 8 位整数定点 | 8-bit Integer |
| FP32 | 32 位单精度浮点 | 32-bit Floating Point |
| MHz | 兆赫兹 | 10⁶ Hz |
| GOPS | 每秒十亿次运算 | Giga Operations Per Second |
| TOPS | 每秒万亿次运算 | Tera Operations Per Second |
| FPS | 每秒帧数 | Frames Per Second |
| WRF | 权重寄存器堆 | Weight Register File（容量 32） |
| ARF | 激活值寄存器堆 | Activation Register File（容量 32） |
| PARF | 部分和寄存器堆 | Partial-Sum Register File（容量 32） |
| IFB | 输入特征缓冲 | Input Feature Buffer（容量 8192 word） |
| WB | 权重缓冲 | Weight Buffer（容量 1024 word） |
| OFB | 输出特征缓冲 | Output Feature Buffer（容量 2048 word） |
| IFM | 输入特征图 | Input Feature Map |
| OFM | 输出特征图 | Output Feature Map |
| Cin / Cout | 输入 / 输出通道数 | Input / Output Channel Count |
| Y 维折叠（Ky-fold） | 卷积核 *Y* 维折叠 | Cin < 16 时编译器侧 PE 利用率优化 |
| S2D | 空间到深度 | Space-to-Depth（步长 ≥ 2 时编译器侧 PE 利用率优化） |
| DFE | 描述符获取引擎 | Descriptor Fetch Engine |
| CSR | 控制状态寄存器 | Control Status Register |
| PTQ | 训练后量化 | Post Training Quantization |
| QAT | 量化感知训练 | Quantization Aware Training |

注：如文中对符号另有说明，以文中对应位置说明为准。

---

## 1 绪论

### 1.1 研究背景与意义

近年来，随着人工智能技术在边缘端（Edge）的广泛渗透，越来越多的应用场景要求将卷积神经网络（Convolutional Neural Network，CNN）等深度学习模型直接部署于资源受限的终端设备上。智慧城市的视频监控[1]、工业视觉缺陷检测[2]、消费电子中的手势识别与关键词唤醒[3]、自动驾驶辅助感知[4]等典型场景，均呈现出"高带宽数据流—边缘处理—低时延响应"的流式计算（Streaming Computation）特征。在智慧城市场景中，部署于路口与建筑外立面的高清摄像设备需对每秒数十帧的1080p视频流进行实时目标检测、车牌识别与异常行为分析，单台设备的推理时延普遍要求控制在50 ms以内；工业视觉缺陷检测场景往往需要对产线上以数米每秒速度行进的工件表面进行像素级缺陷分割，且不允许出现帧丢失；消费电子中的手势识别与关键词唤醒则要求处理设备始终保持低功耗常开状态，仅在检测到有效信号时唤醒主系统；自动驾驶辅助感知更是对端到端延迟与功能安全提出严苛要求。与云端推理相比，端侧部署能够显著降低数据上传带宽、保障用户隐私，并提供毫秒级实时响应，已成为人工智能落地的重要路径。

然而，端侧场景与云端数据中心在硬件资源约束上存在本质差异。主流CNN模型的参数量与单次推理计算量普遍在10⁷至10⁹次乘累加（Multiply-Accumulate，MAC）操作量级[5]，例如轻量级目标检测模型YOLOv5n单帧推理约需4.5 GFLOPs，而ResNet-50在224×224输入下需约4.1 GMAC<!-- [CHECK: 4.5 GFLOPs 与 4.1 GMAC 两处口径来源待按 Ultralytics YOLOv5 仓库 README 与 He et al. ResNet 原论文核对] -->；而端侧设备的供电功耗通常限制在数瓦乃至数百毫瓦以内，电池供电的可穿戴设备甚至要求亚瓦级常驻功耗，片上存储容量往往仅为数MB。将通用处理器（Central Processing Unit，CPU）或图形处理器（Graphics Processing Unit，GPU）直接移植到端侧推理，面临算力不足或功耗过大的两难困境[6]。CPU顺序执行的指令流模型难以在功耗包线内提供端侧实时推理所需的吞吐量；GPU虽具备数据并行能力，但其大规模SIMT架构与高速显存子系统在端侧场景下功耗占比过大，能效比通常显著低于专用加速硬件。面向端侧的商用AI加速器（如Google Edge TPU、Intel Movidius、Apple Neural Engine等）已证明硬件定制加速的工程可行性。然而端侧应用场景高度碎片化，不同终端在算子组合、数据位宽、延迟预算与功耗包线上的差异悬殊，单一通用商用产品难以同时覆盖全部细分需求[6]；与此同时，商用NPU的架构细节与编译栈通常以知识产权形式闭源发布，限制了其作为开放研究平台的二次创新空间。由此，面向特定端侧流式场景、以可复现可演进为目标的卷积加速器定制化设计，仍是当前硬件架构研究的主要热点之一。

端侧加速器的最终硬件载体通常是专用集成电路（Application Specific Integrated Circuit，ASIC）。相比GPU，ASIC的动态功耗低一个数量级以上，单位面积算力与能效比（Energy Efficiency）显著更高；相比现场可编程门阵列（Field Programmable Gate Array，FPGA），ASIC流片后在静态功耗与时钟频率方面亦具备结构性优势，适合对功耗与算力密度要求严苛的端侧部署。然而固定的硬件规模与单一数据流策略难以覆盖多种网络结构与层间差异，限制了加速器的长期可用生命周期。为突破这一限制，近年业界开始探索粗粒度可重构阵列（Coarse-Grained Reconfigurable Array，CGRA）思想在AI加速器中的应用——以AMD XDNA / Versal AI Engine为代表的商用二维AI Engine阵列通过可编程tile结构与片上mesh互联，在保持ASIC能效优势的同时为不同算子提供运行时配置能力[7]。基于可重构阵列的CNN加速器设计涵盖数据流编排、阵列结构、存储层次、软硬协同调度等多个层面，是硬件架构、电子设计自动化（EDA）与深度学习三者的交叉研究方向[8,9,10]。在工艺方面，端侧加速器流片节点已从早期的65 nm演进至16 nm与7 nm，单位面积可集成的乘累加单元数与片上存储容量均提升近一个数量级，为更复杂的数据通路与更精细的调度机制提供了硬件基础。

CNN加速器需要解决"计算—访存"不平衡问题。由于卷积运算本质上是规则的多维张量乘累加，其算术强度（Arithmetic Intensity）对数据复用策略设计极度敏感。在典型卷积层中，输入特征图、卷积核与输出特征图三者均存在跨维度复用机会：输入特征图沿空间维度被多个输出位置共享，卷积核在批量与空间维度上被反复使用，输出部分和则需在通道方向累加聚合。若数据复用策略不当，片外动态随机存取存储器（Dynamic Random-Access Memory，DRAM）的访问能耗将吞噬大部分能量预算。早期加速器采用全硬化卷积状态机，硬件生成2D图像坐标与步长循环，在小规模模型上可取得高能效；但面对任意K×K卷积核、可变步长（Stride）、多分支残差等现代网络结构时，硬件逻辑复杂度急剧上升，难以通用化[8]。为此，近年来出现了"软硬协同调度"（Software-Hardware Co-scheduling）的设计范式：将2D空间迭代、通道切片、层间依赖等调度决策交由软件完成，硬件仅负责执行简单、规则、可流水的原子算子[9,10]。这一范式着重划分软件与硬件的职责边界——硬件保持专用化与高吞吐，软件提供面向不同模型的调度灵活性，二者通过紧凑的配置接口或宏粒度指令耦合。

本论文在上述背景下，以端侧流式CNN推理为目标场景，设计并实现了一款软硬协同分层调度的卷积加速器FLUX_CNN。在微观层面，核内采用配置寄存器驱动的自循环有限状态机（Finite State Machine，FSM），一次性写入全部卷积参数与地址步进量后即可自动完成单层计算；在宏观层面，预留多核Task Descriptor接口与行级流式跨层融合调度机制。本研究的工程意义在于通过分层调度架构兼顾灵活性与高能效，为端侧定制化卷积加速器提供一种可参考的硬件实现范式；学术意义在于通过模块化、开源式的设计实践为端侧加速器领域提供可复现、可扩展的研究平台，便于后续在数据流策略、稀疏加速、跨层融合等方向上开展演进研究。

### 1.2 国内外相关研究工作进展

#### 1.2.1 端侧卷积神经网络硬件加速器架构研究进展

CNN专用硬件加速器的研究可追溯至2014年前后，寒武纪团队提出的DianNao系列首开先河[11]。DianNao采用固定规模的神经运算单元（Neural Functional Unit，NFU）与分离的三缓冲结构（输入缓冲、权重缓冲、输出缓冲），奠定了后续端侧加速器的基本范式。DianNao在65 nm工艺下以980 MHz时钟实现了452 GOPS的峰值算力与0.92 TOPS/W的能效比，证明了硬件定制相对于通用处理器在密集线性代数任务上的显著能效优势。随后Google于2017年发布的TPU（Tensor Processing Unit）[12]，将脉动阵列（Systolic Array）引入大规模矩阵乘法加速，256×256的脉动结构配合指令驱动的数据流，在数据中心场景取得显著能效优势。TPU v1在28 nm工艺下以700 MHz工作频率实现92 TOPS峰值算力，整板功耗约40 W，其设计选择反映了数据中心对吞吐与能效平衡的优先取舍。TPU所采用的CISC风格粗粒度指令集思路对后续端侧加速器设计亦有深远影响：硬件只需支持少量宏粒度指令，即可表达完整的神经网络推理流程，从而避免端侧场景下指令缓冲与取指开销对功耗的侵蚀。

麻省理工学院Chen等人提出的Eyeriss架构[13]在数据流（Dataflow）层面做出了奠基性贡献，系统比较了权重静止（Weight Stationary，WS）、输出静止（Output Stationary，OS）、行静止（Row Stationary，RS）等多种数据流策略，并选择RS方案以最大化卷积核、部分和、输入激活三者的协同复用；该工作在65 nm工艺下取得了200 MHz / 168 GOPS / AlexNet 35 fps @ 278 mW的端侧能效水平[13]，成为学术界CNN加速器的重要标杆。Eyeriss的主要贡献在于建立了数据流策略与片上能耗之间的定量映射，为后续工作提供了系统化的设计空间分析方法。Chen等人随后提出的Eyeriss v2[14]针对移动端稀疏网络进一步优化，引入灵活的分层mesh互联网络以支持不同层数据流切换，使单一硬件能够在不同卷积层之间动态选取最优数据复用模式。Parashar等人的SCNN[15]通过压缩稀疏权重与跳零机制进一步降低冗余计算，在剪枝后的稀疏CNN上将计算与存储能耗同步降低；Horowitz的经典能耗分析[16]指出片外DRAM访问能耗高出片内寄存器访问约三个数量级，这一结论为后续加速器"尽量把数据留在片内"的设计思路提供了理论依据。Du等人的ShiDianNao[17]将感知计算部署至传感器附近，通过近传感器架构降低数据搬运能耗，与流式计算场景的能效诉求一脉相承——其主要思想在于把数据通路尽量收缩在传感器与计算阵列之间的短链路内，避免冗长的总线与片外缓存往返。

进入2020年以后，开源与标准化成为该领域的重要趋势。NVIDIA的NVDLA（NVIDIA Deep Learning Accelerator）[18]作为开源参考设计，采用CDMA-CBUF-CSC-CMAC-CACC-SDP-PDP六级独立模块流水线结构，支持从边缘小型到服务器级的多种可配置参数。其内部的CDMA通过坐标发生器实现"逻辑零填充"式Padding，读取单元在边界越界时直接向计算流水线喂0，避免了额外SRAM存储开销。NVDLA采用多模块各自独立FSM的分布式控制，模块间通过握手与中间缓冲传递数据，便于在不同SoC中按需裁剪不同流水级。在FPGA加速器方向，Genc等人发布的开源框架Gemmini[19]为系统评估DNN架构提供了完整的SoC集成方案，其在22 nm工艺下达到1 GHz时钟与512 GOPS峰值算力；Yu等人的OPU（Overlay Processor Unit）[20]通过构建CNN专用指令集overlay，使多种不同模型共享同一硬件部署，无需针对每个模型重新综合，从而显著缩短部署周期；Zhang等人的DNNBuilder[21]通过自动化工具链从高层网络描述直接生成FPGA比特流，验证了端到端编译流程的可行性。近年来，Aung等人提出的DeepFire2[22]放弃传统查找表实现方式、采用灵活资源调度策略，在多芯片FPGA上达到600 MHz运行频率；Kim等人针对移动FPGA平台设计的可重构目标检测加速器[23]通过RTL级定向优化，在能效、资源利用率与吞吐率三个维度同步提升15%至58%。国内方面，Peng等人提出的全栈FPGA加速器[24]在Intel Arria 10平台上实现8.3 TOPS峰值吞吐、功耗仅0.93 W，代表当前同类设计的能效上限之一；Zhao等人提出的紧凑查找表乘法器结构[25]结合4-bit量化方案，在FPGA上大幅压缩模型体积的同时保持分类精度。Wang等人的FPGA加速器综述[26]系统梳理了2018-2023年间的典型工作，指出端侧场景下50-500 GOPS、1-3 W是当前FPGA CNN加速器的主流工作点，亦反映出当前学术界对端侧能效甜点的共识区间。

表 1.1 代表性 CNN 硬件加速器的阵列规模、工作频率、峰值算力与能效对比
Table 1.1 Comparison of array size, clock frequency, peak performance and energy efficiency among representative CNN hardware accelerators

| 加速器 | 年份 | 平台 | 频率（MHz） | 峰值算力（GOPS） | 功耗（mW） | 能效（TOPS/W） | 引用 |
|---|---|---|---|---|---|---|---|
| DianNao | 2014 | 65 nm | 980 | 452 | 485 | 0.92 | [11] |
| TPU v1 | 2017 | 28 nm | 700 | 92000 | 40000 | 2.30 | [12] |
| Eyeriss | 2017 | 65 nm | 200 | 168 | 278 | 0.12 | [13] |
| NVDLA* | 2018 | 16 nm | 1000 | 128 | ~300 | ~0.40 | [18] |
| Eyeriss v2 | 2019 | 65 nm | 200 | 153.6 | 606 | 0.96 | [14] |
| VTA | 2019 | ZCU102 | 333 | 170 | - | - | [27] |
| Simba* | 2019 | 16 nm | 1800 | - | - | 6.1 | [33] |
| OPU | 2020 | XC7Z045 | 200 | 160 | 8700 | 0.018 | [20] |
| Gemmini | 2021 | 22 nm | 1000 | 512 | 380 | 1.35 | [19] |
| Peng et al. | 2022 | Arria 10 | 1000 | 8300 | 930 | 8.92 | [24] |

*NVDLA可配置为不同阵列形状。
*Eyeriss v2能效按0%稀疏率测得；激活值稀疏被利用时该值更高。
*Peng等人报告的是全栈加速器的片上活动功耗；板级功耗高于此值。
*Simba在最优工作点上单芯粒能效达6.1 TOPS/W；36芯粒MCM整体能效与单芯粒结果不同。

#### 1.2.2 软硬件协同调度与流式计算加速研究进展

软硬协同调度的思想可追溯至通用计算领域的VLIW（Very Long Instruction Word）架构。Moreau等人提出的VTA（Versatile Tensor Accelerator）[27]是将VLIW范式引入深度学习加速器的标志性工作。VTA通过张量指令（Tensor Instruction）解耦硬件流水与算法迭代：硬件内部维护计算、加载、存储三组并发执行单元，指令仅指定依赖关系与宏算子粒度，从而避免了每条指令都需取指译码的开销。VTA同时建立了TVM软件栈与硬件之间的桥梁，使深度学习编译器可以直接生成宏算子指令流。

然而完整的张量指令集仍保留了取指、译码、依赖跟踪与指令缓冲等通用处理器范式下的控制开销。Horowitz的经典能耗测算表明，片上SRAM的指令读取能耗相对单次8 bit乘累加运算可达数百倍量级[16]<!-- [CHECK: 准确比例待按 Horowitz ISSCC'14 keynote 原始 slide 核对] -->；Reuther等人的AI加速器综述亦指出，在端侧静态卷积场景下指令控制路径的动态功耗不可忽略[6]<!-- [CHECK: 具体百分比待按 Reuther 综述原文页码核对，literature.md 暂未收录该综述完整条目] -->。在端侧静态卷积这一典型推理场景下，网络拓扑在推理阶段固定，卷积参数在层启动时即已确定，层内不存在运行时分支，完整指令流带来的控制灵活性对该场景收益有限。

层间流式融合（Layer Fusion）是另一条重要的软硬协同优化路线。Alwani等人提出的Fused-Layer CNN架构[28]首次在硬件层面实现跨多层的流水推理：上层产出若干行输出即可触发下层计算，无需等待整层结束；由此片上缓存需求从*O*(*H*×*W*)降至*O*(*K*×*W*)，显著减少DRAM访问。Shen等人的Escher[29]、Xiao等人的FPGA层融合工作[30]进一步将这一思路工程化，在特定网络上达到2-5倍能效提升。层融合的能效增益主要来自两方面：一是中间特征图无需反复落盘，DRAM访问量按层数线性下降；二是相邻层之间的同步等待被替换为细粒度行级握手，使流水线气泡显著减少。然而层融合对调度灵活性提出了更高要求：不同层的*K*、Stride、Padding各异，硬化状态机难以适配；融合层数过深还会导致片上缓冲与控制状态空间膨胀。如何在硬件侧提供规则的缓冲与同步原语、把融合策略交由软件根据网络结构动态决定，是该方向尚未形成统一接口的工程难点。

数据流可重构（Dataflow Reconfigurability）是近年来该领域的活跃方向。Kwon等人提出的MAERI[31]通过可重构mesh互联支持WS、OS、RS等多种数据流在同一硬件上动态切换；Qin等人的SIGMA[32]进一步将该思路扩展至不规则稀疏矩阵乘法，使加速器能够在保持高利用率的前提下应对变化的张量形状。这类工作代表了软件指定数据流的技术路径，其主要代价是互联结构的额外面积与控制复杂度。在端侧单层卷积场景下，最优数据流策略（如WS+滑窗复用+输出通道广播）通常相对固定，柔性互联带来的面积与控制开销在单核场景下收益边际递减；而当任务粒度从单核扩展到多核多层时，灵活性的需求会迁移到核间互联与任务编排层面，由此催生了对单核内部紧凑数据流与核间可重构互联分层处理的需求。

多核加速器的调度问题亦得到持续关注。NVIDIA与MIT合作的Simba[33]采用36芯片多核mesh架构，通过核间互联将大模型划分至多芯片并行执行，成为后续多核NPU研究的重要参考。Simba的工作揭示了多核扩展中数据路由与负载均衡的若干工程难点，特别是当模型层间张量形状变化较大时，固定划分策略往往难以兼顾各层的核利用率。Tan等人研究的NN-Baton[34]针对分布式NPU提出层级化的任务划分与负载均衡策略，从算子、张量、子张量三层粒度建立任务搜索空间；近年来Park等人进一步提出面向异构多核的任务调度框架[35]，支持多核之间能力差异下的全局调度。在FPGA端，Wang等人提出的分布式CNN推理框架[36]在多FPGA平台上实现跨层流水推理。多核调度的主要难点在于数据路由与核间同步：当不同核执行的层在张量形状、计算密度与数据依赖上存在差异时，简单的同步广播会导致显著的等待时间；如何在静态调度与动态反馈之间取得平衡，是当前多核CNN加速器仍在探索的开放问题。

除CNN类网络外，新型骨干网络对加速器架构亦提出了新的扩展需求。大模型时代Vision Transformer（ViT）[37]的兴起对CNN加速器架构也提出了新挑战。Sun等人的ViTA[38]、You等人的ViTCoD[39]等ViT专用加速器通过算法硬件协同剪枝、极化注意力图等方式降低计算复杂度。尽管ViT的自注意力与CNN的滑窗卷积访存特征差异较大，但二者在张量乘累加这一基础算子层面仍有共性，相关协同设计思想可为未来FLUX_CNN扩展至Transformer类算子提供借鉴。

综合上述分析可见，端侧CNN加速器发展呈现三条明显趋势：其一，从全硬化控制向软硬协同调度演化；其二，从层内优化向跨层流式融合扩展；其三，从单核高性能向多核高能效延伸。然而现有工作在以下两方面仍存在研究空间：第一，多数FPGA加速器沿用完整指令流控制方案，当网络结构固定、仅卷积参数变化时，指令流取指与译码开销存在冗余，指令序列复杂度也随Padding、Cin/Cout切片、轮次分块等特性迅速膨胀；第二，行级流式跨层融合的硬件接口尚未标准化，多核数据路由与同步机制仍是个案设计，缺少可在不同网络与不同核数下复用的统一调度抽象。本论文针对上述空间，从微观寄存器配置与宏观指令流调度两个层面展开研究。

### 1.3 本论文的主要研究内容

针对上述研究空间，本论文提出面向端侧流式计算场景的分层调度卷积加速器FLUX_CNN，主要研究工作包括：

第一，提出"微观寄存器配置 + 宏观指令流调度"的分层架构方案。核内采用配置寄存器 + 6层嵌套自循环FSM（cs → yout → tile → cins → round → pos）完成单层全部调度，无需运行时取指译码；核间保留宏指令流接口，通过Task Descriptor编排多核多层协作与数据路由。该方案兼顾单核高能效与多核高灵活性，使硬件能在严格的端侧功耗包线下保持充分的扩展空间。

第二，设计16×16 INT8 MAC阵列与三级寄存器堆（WRF / ARF / PARF）数据通路，建立权重静止、激活值滑窗复用、输出通道广播三层数据复用策略；实现硬件零乘除法的地址生成，支持任意$K \times K$卷积核、任意步长、任意Cin/Cout切片，以及$K^2$ > WRF深度时的轮次分块（round chunking），从而在固定阵列规模下覆盖典型端侧CNN模型的全部卷积层形状。

第三，针对小通道场景下PE阵列利用率受限的问题，在编译器侧提出Y维折叠（Ky-fold）与空间到深度（Space-to-Depth，S2D）两种重映射方法。Y维折叠将卷积核*Y*维循环折叠到Cin方向，使Cin < 16的层在不改硬件接口的前提下填满16路Cin并行；S2D则在步长大于1的层将4个空间相位折叠到Cin方向，使降采样层的有效Cin同步扩张4倍。两种方法分别覆盖小Cin与大步长两种典型低利用率情形，且均不引入额外硬件代价。

第四，针对端侧多核扩展中跨核数据搬运与中转开销问题，构建多核扩展层。多核之间采用W维切片调度，每核处理一段每核子图像宽度子图加上必要的边界冗余列；相邻核之间通过跨核SRAM直送把上一层OFM直接写入下一核的IFB区域，避免经过外部DDR中转。多核之间的同步通过描述符链表完成，不依赖中央调度器，扩展到$N=2$与$N=4$时单核RTL无需改动。

第五，针对FPGA验证平台上DSP资源的有效利用，提出一种利用OC-broadcast数据流中相邻列共享激活值特性、把两个INT8乘法合并到单个DSP48E1块中的硬件无关映射方案。该方案利用DSP48E1块25 bit×18 bit乘法器的位宽冗余，在A端打包两组相邻列的INT8权重、B端共享同一拍激活值；输出端通过位提取与符号校正还原两路独立乘积。该映射在不改动RTL数据通路接口的前提下，使16×16阵列的乘法器全部由DSP块承担，显著降低LUT占用并提升时序裕度。

第六，构建面向FLUX_CNN加速器的软件工具链与验证框架，最终目标是实现TensorFlow / PyTorch训练模型的端到端部署。完整工具链规划包含三阶段：前端算子解析（Operator Parsing）与量化，支持Conv2D、BatchNorm、ReLU、Pooling、Residual Add等典型CNN算子的适配；中端图级调度（Graph-level Scheduling），负责多层网络的层间融合、核间任务切分与Task Descriptor生成；后端代码生成，预算各地址步进、轮次分块参数与Cin/Cout切片策略，产出各核的配置寄存器取值与宏指令流。

第七，完成综合评估与控制变量验证。在Xilinx Kintex-7 XC7K325T-2平台完成路由后实现，给出$N=1$ / $N=2$ / $N=4$三种配置的资源占用、布线后最高时钟频率与ResNet11整网帧率；并通过将SmartConnect IP、偏置寄存器堆、仲裁器逐个替换为零延迟理想模型的方式，对各调度因素在整网周期数中的贡献给出基于实测的定量分解。

### 1.4 论文组织结构

全文按章节顺序展开研究背景与意义、理论基础、总体方案、具体实现、验证与性能分析、结论与展望六个部分。

---

## 2 理论基础

### 2.1 引言

卷积神经网络作为端侧视觉计算的主流模型，其加速器设计需要综合权衡算法层面的计算特征与硬件层面的资源约束。本章首先介绍卷积神经网络的基本原理与基本层结构，主要包括卷积层、池化层、全连接层的计算公式与多通道情形下的展开方式；其次，结合典型加速器设计的归纳，介绍卷积层数据流的经典分类与硬件复用策略，包括权重静止、输出静止、行静止三种代表性数据流的复用关系与片上存储模式；接着，介绍硬件加速器中常用的基础技术，主要包括流水线设计、行缓存窗口生成、握手协议以及高级可扩展接口（Advanced eXtensible Interface，AXI）总线协议；随后，介绍多核加速器设计中常被援引的非一致性内存访问架构与存储层级访存功耗差异两项体系结构原理，包括本地与远端访存延迟的非对称性，以及寄存器、片上静态存储、片外动态存储之间数十倍至数百倍的能耗差距；最后，介绍模型量化的基本原理，包括训练后量化与量化感知训练两种主流方法，以及定点乘加在硬件实现中的位宽选择。本章内容作为后续加速器架构设计与模块实现的理论基础。

### 2.2 卷积神经网络基础

#### 2.2.1 卷积层

卷积层是卷积神经网络中承担特征提取的核心层，其计算量通常占整个网络的90%以上<sup>[5]</sup>，因此卷积计算的优化与加速是硬件加速器设计的重中之重。卷积运算的基本思路是用一个$K \times K$的卷积核在输入特征图上逐点滑动，每滑到一个位置即将卷积核覆盖区域的输入与对应权重逐元素相乘并累加，最终得到输出特征图上的一个像素值。

如图 2.1 所示卷积计算原理。以3×3卷积核为例，单通道情形下的卷积计算公式如下：

$$ Y(i,j) = \sum_{u=0}^{K-1} \sum_{v=0}^{K-1} a(i+u,\, j+v) \cdot w(u,v) + b $$

其中*Y*为输出特征图，*a*为当前滑窗下的输入特征图数据，*w*为与*a*对应的权重数据，*b*为偏置项。卷积核在滑动过程中权重保持不变，达到了权重共享的效果，使得参数量相对全连接层显著降低，这一性质既缓解了网络训练中的过拟合问题，也减少了硬件实现时的权重搬运开销，提高了计算效率。

实际网络中的卷积层往往涉及多通道输入与多通道输出。设输入通道数为Cin，输出通道数为Cout，则多通道卷积可表示为：

$$ Y_{c_{out}}(i,j) = \sum_{c=0}^{C_{in}-1} \sum_{u=0}^{K-1} \sum_{v=0}^{K-1} a_c(i+u,\, j+v) \cdot w_{c_{out},c}(u,v) + b_{c_{out}} $$

其中*c*为输入通道序号，*c_out*为输出通道序号。从公式可以看出，多通道卷积的输出由所有输入通道在该位置的卷积结果求和并加偏置得到。在工程实践中，输入输出通道数往往达到64、128、256甚至更多，单层卷积所需的乘加运算量可达数百万至数亿次。因此，必须将通道维度进行切片，按片分批送入有限规模的乘加阵列计算，再在累加器中将各片结果汇总，才能得到完整的输出特征图。

卷积层的另外两个重要参数是步长（stride）和填充（padding）。步长决定卷积核在输入特征图上的滑动间隔，步长为1时输出特征图与输入空间分辨率近似一致，步长为2时输出空间分辨率减半，常用于在网络中段实现下采样。填充则在输入特征图的边界外补零，以控制输出特征图的尺寸边界并保证边缘像素被卷积核充分覆盖。给定输入尺寸*H_in*、*W_in*、卷积核尺寸*K*、步长*s*、填充*p*，输出特征图尺寸为：

$$ H_{out} = \left\lfloor \frac{H_{in} + 2p - K}{s} \right\rfloor + 1, \quad W_{out} = \left\lfloor \frac{W_{in} + 2p - K}{s} \right\rfloor + 1 $$

不同尺寸的卷积核可提取不同尺度的局部特征，3×3、5×5、7×7是端侧CNN模型中最常用的三种卷积核尺寸。

#### 2.2.2 池化层

池化层的主要功能是对卷积层输出的特征图进行下采样，以逐步压缩空间分辨率、减少后续卷积层的计算量并增大有效感受野。常见的池化算子尺寸为2×2，步长为2，经过一次池化后特征图的高度和宽度各减为原来的一半，总像素数变为原来的四分之一。常用的池化方式有最大值池化（Max Pooling）和平均值池化（Average Pooling）两种。研究表明，最大值池化与平均值池化的特征保留能力相近，但最大值池化只需比较运算，省去了平均值池化所需的加法和除法，因此在硬件实现中更为常用。在数据通路上，池化通常复用与卷积相同的行缓存与窗口生成逻辑，仅将卷积核乘加器替换为比较树或加法树，实现代价较低。

#### 2.2.3 全连接层

全连接层常出现在分类网络的尾端，用于将卷积层提取的高维特征向量映射到类别分数。全连接层中每一个输出神经元都与所有输入神经元相连，其计算公式为：

$$ y_j = \sum_{i=0}^{N-1} w_{i,j} \cdot x_i + b_j $$

其中*x_i*为全连接层的第*i*个输入神经元，*w_{i,j}*为输入*i*到输出*j*的连接权重，*b_j*为输出*j*的偏置项，*N*为输入神经元数量。从计算结构上看，全连接层是1×1卷积的一种特例，可视作输入特征图尺寸退化为1×1、通道数为*N*、输出通道数为类别数的卷积层。因此，加速卷积层所采用的乘加阵列与数据通路同样可以复用于全连接层的计算，只需在地址生成器中将空间维度循环退化为单点循环即可。

### 2.3 数据流分类与硬件复用策略

卷积神经网络中存在大量的数据复用机会：同一卷积核在输入特征图上滑动时权重被反复使用、相邻输出位置共享部分输入像素、同一输入通道在不同输出通道间被广播。如何在有限的片上存储与片上互连约束下尽量挖掘这些复用机会，是硬件加速器架构设计的核心议题。Eyeriss<sup>[15]</sup>对此给出了系统的归纳，将卷积层的硬件数据流划分为权重静止（Weight Stationary，WS）、输出静止（Output Stationary，OS）和行静止（Row Stationary，RS）三种典型类型，每种类型对应不同的复用对象与片上存储模式。

#### 2.3.1 权重静止数据流

权重静止数据流的核心思想是将卷积核权重预先加载到处理单元（Processing Element，PE）阵列内的本地寄存器中，整个卷积层计算过程中权重保持不变，由输入激活值在PE阵列内流动并与权重做乘加运算。其优点在于：权重的片外访问次数最少，仅在层切换时才需要重新加载，对于权重规模相对较小、特征图相对较大的卷积层，可显著降低片外存储带宽压力。权重静止数据流的代表实现包括nn-X<sup>[10]</sup>等早期加速器，以及NVDLA<sup>[14]</sup>等工业级ASIC加速器。其主要不足是：当卷积核较小而输出通道数较多时，PE内的权重寄存器利用率偏低，且部分和（Partial Sum）需要在PE之间或在专门的累加器中进一步规约，对片上互连提出额外要求。

#### 2.3.2 输出静止数据流

输出静止数据流将累加目标（即输出特征图的某个像素或某通道部分和）固定在PE内的本地寄存器中，由权重和输入激活值同时流入PE完成乘加更新，直至该输出像素的全部累加项完成后才将结果写出。其优点在于：部分和的中间结果完全本地化，避免了在PE之间搬运32位部分和带来的功耗与互连开销。其代表实现包括ShiDianNao<sup>[12]</sup>等。输出静止的主要不足是：权重和激活值都需要在PE阵列内流动，片上存储带宽压力相对较大，当卷积核尺寸或通道数变化较大时，地址生成与数据广播逻辑较为复杂。

#### 2.3.3 行静止数据流

行静止数据流由Eyeriss<sup>[15]</sup>提出，将卷积运算按"一行权重 × 一行输入 = 一行部分和"的最小卷积单元映射到单个PE上，多个PE通过纵向、横向、对角线三种方向的数据传递组合出完整的二维卷积。该数据流同时利用了权重在水平方向的复用、输入在对角线方向的复用、部分和在垂直方向的累加复用，因此在卷积核尺寸、输入特征图尺寸、通道数等参数变化时仍能保持较高的PE利用率。Eyeriss报告其在AlexNet多个卷积层上的平均能效相比同期的固定数据流加速器有显著提升<sup>[15]</sup>。其主要代价是：PE之间的局部互连较为复杂，配置开销相对较高，对编译器的循环映射要求也更为严苛。

#### 2.3.4 数据流选择的工程权衡

上述三种数据流并非互斥，实际加速器常根据目标应用场景与工艺约束进行混合选择。当目标场景以中小卷积核、规则形状、固定网络拓扑为主时，权重静止数据流可在保持较低控制复杂度的前提下获得良好的能效；当目标场景需要在多种卷积形状之间灵活切换、且片上互连预算充裕时，行静止数据流可提供更高的平均利用率。在端侧流式视频处理场景中，由于输入特征图尺寸较大、卷积核尺寸相对适中、模型一旦部署后拓扑相对固定，权重静止结合输入激活滑窗复用与输出通道广播是较为合适的复用策略。

### 2.4 硬件加速基础技术

#### 2.4.1 流水线设计

流水线是数字硬件中提升吞吐率的基本手段，其核心思想是将一个长组合逻辑路径切分为多级时序子路径，相邻级之间用寄存器隔开，使得各级在每个时钟周期内可以并行处理不同的数据。设流水线划分为*N*级，理想情况下吞吐率可由原先的1/(*N*·*t_stage*)提升至1/*t_stage*，其中*t_stage*是单级延迟。流水线带来的额外代价主要包括寄存器面积、流水线启动延迟与流水线冲刷损失。在卷积加速器中，乘法器与加法树往往位于时序最长路径上，将其切分为乘法级、加法树第一级、加法树第二级等多级流水线段，是提升时钟频率的有效手段。

#### 2.4.2 行缓存与窗口生成

卷积运算需要在每一步访问输入特征图上一个$K \times K$的局部窗口，若每次都从片外存储读取整个窗口，将带来巨大的带宽压力。行缓存（line buffer）通过在片上保存最近的*K*行输入像素，使得当卷积核滑动到下一像素位置时，可直接在片上读出对应的$K \times K$窗口而无需重复访问片外。行缓存通常采用环形缓冲（ring buffer）的写入策略：当第*K*+1行像素流入时，覆盖最早一行所占的存储位置，从而以固定的存储容量持续支持任意高度的输入特征图流式输入。在端侧流式应用中，行缓存承担着连接片外像素流与片上PE阵列的缓冲职责。

#### 2.4.3 握手协议

数字模块之间的数据传输需要明确的同步机制以应对生产者与消费者之间的速率失配。valid-ready握手协议是一种在异步速率耦合中广泛采用的双向握手机制：生产者在数据有效时拉高valid信号，消费者在可接收数据时拉高ready信号，仅当valid与ready在同一时钟沿同时为高时数据才被认为完成传输。该协议的优点在于：双方各自管理自身的状态，互不干涉；任意一方反压时另一方自动停顿，不需要中心仲裁；多个模块可级联组成无中心FSM的流水线。在CNN加速器内部，valid-ready握手是连接行缓存、乘加阵列、部分和累加器、输出缓冲等模块的标准接口形式。

#### 2.4.4 AXI 总线协议

AXI总线是ARM公司提出的一种面向高性能片上系统的标准总线协议，目前已成为片上系统设计中的事实标准之一。AXI协议主要分为两大类：AXI4完整版（AXI4 Full）面向高吞吐数据传输，支持突发读写、乱序事务与多个ID并发，常用于加速器与外部DDR控制器之间的批量数据搬运；AXI4-Lite面向低吞吐配置寄存器访问，仅支持单拍读写，用于主机到加速器配置寄存器（Control Status Register，CSR）的写入与状态回读。AXI协议本身基于valid-ready握手机制构建，因此可与加速器内部的握手接口自然衔接。在加速器顶层，主机通过AXI4-Lite写入配置寄存器与启动信号，加速器内部的DMA控制器通过AXI4完整版主接口向DDR发起读写突发，是端侧SoC中卷积加速器对外接口的典型结构。

### 2.5 非一致性内存访问架构

非一致性内存访问（Non-Uniform Memory Access，NUMA）是多核体系结构中描述内存访问延迟非对称性的基本概念。其核心特征是：处理核访问与之物理邻近的本地存储节点延迟较低，访问其他核所属的远端存储节点则需穿越互连网络，延迟显著上升；当多核同时争用同一远端节点时，互连拥塞还会进一步放大尾部延迟。Hennessy和Patterson<sup>[5]</sup>在体系结构教科书中将NUMA与对称多处理器（Symmetric Multi-Processor，SMP）相对照，指出在核数超过约8之后，集中式共享存储的总线带宽与一致性开销会成为瓶颈，因此现代多核处理器与片上系统普遍采用分布式本地存储加片上互连的NUMA结构。<!-- [CHECK: Hennessy/Patterson 教科书条目待 literature.md 收录并核对编号 [5]] -->

NUMA特征同样体现在面向深度学习的多核加速器中。Chen等人<sup>[14]</sup>在2019年报告的Eyeriss v2为多PE簇配置独立的私有静态存储，通过分层mesh互连支持簇间数据交换，簇内访问延迟约1拍，簇间数据搬运需多拍并经历仲裁；Shao等人<sup>[33]</sup>在2019年报告的Simba多芯片粒（Multi-Chip Module，MCM）加速器在36个芯片粒之间也表现出明显的本地与跨片访问延迟差距。对多核卷积加速器而言，每核独立的输入特征缓冲（IFB）、权重缓冲（WB）与输出特征缓冲（OFB）天然构成NUMA架构中的本地存储节点；不同核之间若需共享中间特征图，最直接的路径是写回片外动态存储再由其他核读入，但这一往返路径既占用片外带宽又引入显著的访存延迟。这一架构特征要求多核加速器在数据通路设计阶段权衡本地存储节点容量、跨节点互连带宽与数据布局策略。

### 2.6 存储层级与访存功耗差异

数字系统中的存储器并非以同一形式承担所有访存需求，而是组织为分层的存储体系。从靠近计算单元到远离计算单元，依次为寄存器堆、一级缓存或片上静态存储（Static Random-Access Memory，SRAM）、片上块静态存储（Block Random-Access Memory，BRAM）以及片外动态存储（Dynamic Random-Access Memory，DRAM）。每一层在容量、带宽、延迟、单次访问能耗四个维度上都呈现明显的阶梯特征：越靠近计算单元的存储容量越小、带宽越高、延迟越低、单次访问能耗也越低；越远离计算单元的存储容量越大、带宽越低、延迟越高、单次访问能耗也越高。这一分层组织的根本动因是：计算单元在每个时钟周期内可能发起多次小粒度访存，若全部由片外动态存储响应，则带宽与延迟均无法满足时序约束。

Horowitz<sup>[16]</sup>在2014年的国际固态电路会议主旨报告中给出了45 nm工艺下各存储层级访存能耗与算术运算能耗的定量对照。其中，1次8位整数乘加运算约消耗0.2 pJ；1次访问处理单元内的本地寄存器约消耗0.1 pJ；1次访问片上8 KB SRAM约消耗5 pJ，相当于约25次乘加运算的能耗；1次访问片外DRAM约消耗640 pJ，相当于约200次乘加运算的能耗<sup>[16]</sup><!-- [CHECK: 具体数值按 Horowitz ISSCC'14 keynote slide 校核] -->。Chen等人<sup>[13]</sup>在Eyeriss工作中给出的能耗分布表也得到一致结论：在AlexNet卷积层执行过程中，片外DRAM访问能耗占整体能耗的相当比例，远高于乘加阵列本身的能耗占比<!-- [CHECK: Eyeriss 论文图 18 给出的具体百分比待回查] -->。这一差距决定了端侧加速器的能效优化重心：单纯增加乘加阵列规模带来的能效收益有限，而把数据尽量留在片上、减少片外搬运次数才是降低单次推理能耗的主要途径。上述差距决定了端侧加速器的能效优化重心是减少片外搬运并最大化片上数据复用，相关数据流与缓冲策略在已有工作中均围绕该目标展开。

### 2.7 模型量化基础

神经网络通常以32位浮点（FP32）格式进行训练，但若直接将FP32模型部署到端侧设备上，其推理过程在能耗、面积与延迟上往往难以满足约束。模型量化通过将权重与激活值用更低位宽的定点数表示，显著降低存储占用与乘加运算的硬件代价。量化白皮书<sup>[27]</sup>系统介绍了主流量化算法，并将其归为训练后量化（Post Training Quantization，PTQ）与量化感知训练（Quantization Aware Training，QAT）两类。

PTQ方法在已训练的浮点模型上直接进行量化，无需重新训练或对训练数据集进行标注。其核心步骤包括用少量校准数据统计权重与激活值的数值分布、为各张量选取量化范围、按线性映射将浮点张量量化为定点张量。在多数视觉分类与检测任务上，PTQ可将FP32模型量化至8位定点（INT8），同时保持1%以内的精度下降<sup>[27]</sup>。PTQ的最大优势在于流程简便、对部署侧友好，是当前工业界最常用的量化路径。

QAT方法在训练阶段即引入量化噪声的模拟，通过反向传播让网络权重对量化误差进行自适应。QAT的实施成本高于PTQ，需要重新训练或微调；但在4位甚至更低的低比特量化场景下，QAT通常能将精度损失控制在比PTQ显著更低的水平。当前QAT已能在8位量化下保持几乎与FP32一致的精度，并将4位量化的精度损失控制在可接受范围。

如图 2.2 所示单层神经网络量化计算原理。在INT8量化推理中，权重通常被量化至8位，偏置量化至32位以避免大数累加溢出。卷积或全连接层执行8位定点乘法，其结果累加进32位部分和；累加完成后与32位偏置相加，再经过激活函数与重量化逻辑得到8位输出，可直接作为下一层的输入。在硬件实现中，8位乘法器的面积约为32位浮点乘法器的十分之一以下，乘加阵列的功耗也相应大幅下降。因此，INT8量化是端侧CNN加速器最常见的数据精度选择。

### 2.8 本章小结

本章介绍了卷积神经网络与硬件加速的相关理论原理。首先介绍了卷积神经网络的基本层结构，包括卷积层、池化层、全连接层的计算公式与多通道展开方式，明确了卷积层在整网计算量中的主导地位。其次介绍了卷积加速器的三种典型数据流分类，包括权重静止、输出静止、行静止，分析了它们在复用对象、片上存储与互连复杂度上的差异。接着介绍了硬件加速中常用的基础技术，包括流水线设计、行缓存与环形缓冲、valid-ready握手协议以及AXI总线协议。随后介绍了多核体系结构中的非一致性内存访问原理，明确了多核加速器中本地存储节点与远端存储节点之间的访存延迟差距，以及由此引出的跨核片上直接搬运需求；同时介绍了寄存器、片上静态存储、片上块静态存储与片外动态存储四级存储层级在容量、带宽、延迟、单次访问能耗四个维度上的阶梯特征，以及片外动态存储访问能耗高达单次乘加运算约200倍的定量对照。最后介绍了模型量化的基本方法，包括训练后量化与量化感知训练两类主流方案，以及INT8量化在乘加阵列硬件实现中的位宽分配。这些内容是后续加速器架构设计与模块实现的理论基础。

---

## 3 总体方案

### 3.1 引言

面向端侧流式计算场景的卷积加速器FLUX_CNN在算力、功耗、面积与调度灵活性之间存在多重权衡。以端侧推理在小面积、低功耗下保持稳定吞吐为目标，加速器采用控制核与加速核分工的双层结构：加速核内由5个流水模块加1个共享配置寄存器堆通过valid-ready握手互联，对外仅暴露1个AXI4主接口与1个AXI-Lite从接口；多核扩展层提供$N=1$/2/4三种核数配置。调度方案采用核内寄存器配置与核外宏观指令流相结合的分层结构，并选用权重静止配合激活值滑窗复用与输出通道广播的复合数据流。下文围绕设计目标与约束、总体架构、调度方案与数据流选择四个层面分别展开。

### 3.2 设计目标与约束

端侧流式计算场景具有三个共性需求。一是输入数据持续到达，加速器需要支持任意H×W尺寸的特征图，而不是只能处理一张图后停机；二是供电与散热受限，单帧推理的能耗预算往往在毫焦量级；三是部署形态多样，从智慧城市的视频监控盒到工业相机的缺陷检测板，再到消费电子的手势识别芯片，对面积与成本敏感。这些需求共同决定了端侧加速器与云端加速器在设计目标上的差异：端侧不追求峰值TOPS，而追求在小面积、低功耗下保持稳定的实测吞吐。

第一个目标是算力—功耗—面积的均衡。本设计选用16×16的INT8乘加阵列（NUM_COL=NUM_PE=16，DATA=8 bit，PSUM=32 bit），共256个MAC单元，单时钟周期理论峰值256 ops。INT8量化后乘法器面积约为32位浮点的十分之一以下，单次乘加的能耗也大幅下降。乘加阵列规模选16×16而非更大，是因为端侧典型卷积层的Cin/Cout经常出现16量级（如ResNet早期层Cin=3、64，MobileNet深度卷积Cin=1），更大的阵列在这些层会出现严重的列空转，得不偿失。

第二个目标是支持任意输入尺寸而不让片上存储成为瓶颈。本设计将片上IFB（Input Feature Buffer）固定为8192 word、OFB（Output Feature Buffer）固定为2048 word，在编译期按行粒度切分，让IFB与OFB都退化为环形缓冲。一张480×640的VGA图像（约4.9 MB）只占用IFB中10 KB量级的滚动窗口，仍能在一次启动内单遍跑完，不需要把整图全部装入片上。

第三个目标是片上控制电路的简单性。中心FSM在层数变化、切片维度变化、输入尺寸变化时都需要重新设计状态机迁移图，工程代价高、复用性差。本设计令加速核内的5个流水模块各自维护本地计数器，模块之间用valid-ready握手串联，没有中心FSM；任何一段反压时，上游自动停顿、下游自动等待，整条流水以弹性方式松耦合协作。这一选择把"哪一层在哪一周期做什么"的复杂度从硬件状态机转移到编译器派生的配置寄存器与宏观指令流。

第四个目标是调度的层次化与可扩展。单层卷积的循环结构包含cout_slice/cin_slice/yout/xout/kx/ky/pe七层嵌套，本设计将这七层循环静态映射到固定硬件，由编译器在配置寄存器中写入各层循环边界；多层之间的串接、多核之间的协同则通过宏观指令流（Task Descriptor）描述，不依赖主机逐层介入。

### 3.3 总体架构

针对上述设计目标，FLUX_CNN加速器的总体结构如图3.1所示。系统整体采用控制核与加速核分工的层次化结构：控制核负责系统级管理、外部数据接口与宏观调度，加速核（以下简称核）专注卷积层的高效计算。控制核与核之间通过AXI4总线连接，AXI-Lite通道用于配置寄存器读写，AXI4完整通道用于权重、特征图与描述符的批量搬运。

**图 3.1 加速器系统总体架构图**
**Figure 3.1 Overall system architecture of the accelerator**

加速器对外仅暴露两个接口：一个AXI4完整主接口与一个AXI-Lite从接口。AXI4主接口连接外部DDR控制器，承担IFM、OFM、权重、偏置与残差数据的批量搬运；AXI-Lite从接口连接控制核，用于核启动信号、状态寄存器与少量启动期参数的读写。这一接口形态使加速器易于挂接到Zynq、定制SoC或其他ARM/RISC-V控制核为主的端侧平台，集成边界清晰。

加速核内部由5个数据通路模块加1个共享配置寄存器堆（cfg_regs）组成。如图3.2所示核内数据通路结构。第一个模块是行缓存模块（line_buffer），承担将片外像素流转为片上K×K卷积窗口的职能，输出激活值向量给MAC阵列；同时维护行级环形缓冲并通过行信用机制向IDMA反压。第二个模块是MAC阵列模块（mac_array），由16列×16 PE共256个INT8乘加单元组成，列间广播激活值，每列对应一个输出通道，列内16 PE沿Cin方向并行累加，单周期消耗1拍激活值向量与16列权重，输出16路部分和。第三个模块是部分和累加模块（parf_accum），对来自MAC阵列的部分和按cin切片维度累加；累加器拆分为16个独立SRAM实例（每列一个，外壳共享写地址与写使能），每列容量PARF=32，匹配输出通道广播的数据流。第四个模块是输出特征图写出模块（ofb_writer），负责接收累加完成的部分和，依次完成偏置加、量化移位、饱和截断到INT8、可选的残差融合，再写入OFB。第五个模块是权重缓存模块（wgt_buffer），从权重缓冲（WB，1024 word）侧路向MAC阵列各列分发权重，配合权重静止数据流让权重在一段时间内保持稳定。

5个模块之间用valid-ready握手互联，任意一方反压时另一方自动停顿。模块各自维护本地计数器，依次走完yout/xout/kx/ky/cin/cout_slice/cin_slice循环；不同模块的计数器自然形成流水关系，没有中心仲裁。模块边界处的弹性拼接使得反压期间在途数据不丢失，相邻tile之间也允许填充与排空重叠，相邻轮次之间允许指针跨界连续推进，提升了整条流水线的稳态利用率。

DMA子系统位于加速核外、对接AXI4主接口。如图3.3所示DMA子系统结构。DMA子系统由1个Xilinx axi_dm IP（AXI DataMover）加多个自研控制器组成。axi_dm提供MM2S与S2MM两条搬运通道：MM2S通道由idma_ctrl（拉取IFM）与wdma_ctrl（拉取权重）通过mm2s_arb串行仲裁共享，S2MM通道由odma_ctrl（写出OFM）独占。另有rdma_ctrl负责拉取偏置与残差数据，dfe（描述符获取引擎）负责按宏观指令流拉取描述符链表。多个控制器的请求经axi_m_mux聚合后挂到唯一的AXI4主接口。这一组织方式让加速器对外只表现为一对AXI接口，简化SoC集成的同时保留了内部多通道并行的能力。

cfg_regs是加速核与外部交互的配置寄存器堆，采用双写口设计：一个写口接AXI-Lite CSR，由控制核写入启动期4个寄存器；另一个写口接sequencer，由sequencer消费dfe拉取的CFG_WRITE描述符流写入逐层配置（dfe仅负责把描述符链表从DDR拉进描述符FIFO，CFG_WRITE类型的描述符再由sequencer从FIFO中取出并写入cfg_regs的描述符写口）。这样控制核与描述符通路互不干扰，描述符流可以批量搬运一层卷积所需的50余个配置寄存器值，控制核只需在启动时写4个寄存器并触发一次启动。

为支持更高吞吐，加速器在加速核之上提供多核扩展层（multicore_top）。多核扩展层包含*N*个加速核（$N=1$/2/4已综合通过）、AXI互连交换（基于Xilinx SmartConnect IP）、AXI-Lite一对多分发。多核之间采用W维切片调度：每核处理一段每核子图像宽度子图加上必要的边界冗余列；相邻核之间通过跨核SRAM直送把上一层OFM直接写入下一核的IFB区域，无需经过外部DDR中转。多核扩展层不改变单核数据通路，只在最外层包络上接入*N*份加速核与互连，单核与多核共享同一套cfg_regs与编译器派生流程。

### 3.4 调度方案

加速器的核心命题是软硬协同的分层调度：把卷积循环嵌套这一具有静态结构的部分留给硬件直接展开，把任务级的灵活性留给编译器与宏观指令流。这样硬件不必在每层卷积参数变化时重新设计状态机，编译器也不必关心每周期PE该做什么。

第一层调度位于核内，基于配置寄存器与6层嵌套自循环FSM。一层卷积的硬件状态由cfg_regs内50余个配置寄存器完整描述，包括循环边界（H_in、W_in、Cin、Cout、K、stride、pad）、片上缓冲基址（IFB/WB/OFB/bias/shortcut）、量化参数（移位量、零点、饱和上下限）以及调度参数（cin_slice数、cout_slice数、tile宽度等）。一旦配置寄存器写入完成并触发start_layer，核内6层嵌套循环（cs → yout → tile → cins → round → pos）依次推进：cs指cout_slice，遍历输出通道切片；yout是输出行；tile是W维tile；cins是cin_slice累加；round是同一tile内的累加轮次；pos是PE阵列位置。每一层循环由对应模块的本地计数器自维护，循环边界来自cfg_regs，循环嵌套关系隐含在模块互连拓扑中。一层卷积处理完成后，核拉起done_sticky状态位，等待下一次启动。

第二层调度位于核外，基于宏观指令流与任务描述符（Task Descriptor）。多层卷积的串接通过描述符链表组织：每层对应若干描述符，分为IFM拉取（IDMA_DESC）、权重拉取（WDMA_DESC）、配置寄存器写入（CFG_WRITE）、输出写出（ODMA_DESC）等类型。描述符链表预先由编译器生成、放置在DDR中，描述符获取引擎dfe在启动后自动按链表顺序拉取并分发到对应控制器。控制核在启动时只需写入4个启动寄存器（描述符链表基址、链表长度、控制位等），然后触发start_dfe，整条多层链路可在不需要逐层介入的情况下跑完。这一设计将控制核的AXI-Lite写次数从原本的50余次/层降到4次/层，显著降低主机侧调度负担。

第三层调度跨多核展开，仍然走宏观指令流。多核场景下，编译器在调度阶段为每一核独立生成一份描述符链表，链表中包含本核处理的W切片范围、所需的边界冗余列、跨核SRAM直送的目标地址等。核间的同步通过cfg_regs中的握手寄存器与跨核SRAM直送的写完成信号完成，不需要中央调度器。各核启动后并行运行，在层边界处通过一次跨核握手交换准备状态，进入下一层。

层次化调度的设计选择让加速器具备较强的可扩展性。增加新的卷积变体（不同*K*、stride、Cin、Cout组合）不需要改RTL，只需扩展编译器；增加新核数（$N=1$ → 2 → 4）也不需要改单核RTL，只需扩展多核扩展层与编译器侧的切片逻辑。硬件的复杂度集中在循环嵌套的静态映射上，可在综合后稳定下来。

### 3.5 数据流选择

加速核的数据流选择需要在权重复用、激活值复用、部分和复用三者之间作出取舍。本设计选用权重静止（Weight-Stationary，WS）配合激活值滑窗复用与输出通道广播的复合数据流。

权重静止的核心思想是：在一段时间内，让每个PE持有的权重保持不变，重复用于多次乘加运算。WS在卷积层中天然成立，因为同一组权重要在整张OFM的所有空间位置上重复使用一次。在本设计中，权重静止体现在权重缓冲WB与权重寄存器堆WRF=32的双层结构：一层卷积启动时，wgt_buffer把当前cout_slice所需的全部权重一次性从WB读入WRF；之后整个cout_slice内所有空间位置的乘加都直接消费WRF内的权重，不再访问WB。权重静止使片外权重带宽与计算频率解耦，匹配端侧场景下权重数据量大但访问规整的特征。

激活值滑窗复用利用了相邻空间位置卷积窗口的列重叠性：相邻空间位置的卷积窗口存在K-stride列的重叠，重叠部分的激活值不需要重复读取。本设计通过行缓存模块的环形缓冲实现激活值复用：当K=3、stride=1时，相邻两个yout的窗口共享2行，相邻两个xout的窗口共享2列，行缓存模块只需在每个新行到来时写入一行，每个新列推进一个写指针，K×K窗口数据的访存量由K²降至接近1。激活值寄存器堆ARF=32缓存当前窗口供PE重复读取，使PE端的激活值访问集中在ARF内部，行缓存的访问频度随之下降。

输出通道广播则让同一空间位置的所有输出通道共用激活值：对同一个空间位置，所有输出通道使用相同的激活值乘以不同的权重。本设计将16列PE配置为16个并行输出通道，每周期由行缓存模块广播1拍激活值向量到16列，每列消费各自的权重产生1路输出通道的部分和。输出通道广播让激活值的片上分发开销与输出通道数解耦：增加输出通道维并行度只需要增加列数，不需要复制激活值通路。

权重静止与输出通道广播的组合让16×16 PE阵列在片上呈现"每列一个输出通道、每列内沿Cin方向16 PE并行"的二维结构，与卷积循环嵌套中的cout维与cin维自然对齐。部分和累加沿列内方向走cin_slice累加，每列累加器使用独立SRAM，使16列累加在结构上彼此隔离、不发生端口冲突。

值得注意的是，输出通道广播在物理实现层面带来一个结构性特征：每周期同一行激活值向量被16列同时消费，列与列之间共享同一拍激活值数据，仅权重各自独立。这一"激活值跨列共享、权重列内独立"的对称性是输出通道广播数据流在物理实现层面的固定性质。

### 3.6 本章小结

本章给出了FLUX_CNN加速器的总体方案。面向端侧流式计算场景下算力—功耗—面积均衡、任意输入尺寸支持、片上控制简单性与调度层次化四项约束，加速器在整体上采用控制核与加速核组成的双层结构：加速核内部由5个数据通路模块加1个共享配置寄存器堆通过valid-ready握手互联，外部仅暴露1个AXI4主接口与1个AXI-Lite从接口；多核扩展层在不改动单核RTL的前提下提供$N=1$/2/4的核数可选。调度方案在核内采用配置寄存器与6层嵌套自循环FSM完成单层全部循环嵌套展开，在核外采用宏观指令流与任务描述符链表完成跨层与跨核任务编排，使硬件保持最简静态结构而灵活性集中在编译器侧。数据流层面，加速核选用权重静止配合激活值滑窗复用与输出通道广播的复合数据流，将权重带宽与计算频率解耦、把窗口复用集中至行缓存环形结构、把输出通道并行度展开至16列PE，并由此带来相邻列共享激活值的结构性特征。这些方案共同构成FLUX_CNN加速器具体实现的整体框架，模块级实现细节将在后续具体展开。

---

## 4 加速器具体实现

### 4.1 引言

FLUX_CNN加速器的具体实现按模块顺序展开：加速核内5个数据通路模块（行缓存模块、MAC阵列模块、部分和累加模块、SDP后处理模块、权重缓存模块）承担窗口生成、乘加计算、部分和累加、量化后处理与权重供给五项职责；配置寄存器与6层嵌套自循环FSM协同完成单层全部调度；编译器侧的Y维折叠与空间到深度（S2D）两项变换覆盖小Cin与大步长两类低利用率场景；DMA子系统、AXI接口集成与多核W切片扩展层承接片外数据搬运与多核扩展；最后给出16×16 INT8 MAC阵列在DSP48E1块上的跨列复用映射方案。

### 4.2 行缓存模块

行缓存模块（line_buffer）承担将片外像素流转换为片上$K \times K$卷积窗口的职能，是片外IFM与MAC阵列之间的桥梁。如图4.1所示行缓存模块结构图。

**图 4.1 行缓存模块结构图**
**Figure 4.1 Line buffer module structure**

行缓存模块的存储核心是一块容量为IFB=8192 word的输入特征缓冲（Input Feature Buffer，IFB），按行粒度组织为环形缓冲。每行宽度为*W_in*，所需行数为*strip_rows*，由编译器在cfg派生阶段算出，使*strip_rows*×*W_in*个word不超过IFB容量。环形缓冲在物理层面是一段连续SRAM，逻辑层面通过模*strip_rows*取地址实现。

行缓存模块的实现流程分为三步。第1步是行写入：IDMA控制器（idma_ctrl）按行向IFB写入新行，每写完一行向行缓存模块汇报"写指针+1"。第2步是窗口读取：行缓存模块按(*y_out*, *x_out*)输出位置组合，读出对应的$K \times K$窗口，沿Cin维聚成16元向量后向MAC阵列发射。第3步是行释放：当某行不再被任何*y_out*窗口需要时，行缓存模块向IDMA控制器拉起row_credit，释放该行的ring槽位，IDMA控制器据此发起下一行的搬运。

行缓存模块的反压机制采用前向压力（forward-pressure）发射策略：仅当 `rows_available ≥ y_out × stride + K_y` 时才发射本行的窗口流，否则停顿等待IDMA写入。该策略确保任意*H*×*W*输入特征图都可在一次start内单遍跑完，片上仅占用*strip_rows*×*W_in*个word的ring容量，不需要把整图缓存到片上。一张480×640的VGA图像约4.9 MB，IFB仅占用10 KB量级即可流式跑完，环形缓冲读写指针在跨round处不复位，相邻tile与相邻round之间允许指针连续推进。

行缓存模块对外提供两路valid-ready接口：上游接idma_ctrl的写口，下游接mac_array的窗口口；上游反压通过row_credit实现，下游反压通过窗口valid-ready握手实现。模块内部不维护中心FSM，仅由(*y_out*, *x_out*, *k_x*, *k_y*)四级本地计数器自维护循环边界，循环边界由cfg_regs提供。

行缓存模块的输出端紧接一组激活值寄存器堆（Activation Register File，ARF=32），介于IFB与MAC阵列模块之间，承担窗口内激活值的局部缓存与PE像素内循环的数据供给。其设计动机有二：其一，单个输出位置(*y_out*, *x_out*)的$K \times K$邻域在MAC阵列内被反复消费$K^2$×cin_pe拍，若每拍直接读取IFB SRAM，将引入与峰值算力同量级的SRAM读功耗与端口竞争；将这部分高频读取迁移至紧邻PE的小容量寄存器堆后，IFB SRAM在像素内循环中保持静止，仅在跨像素切换窗口时才被访问。其二，ARF为MAC阵列模块提供一份与PE阵列流水节拍对齐的稳定数据视图：当(*k_x*, *k_y*, cin_pe)三级本地计数器在ARF视图内推进时，行缓存模块与IFB读出通路均无需参与，使像素内循环的发射节拍与外层(*y_out*, *x_out*)循环、外层IDMA行写入彻底解耦。ARF容量32项与cfg_regs中ARF=32一致，覆盖一个$K \times K$窗口在Cin方向并行展开的全部激活值，加载完成后整段像素内循环不再触发IFB读出。

### 4.3 MAC阵列模块

MAC阵列模块（mac_array）是加速核的算力核心，承担卷积层的乘加运算。如图4.2所示MAC阵列模块结构图。

**图 4.2 MAC阵列模块结构图**
**Figure 4.2 MAC array module structure**

MAC阵列模块由16列×16 PE共256个INT8乘加单元（NUM_COL=NUM_PE=16，DATA=8 bit）组成。16列在结构上彼此独立，每列对应一个输出通道；列内16个PE沿Cin方向并行排布，承担单输出通道在16个输入通道方向上的并行乘加。这一二维结构与卷积循环嵌套中的cout维与cin维自然对齐：列方向广播激活值、列内方向沿cin累加。

MAC阵列模块采用权重静止（Weight-Stationary，WS）+激活值滑窗复用+输出通道广播的复合数据流。第1步权重加载：在一次cout_slice启动时，权重缓存模块（wgt_buffer）将当前cout_slice所需的全部权重从权重缓冲（Weight Buffer，WB）一次性读入权重寄存器堆（Weight Register File，WRF=32），整段cout_slice内的全部空间位置共享这份权重，不再访问WB。第2步激活值缓存：行缓存模块输出的当前窗口激活值向量进入激活值寄存器堆（Activation Register File，ARF=32），ARF内的激活值供同一窗口在*k_x*/*k_y*子周期内重复读取。第3步广播与乘加：每周期由ARF广播1拍激活值向量到16列，每列消费各自的WRF项产生1路输出通道的16路PE并行乘积，列内16路乘积经一棵加法树规约为1路PSUM=32 bit部分和。第4步部分和发射：每周期16列同时向部分和累加模块发射16路PSUM。

MAC阵列模块的并行度可分解为三个维度。Cout维并行度16（列数）、Cin维并行度16（每列内PE数）、Kx/Ky维顺序展开（同一窗口在$K^2$个子周期内顺次计算）。理论峰值算力为256 ops/cy × Fmax。当Cin<16时，Cin维存在列空转，需配合编译器侧Y维折叠优化；当Cout<16时，硬件不复用，对应PE列空转，不在此层做硬件级优化。

MAC阵列模块对外提供三路接口。上游接行缓存模块的窗口口（激活值向量valid-ready）；侧路接权重缓存模块的权重口（权重广播valid，无反压）；下游接部分和累加模块的部分和口（16路PSUM valid-ready）。模块内部由(*k_x*, *k_y*, *cin_pe*)三级本地计数器自维护，循环边界由cfg_regs提供。每个PE由1个INT8乘加器构成，256个PE构成本章MAC阵列的核心计算资源。

### 4.4 部分和累加模块

部分和累加模块（parf_accum）位于MAC阵列模块下游，对16列发射的部分和按cin_slice维度累加，等当前(*y_out*, *x_out*)输出位置在所有cin_slice上累加完毕后向SDP后处理模块发射。如图4.3所示部分和累加模块结构图。

**图 4.3 部分和累加模块结构图**
**Figure 4.3 Partial-sum accumulator module structure**

部分和累加模块的存储核心是部分和寄存器堆(Partial-Sum Register File，PARF=32)，逻辑容量为16列×32项。在物理实现上，本设计将PARF拆分为16个独立SRAM实例（parf_col × NUM_COL=16），每列一个SRAM，避免16列共用一个大SRAM时的端口冲突。16列共享同一组写地址（wr_addr）与写使能（we），由外壳模块统一驱动；每列的写数据来自本列的mac_array输出PSUM，每列的读数据独立返回，各列的累加在结构上完全隔离。

部分和累加模块的实现流程分为三步。第1步累加器读出：当cin_slice索引大于0时，从PARF当前(*tile_w*, *cout_col*)槽位读出已累加的部分和。第2步加法：将本周期mac_array发射的16路PSUM与读出的累加值相加，结果回写PARF同一槽位；当cin_slice=0时跳过读出，直接写入新值。第3步累加完成发射：当一个(*tile_w*, *cout_col*)槽位的所有cin_slice累加完毕（由cfg_regs中的cin_slice_total边界标识），将累加结果通过valid-ready握手发射到SDP后处理模块。

PARF=32容量对应一个cout_slice内32个*tile_w*输出位置的部分和缓存。当*tile_w*输出数大于32时，编译器会进一步切分tile，由6层FSM中的tile维循环承担。每列独立SRAM让16列累加在物理端口上完全隔离，匹配MAC阵列模块输出通道广播的数据流形态。

部分和累加模块对外提供两路接口。上游接MAC阵列模块的部分和口（16路PSUM valid-ready）；下游接SDP后处理模块的累加完成口（16路PSUM valid-ready）。模块内部由(*tile_w*, *cin_slice*)二级本地计数器自维护，循环边界由cfg_regs提供，cin_slice=0与cin_slice_total-1两个边界条件分别控制累加器初值与发射时机。

### 4.5 SDP后处理模块

SDP后处理模块（ofb_writer）位于部分和累加模块下游，对累加完成的32位部分和依次完成偏置加、量化移位、饱和截断到INT8、可选的残差融合，再写入输出特征缓冲（Output Feature Buffer，OFB=2048 word），承担INT8量化推理通路上从PSUM到OFM的后处理链。如图4.4所示SDP后处理模块结构图。

**图 4.4 SDP后处理模块结构图**
**Figure 4.4 SDP post-processing module structure**

SDP后处理模块的实现流程分为五步。第1步偏置加：从偏置寄存器堆（bias_rf）读出当前输出通道的32位偏置，与累加完成的32位部分和相加。偏置由RDMA控制器从DDR中预先拉入bias_rf。第2步量化移位：对偏置加结果按cfg_regs中配置的右移量进行算术右移，将32位部分和按层量化尺度缩放到目标位宽。第3步饱和截断：对移位结果按上下限（INT8通常为\[−128, 127\]）做饱和裁剪，超出范围的值钳位到边界。

第4步残差融合：当当前层启用残差时，从残差缓冲（Shortcut Bank）读出对应空间位置的残差值（INT8），按shortcut_mult/shortcut_shift进行通道级缩放后与饱和结果相加，结果再次饱和到INT8。残差缓冲由上一层的OFM直接写入，无需经主机介入。第5步OFB写入：将最终INT8结果按(*y_out*, *x_out*, *c_out*)地址写入OFB ring。

SDP后处理模块在ofb_writer内集成偏置加、量化移位、饱和、残差融合，使ResNet残差通路在加速器内部一站式完成。Shortcut Bank与bias_rf让残差加与偏置加都在片上完成，主机不需要为每层介入；ofb_writer内部的SDP链以全INT8后处理形式落地（PSUM→bias→shift→clip→residual→clip→OFB），单层cfg_regs配置完成后整段后处理无需进一步指令。

SDP后处理模块的OFB写入端同样按行粒度组织为环形缓冲。每写完一行向ODMA控制器（odma_ctrl）汇报"写指针+1"，ODMA控制器据此发起S2MM突发写入DDR。当ODMA排空一行后向ofb_writer拉起row_credit，让ofb_writer继续写入下一段。OFB ring的*strip_rows*由编译器派生，与IFB ring相互独立。

SDP后处理模块对外提供三路接口。上游接部分和累加模块的累加完成口；侧路接bias_rf与Shortcut Bank两个寄存器堆；下游接OFB写口与ODMA行信用口。模块内部由(*y_out*, *x_out*, *cout_col*, *cout_slice*)四级本地计数器自维护，循环边界由cfg_regs提供。SDP量化组合链（偏置加 → 移位 → 饱和 → 残差 → 饱和）目前在单stage内组合实现，未做stage切分。

#### 4.5.1 Shortcut Bank残差通路

ResNet系列网络的残差连接要求当前层的卷积输出与若干层之前的特征图按位融合。若每次残差融合都从片外DDR重拉残差像素，将引入与卷积主通路同量级的输入带宽开销，残差路径的访存延迟亦会与主路径串行叠加。SDP后处理模块在ofb_writer内集成残差缓冲（Shortcut Bank）作为残差通路的片上输入端，使残差融合在加速器内部一站式完成，无需主机或主路径以外的访存介入。

Shortcut Bank的存储核心是一块容量为8192 word的SRAM，与IFB容量同量级，按行粒度组织为环形缓冲，按(*y_out*, *x_out*, *c_out*)地址按位读取INT8残差像素。在多核扩展层$N=4$配置下，按多核W切片的容量划分，每核Shortcut Bank的有效容量缩至2048 word\[CHECK: 多核配置下Shortcut Bank的逐核容量分配以params.py与综合报告为准\]。

Shortcut Bank的填充由前序层的输出搬运直接完成。编译器在描述符链生成阶段决定哪一层的OFM作为残差源，将该层odma_ctrl的目标地址改为后续残差融合层的Shortcut Bank物理地址；ODMA控制器据此发起S2MM突发写入Shortcut Bank。残差缓冲填充复用前序层OFM落盘的写带宽，不经主机介入，不引入额外片外读带宽。

Shortcut Bank的消费由SDP后处理链的第4步残差融合触发。当cfg_regs中残差启用位拉起时，ofb_writer在完成"偏置加→量化移位→饱和截断到INT8"三步后，按当前(*y_out*, *x_out*, *c_out*)地址从Shortcut Bank读出对应位置的INT8残差像素，按cfg_regs中shortcut_mult与shortcut_shift两参数完成通道级缩放（乘加+算术右移），与本层量化结果按位相加，结果再次饱和到INT8后送入OFB写入步骤。整段残差融合在ofb_writer内组合实现，残差通路与主累加通路在SDP链内汇合，不引入额外的握手层级。

### 4.6 权重缓存模块

权重缓存模块（wgt_buffer）侧路供给MAC阵列模块的权重，配合权重静止数据流让权重在一段时间内保持稳定。如图4.5所示权重缓存模块结构图。

**图 4.5 权重缓存模块结构图**
**Figure 4.5 Weight buffer module structure**

权重缓存模块的存储核心是权重缓冲（Weight Buffer，WB=1024 word）与权重寄存器堆（Weight Register File，WRF=32）双层结构。WB是较大容量的SRAM，存放一层卷积所需的全部权重；WRF是更小、更靠近PE的寄存器堆，缓存当前cout_slice所需的权重。

权重缓存模块的实现流程分为两步。第1步WB加载：在一层卷积启动时，WDMA控制器（wdma_ctrl）按(*c_out*, *c_in*, *k_x*, *k_y*)顺序将该层全部权重从DDR经MM2S通道写入WB。第2步WRF加载：在每个cout_slice启动时，权重缓存模块将本cout_slice所需的16列×32项权重从WB一次性读入WRF；之后整段cout_slice内所有空间位置的乘加都直接消费WRF内的权重，不再访问WB。

WB容量按一层最大权重需求设计，每层加载一次；WRF容量按一个cout_slice设计，cout_slice内反复使用，稳态下不再访问DDR。

权重缓存模块对外提供两路接口。上游接WDMA控制器的写口；侧路接MAC阵列模块的权重广播口（无反压）。模块内部由(*c_out*, *c_in*, *k_x*, *k_y*)四级本地计数器自维护，循环边界由cfg_regs提供。

### 4.7 配置寄存器与6层嵌套自循环FSM

加速核内的层级控制由配置寄存器（cfg_regs）与6层嵌套自循环FSM（sequencer）协同完成。如图4.6所示配置寄存器与6层嵌套自循环FSM结构图。

**图 4.6 配置寄存器与6层嵌套自循环FSM结构图**
**Figure 4.6 Configuration registers and 6-level nested self-loop FSM structure**

cfg_regs是加速核与外部交互的配置寄存器堆，承载一层卷积的全部静态参数。其内部包含约50余个寄存器，可分为四类：循环边界类（*H_in*、*W_in*、*C_in*、*C_out*、*K*、stride、pad）、片上缓冲基址类（IFB/WB/OFB/bias/shortcut基址）、量化参数类（移位量、饱和上下限）、调度参数类（cin_slice数、cout_slice数、tile宽度、strip行数）。一层卷积启动前，整段cfg一次性写入完成；启动后整层硬件状态由cfg完整描述，运行期间不再变化。

cfg_regs采用双写口设计。第1个写口接AXI-Lite CSR（csr_w），由控制核写入启动期4个寄存器（描述符链表基址、链表长度、控制位、start_dfe触发位）。第2个写口接sequencer（seq_w），由sequencer消费描述符获取引擎（DFE）拉取的CFG_WRITE描述符流写入逐层配置。两路写口互不干扰，控制核与描述符通路在结构上隔离。这一设计将控制核的AXI-Lite写次数从50余次/层降到4次/层，多层链式调用时控制核仅需在系统启动时写一次4个寄存器并触发一次start_dfe。

sequencer实现6层嵌套自循环FSM。一层卷积处理过程由(cs → yout → tile → cins → round → pos) 6层循环依次推进：cs是cout_slice，遍历输出通道切片；yout是输出行；tile是*W*维tile；cins是cin_slice累加；round是同一tile内的累加轮次；pos是PE阵列位置。每一层循环由对应模块的本地计数器自维护，循环边界来自cfg_regs，循环嵌套关系隐含在模块互连拓扑中——行缓存模块走yout/tile，MAC阵列模块与权重缓存模块走round/pos，部分和累加模块走cins，SDP后处理模块走cs。模块之间通过valid-ready握手互联，没有中心FSM协调。

sequencer的另一项职责是描述符流处理。DFE在系统启动后自动按描述符链表顺序从DDR拉取描述符，并按类型分发：IDMA_DESC送入idma_ctrl，WDMA_DESC送入wdma_ctrl，ODMA_DESC送入odma_ctrl，CFG_WRITE描述符则由sequencer从desc_fifo中取出并写入cfg_regs的seq_w端口。一层卷积的描述符段长度可达50余个CFG_WRITE，desc_fifo容量从32扩到128以容纳一层的完整cfg。

层完成后sequencer拉起done_sticky状态位，控制核可通过AXI-Lite读STATUS寄存器或接GIC level IRQ检测层完成。done_sticky在start_layer上升沿自动清零，避免与下一层启动产生竞态。

### 4.8 编译器优化：Y维折叠（Ky-fold）

针对$C_{\mathrm{in}} < \mathrm{NUM\_PE} = 16$的卷积层（如ResNet早期层$C_{\mathrm{in}} = 3$、MobileNet深度卷积$C_{\mathrm{in}} = 1$），MAC阵列模块在Cin维存在严重列空转，理论PE利用率仅$C_{\mathrm{in}} / 16$。在编译器侧提出Y维折叠（Ky-fold）优化，将$K_y$（卷积$Y$方向）维度折叠到Cin方向，在不改动RTL的前提下提升Cin维利用率。
Y维折叠的数学等价关系如下。原卷积按(1)式计算：
$Y(y, x, c_o) = \sum_{k_y=0}^{K-1} \sum_{k_x=0}^{K-1} \sum_{c_i=0}^{C_{\mathrm{in}}-1} X(y+k_y,\, x+k_x,\, c_i) \cdot W(k_y,\, k_x,\, c_i,\, c_o)$    (1)
为按NUM_PE=16的硬件cin维容量分组折叠，定义每组容纳$g_y = \lfloor \mathrm{NUM\_PE} / C_{\mathrm{in}} \rfloor$个$k_y$行（以$C_{\mathrm{in}}=3$为例，$g_y = \lfloor 16/3 \rfloor = 5$），剩余$k_y$维分到$k_{y,\mathrm{per}} = \lceil K / g_y \rceil$组顺次执行。将组内$k_y$维与$c_i$维合并为cin_fake维，得到(2)式：
$Y(y, x, c_o) = \sum_{g=0}^{k_{y,\mathrm{per}}-1} \sum_{k_x=0}^{K-1} \sum_{c_{f}=0}^{g_y \cdot C_{\mathrm{in}}-1} X'(y,\, x+k_x,\, g,\, c_{f}) \cdot W'(k_x,\, g,\, c_{f},\, c_o)$    (2)
其中$c_{f} = (k_y \,\mathrm{mod}\, g_y) \times C_{\mathrm{in}} + c_i$，$g = \lfloor k_y / g_y \rfloor$，$X'(y, x, g, c_{f}) = X(y + g \cdot g_y + (k_y \,\mathrm{mod}\, g_y),\, x,\, c_i)$（将$y$方向偏移复制到cin维），$W'(k_x, g, c_{f}, c_o) = W(k_y, k_x, c_i, c_o)$。等效卷积的硬件视角变为$K_{y,\mathrm{eff}} = k_{y,\mathrm{per}}$、$C_{\mathrm{in,eff}} = g_y \cdot C_{\mathrm{in}}$的卷积。当$K \cdot C_{\mathrm{in}} \leq \mathrm{NUM\_PE}$（如ResNet首层$K=3$、$C_{\mathrm{in}}=3$）时$k_{y,\mathrm{per}} = 1$，整个$K_y$维一次折入，cin维上PE利用率提升至$K \cdot C_{\mathrm{in}} / 16$；当$K \cdot C_{\mathrm{in}} > \mathrm{NUM\_PE}$（如$K=7$、$C_{\mathrm{in}}=4$）时$k_{y,\mathrm{per}} > 1$，剩余$k_y$维仍走硬件$k_y$顺序展开（详见docs/pe-fold.md）。
Y维折叠的实现完全在编译器侧。第1步条件判定：scheduler在编译期判断$C_{\mathrm{in}} < 16 \land K > 1$时启用Y维折叠。第2步权重重排：编译器把权重张量按cin_fake索引重排，在DDR中按硬件视角的等效卷积布局摆放。第3步特征图复制：在Y维折叠路径下，输入特征图按$k_y$偏移在cin维上复制，由IDMA控制器按重排后的布局拉取。第4步cfg派生：scheduler用cin_fake维度算cin_slice数与循环边界，写入cfg_regs。整个过程无需RTL改动，硬件继续按“普通卷积”运行。
Y维折叠的代价是输入特征图在片外的存储量随$K_y$倍增——这是数据复制的直接代价。但由于权重静止数据流下激活值访问规模相对权重小，在ResNet早期层等场景下这一代价可被PE利用率提升所抵消。Y维折叠让硬件保持最简固定阵列，编译器侧承担相位重排与权重布局，无需RTL改动即可完成PE利用率优化。如图 4.9 所示为Y维折叠（Ky-fold）原理示意图。

**图 4.9 Y维折叠（Ky-fold）原理示意图**
**Figure 4.9 Ky-fold transformation principle**

### 4.9 编译器优化：空间到深度(S2D)

针对$\mathrm{stride} \geq 2$的卷积层，传统硬件实现会让$\mathrm{stride}^2$个相位的PE在大部分时间空转。在编译器侧提出空间到深度（Space-to-Depth，S2D）优化，将$(k_x \,\mathrm{mod}\, \mathrm{stride}, k_y \,\mathrm{mod}\, \mathrm{stride})$的$\mathrm{stride}^2$个相位折到cin维，等价转换为$\mathrm{stride}=1$的卷积，提升PE利用率。
S2D的数学等价关系如下。设原卷积参数为$K$、$\mathrm{stride}=s$，将输入$x$与$y$坐标按(3)式分解：
$y = q_y \cdot s + r_y,\; x = q_x \cdot s + r_x,\; \text{其中}\; r_y, r_x \in [0, s-1]$    (3)
对每个相位$(r_y, r_x)$，构造重排后的输入特征图$X'_{r_y, r_x}(q_y, q_x, c_i) = X(q_y \cdot s + r_y,\, q_x \cdot s + r_x,\, c_i)$，将$\mathrm{stride}^2$个相位沿cin维拼接，得到$C_{\mathrm{in,new}} = s^2 \cdot C_{\mathrm{in}}$的等效输入；同时把卷积核按相位重排为$K_{\mathrm{new}} = \lceil K/s \rceil$、$\mathrm{stride_{new}}=1$的等效卷积核。原卷积按(4)式等价于：
$Y(q_y, q_x, c_o) = \sum_{k_{y,\mathrm{eff}}=0}^{K_{\mathrm{new}}-1} \sum_{k_{x,\mathrm{eff}}=0}^{K_{\mathrm{new}}-1} \sum_{c_{i,\mathrm{new}}=0}^{s^2 \cdot C_{\mathrm{in}}-1} X'_{r_y, r_x}(q_y + k_{y,\mathrm{eff}}, q_x + k_{x,\mathrm{eff}}, c_i) \cdot W'(k_{y,\mathrm{eff}}, k_{x,\mathrm{eff}}, c_{i,\mathrm{new}}, c_o)$    (4)
S2D的核心性质是输入数据在DDR中只是按相位重排索引，不需要复制——这与Y维折叠的“y偏移复制”不同。S2D在编译器侧完成数据重排，硬件视角看到的是$K_{\mathrm{new}} \times K_{\mathrm{new}}$、$\mathrm{stride}=1$、cin加宽至$s^2 \cdot C_{\mathrm{in}}$的普通卷积，PE阵列在Cin维充分填充。
S2D的实现完全在编译器侧。第1步条件判定：scheduler在编译期判断$\mathrm{stride} \geq 2 \land K \geq \mathrm{stride}$时自动启用S2D；与Y维折叠判定独立，两者可叠加触发。第2步权重与特征图重排：编译器将$\mathrm{stride}^2$个相位的权重按$C_{\mathrm{in,new}}$索引拼接，将输入特征图按相位拼接为cin加宽形式，DDR中按重排后的布局摆放。第3步cfg派生：scheduler用$K_{\mathrm{new}}$、$\mathrm{stride}=1$、$C_{\mathrm{in,new}}$等效维度算cfg_regs，硬件按等效卷积运行。整个过程无需RTL改动，硬件继续按“普通卷积”运行。
S2D配合大stride的首层Patch卷积（典型如$K=4$、$\mathrm{stride}=4$）效果显著。ResNet11网络的Patch层（$K=4$、$\mathrm{stride}=4$）实测显示：未启用S2D时该层整网周期数为654,404，启用S2D后等效转换为$K=1$、$\mathrm{stride}=1$、$C_{\mathrm{in}}=64$标准卷积，PE行被填满，整网周期数降至129,594，单层加速5.05×。Y维折叠与S2D在scheduler.Layer中由force_fold/force_s2d自动决策，亦可由用户通过命令行选项强制启用。
S2D与Y维折叠的联合作用让编译器可以在不改动硬件的前提下，对$C_{\mathrm{in}}$小、stride大两类常见低利用率场景同时给出优化方案，硬件保持固定16×16阵列与最简cfg接口。

### 4.10 DMA子系统与AXI接口集成

DMA子系统位于加速核外，对接AXI4主接口，承担片外DDR与片上各缓冲区之间的批量数据搬运。如图 4.7 所示DMA子系统结构图。

**图 4.7 DMA子系统结构图**
**Figure 4.7 DMA subsystem structure**

DMA子系统的核心是1个Xilinx axi_dm IP（AXI DataMover，由Vivado 2023.1 IP集成生成），提供MM2S（Memory-Mapped to Stream）与S2MM（Stream to Memory-Mapped）两条搬运通道。axi_dm IP自身实现AXI4突发协议，对外暴露简化的命令—状态—数据流接口，简化自研控制器的实现。
DMA子系统的自研控制器分为四类。第1类是输入特征图DMA控制器（idma_ctrl），将IFM从DDR经MM2S通道写入IFB。第2类是权重DMA控制器（wdma_ctrl），将权重从DDR经MM2S通道写入WB。第3类是输出特征图DMA控制器（odma_ctrl），将OFM从OFB经S2MM通道写出到DDR。第4类是残差/偏置DMA控制器（rdma_ctrl），将偏置与残差数据从DDR经MM2S通道写入bias_rf与Shortcut Bank。
由于axi_dm IP仅有1条MM2S通道，idma_ctrl与wdma_ctrl通过mm2s仲裁器（mm2s_arb）串行共享。mm2s_arb实现简单的轮转仲裁：每次完成一段burst后切换到对端，避免idma长时段独占导致wdma饥饿。S2MM通道由odma_ctrl独占，无需仲裁。rdma_ctrl在多核扩展层中也参与MM2S通道竞争，由mm2s_arb统一仲裁。
DMA子系统对外仅暴露1个AXI4主接口，由AXI主口聚合器（axi_m_mux）实现。axi_m_mux把axi_dm的MM2S/S2MM主口与描述符获取引擎（DFE）的主口三路聚合后挂到唯一的AXI4主接口。这一组织方式让加速器对外的AXI拓扑与SoC内常见的“加速器+1主+1 Lite”结构对齐，简化集成。
AXI-Lite从口由AXI-Lite CSR模块（axi_lite_csr）实现。axi_lite_csr暴露4个启动期寄存器（描述符链表基址、链表长度、控制位、start_dfe触发位）以及若干状态寄存器（done_sticky、错误码等）给控制核。控制核在系统启动时写一次4个寄存器并触发start_dfe，整段多层卷积的层切换、权重切换、配置切换均由DFE与sequencer自动完成，无需控制核逐层介入。
描述符获取引擎（DFE）是DMA子系统与sequencer之间的桥梁。DFE在start_dfe拉起后自动从DDR中拉取描述符链表，按描述符类型分发：TYPE_IDMA送入idma_ctrl命令口、TYPE_WDMA送入wdma_ctrl命令口、TYPE_ODMA送入odma_ctrl命令口、TYPE_RDMA送入rdma_ctrl命令口、TYPE_CFG=0x3送入sequencer的cfg写口。描述符FIFO（desc_fifo）容量为128，可容纳一层完整cfg的全部CFG_WRITE描述符。一层卷积的描述符段顺序为：CFG_WRITE × N → IDMA_DESC → WDMA_DESC → RDMA_DESC → ODMA_DESC，由编译器在描述符链生成阶段确定。
DMA子系统采用Xilinx axi_dm IP替代原版自研DMA控制器，目的在于降低维护代价并改善与Vivado工具链协同；自研控制器仅承担命令拼装、地址生成与反压逻辑，AXI4突发协议交由IP完成。

### 4.11 多核W切片扩展

为支持更高吞吐与多核扩展，加速器在加速核之上提供多核扩展层（multicore_top）。如图 4.8 所示多核扩展层结构图。

**图 4.8 多核扩展层结构图**
**Figure 4.8 Multi-core extension layer structure**

多核扩展层包含$N$个加速核（$N=1/2/4$已综合通过）、AXI互连交换（基于Xilinx SmartConnect IP）、AXI-Lite一对多分发模块（axi_lite_1to4）。各核之间通过W维切片调度并行，核间不共享数据通路与控制FSM，仅在层边界通过握手寄存器同步。
W切片调度的实现流程分为三步。第1步切片分配：编译器在scheduler.analyze_slicing中按核数$N$把$W_{\mathrm{in}}$维分给$N$个核，每核处理一段sub_$W$子图。为保证卷积窗口在核边界处的语义一致性，每核额外保留边界冗余列：当$K=3$且$\mathrm{stride}=1$时，相邻核之间需要重叠2列输入；边界冗余列采用计算冗余方式实现（重叠输入像素分别在两核IFB中各存一份），不引入跨核同步开销。第2步非对称pad：边界核的外侧使用cfg中配置的真实pad，内侧使用0 pad（与重叠列衔接）；这一非对称pad由cfg_regs在多核场景下分别配置。第3步层边界握手：每核完成一层后拉起done_sticky，多核扩展层在所有核done_sticky拉起后才允许下一层启动。
跨核SRAM直送是多核扩展层的核心优化。生产者核的odma_ctrl在写出本层OFM时，目标地址不是DDR而是消费者核的IFB物理地址（按多核地址映射 `0x8000_0000 + i × 0x1000_0000`）。消费者核的IFB通过ifb_axi_slave模块挂载在AXI互连上，可从外部接收写入，将数据直接写入IFB ring。这一方式避免了上一层OFM写DDR、下一层IFM再从DDR读的双向带宽消耗，使多核加速比不被DDR带宽过早限制。
多核扩展层在代码组织上保持单核RTL不变，扩展通过外层完成：单核RTL（line_buffer/mac_array/parf_accum/ofb_writer/wgt_buffer/cfg_regs/sequencer/各DMA控制器）在$N=1/2/4$配置下完全相同，仅多核扩展层与编译器侧scheduler.analyze_slicing在不同$N$之间切换。增加新核数仅需扩展多核扩展层的外壳与AXI互连配置，不需要改单核RTL。多核扩展层目前已在$N=1/2/4$三种配置下完成综合\[CHECK: 综合环境与各配置下的资源占用以综合报告为准\]。

### 4.12 DSP块跨列复用映射方案

MAC阵列模块在OC-broadcast数据流下，每周期同一行激活值同时被16列消费、各列权重独立，相邻列在物理实现层面共享同一拍激活向量、仅权重不同。据此结构性特征，可把单核mac_array中相邻两列的INT8乘法器合并映射到一个DSP48E1块上，使整个16×16阵列的256个乘法器从查找表实现迁移至DSP块实现。该方案在保持数据通路对外接口与位级行为完全等价的前提下，由综合阶段在两种映射之间作选择。如图 4.10 所示DSP块跨列复用映射方案原理图。

**图 4.10 DSP块跨列复用映射方案原理图**
**Figure 4.10 Cross-column DSP-block mapping scheme**

DSP48E1块内部提供一个25 bit×18 bit有符号乘法器，其乘积位宽为43 bit。映射规则将相邻两列的两个8 bit权重$w_0$、$w_1$打包到25 bit A端：低8 bit放$w_0$、中间9 bit置零作为进位隔离位、高8 bit放$w_1$，可写为$A = \{w_1, 9'\mathrm{b}0, w_0\}$；将该行激活值$\mathit{act}$符号扩展为18 bit B端。一次乘法的43 bit乘积$P = A \times B$同时承载两路独立INT8乘积：低16位经位提取后作符号校正得$w_0 \times \mathit{act}$的有符号结果，高位段（按打包偏移取出）经位提取后同样作符号校正得$w_1 \times \mathit{act}$的有符号结果。9 bit进位隔离位保证了两路乘积在43 bit宽度内不会跨段串扰。符号校正逻辑承担将无符号视图与有符号视图之间的偏置项扣除，按$w_0$、$w_1$的符号位与$\mathit{act}$拼出常数补偿项即可，整段以查找表组合电路实现，不再调用乘法器资源。
按上述映射规则展开至阵列层，16×16阵列以8列对×16行的方式重组：每行的相邻两列共用同一DSP48E1块，行间结构独立。单核mac_array的乘法器从原查找表实现的256个INT8乘法器折合为8×16=128个DSP48E1块，$N=4$配置下整个加速器的MAC阵列共占用4×128=512个DSP块；上游接行缓存模块的窗口口、侧路接权重缓存模块的权重口、下游接部分和累加模块的部分和口三组接口的位宽、握手协议、循环边界与原查找表实现完全一致，加法树结构、PSUM位宽以及流水节拍亦保持不变。综合阶段可在两种映射之间选择走查找表实现还是DSP块实现，加速核外其余模块不感知该选择。
将MAC阵列乘法器从查找表实现迁移至DSP块实现，可使加速器整体的查找表占用显著下降、DSP资源利用率明显提高；与此同时，由于DSP块内乘法器为硬连线结构、其传播延迟短于查找表搭建的多级乘法树，关键路径的时序裕度也相应改善。该映射方案不进入端侧ASIC主配置，仅作为FPGA验证平台上的可选资源映射方案。

### 4.13 本章小结

本章按模块顺序给出了FLUX_CNN加速器的具体实现。加速核内的5个数据通路模块依次承担窗口生成、乘加计算、部分和累加、量化后处理与权重供给五项职责：行缓存模块以IFB=8192 word行级环形缓冲组织$K \times K$窗口生成，前向压力发射策略支持任意$H \times W$输入；MAC阵列模块由16列×16 PE共256个INT8乘加单元组成，采用权重静止+滑窗复用+输出通道广播复合数据流；部分和累加模块以16列独立SRAM实现PARF=32累加，每列共享写地址写使能；SDP后处理模块在ofb_writer内实现“偏置 → 移位 → 饱和 → 残差 → 饱和 → OFB”全INT8后处理链；权重缓存模块以WB=1024 word+WRF=32双层结构供给权重。
控制路径上，cfg_regs双写口设计与sequencer 6层嵌套自循环FSM协同完成单层全部调度：50余个cfg寄存器完整描述一层卷积，控制核仅写4个启动寄存器，描述符流由DFE自动消费。在此基础上，编译器侧的Y维折叠与空间到深度（S2D）两项PE利用率优化方案分别针对$C_{\mathrm{in}} < 16$与$\mathrm{stride} \geq 2$两类低利用率场景，将形状适配压力从硬件迁移至编译器，使硬件得以保持最简固定阵列。
DMA子系统（axi_dm IP+自研4类控制器+mm2s_arb+axi_m_mux+axi_lite_csr+DFE）承担片外数据搬运，对外仅暴露1个AXI4主接口与1个AXI-Lite从接口；多核W切片扩展层（$N=1/2/4$，边界冗余列+跨核SRAM直送）在不改动单核RTL的前提下完成核数扩展。此外，本章给出了16×16 INT8 MAC阵列在DSP48E1块上的两种可选映射方案，为综合阶段在查找表与DSP块之间作权衡保留了入口。

---

## 5 验证与性能分析

### 5.1 引言

本章对FLUX_CNN加速器进行实验验证，围绕功能正确性、综合可实现性与系统级性能三类指标展开测试与分析。验证按硬件平台与方法、功能仿真、综合实现、性能分析、对比验证五个层面分别展开：硬件平台与方法层面明确所采用的FPGA验证平台、综合与仿真工具链、PyTorch浮点参考模型与逐字节比较准则；功能仿真层面以ModelSim RTL仿真输出与PyTorch浮点参考逐位对比，覆盖单算子、多层链与整网三种验证粒度；综合实现层面在Vivado中给出资源占用、时序与功耗的报告口径与数值；性能分析层面以端到端推理时延、PE阵列利用率与多核扩展加速比为核心指标，结合控制变量实验定位性能瓶颈与调度策略影响；对比验证层面将FLUX_CNN的实测指标与已有CNN加速器在峰值算力、能效比、时延与资源利用率四个维度按可比口径折算后进行对照。

### 5.2 验证环境与方法

#### 5.2.1 验证环境

(1) 硬件平台
FLUX_CNN的最终实现目标是专用集成电路。为完成RTL级功能正确性与综合可行性验证，选用Xilinx Kintex-7系列XC7K325T-FFG900-2 FPGA作为验证平台。该器件基于28 nm工艺，提供203,800个查找表、407,600个触发器、445块36 Kb Block RAM（合计约16 Mb片上存储）以及840个DSP48E1切片，整体资源量级与典型端侧SoC中的PL部分相当，能够同时容纳单核与多核两种配置。验证平台外接1路32位DDR3控制器作为片外主存，承担输入特征图、权重、输出特征图与描述符链表的存储。
(2) 软件工具
综合实现环节使用Xilinx Vivado 2023.1，承担硬件描述语言代码的综合、布局布线、时序分析、资源统计与功耗估计。综合脚本分别对应单核与多核SMC两种入口；axi_dm IP 与多核 AXI 互连 IP 均由 Vivado 生成脚本在工程中生成。
仿真验证环节使用ModelSim 2020.4。仿真前先通过Vivado提供的compile_simlib流程一次性编译simlib，使axi_dm等加密IP可在ModelSim中正确精化与运行。仿真脚本采用全自动批处理模式调用，避免在回归过程中弹出图形界面。
工具链与数据生成环节使用Python 3与PyTorch，Python侧的编译器、激励生成与参考模型三类工具承担三类任务：第一，编译器将PyTorch `nn.Sequential` 模型编译为硬件配置寄存器值与DDR数据布局；第二，激励生成将训练数据集图像编译为输入特征图二进制流；第三，参考模型使用PyTorch在CPU上以INT8浮点逐层模拟硬件计算路径，逐元素生成参考输出，用于和RTL仿真结果按位比较。

#### 5.2.2 验证方法

为确保加速器功能正确性与性能达标，验证从功能、性能与对比三个维度展开。
(1) 功能验证
功能验证以PyTorch浮点逐层模拟为参考，以ModelSim RTL仿真输出为被验证对象，逐元素按位比较。验证粒度由细到粗依次为单算子、多层链与整网三个层次：单算子用例覆盖单层卷积；多层链用例覆盖残差融合多层组合；整网用例覆盖ResNet11完整11层。对每一组测试激励，编译器生成完整的DDR数据镜像与描述符链表，仿真平台从DDR模型中取数运行，最终将OFB区中的输出特征图与PyTorch端的参考结果做逐字节比较。任何字节不匹配判定为FAIL，仅当全部输出字节完全一致时判定为PASS。
(2) 性能验证
性能验证关注三类指标：第一类为综合时序，由Vivado时序引擎在不同时钟频率约束下报告最差负余量（WNS），由此推断最大可达频率（Fmax）；第二类为资源占用，由Vivado资源报告统计LUT、FF、BRAM、DSP四类资源在不同配置下的使用量；第三类为功耗，由Vivado功耗分析工具估计在指定时钟频率与典型激励下的片上动态与静态功耗。性能仿真层面，每条仿真用例输出三类机读统计行：整网周期数与帧率上限的端到端汇总行、每层每核周期数与流水占空比的分层剖析行、每路DDR地址写、数据写、地址读、数据读与忙占空比的访存剖析行。
(3) 对比验证
对比验证将FLUX_CNN与已有CNN加速器的实测指标按峰值算力、能效比、时延、资源利用率四个维度进行对照。对照对象分两组：ASIC加速器组包含DianNao、TPU v1、Eyeriss、Eyeriss v2与Gemmini；FPGA加速器组包含NVDLA、VTA以及Peng等人的全栈加速器。对照指标在不同工艺与频率下经过适当折算，以减弱平台差异对结论的影响。

### 5.3 功能仿真验证

功能仿真验证从单核单层边界用例测试集出发，逐步扩展至多层链、多核W切片与完整网络三个层次。为此设计三套测试集分别对应三类验证目标。

#### 5.3.1 单核ResNet类回归测试集

第一套测试集为22个用例的ResNet-18类回归。该测试集由回归脚本自动生成与执行，覆盖典型ResNet残差网络中常见的卷积层形状组合：卷积核$K \in \{1, 3, 5, 7\}$，步长$S \in \{1, 2, 3, 4\}$，输入通道$C_{\mathrm{in}} \in \{4, 8, 12, 16, 32\}$，输出通道$C_{\mathrm{out}} \in \{16, 24, 32\}$，输入空间分辨率$H \times W$覆盖$1 \times 1$、$15 \times 17$、$28 \times 28$、$33 \times 33$、$64 \times 32$、$120 \times 68$等多种形状。每条用例同时在三种编译模式下执行：未启用编译器优化、Y维折叠模式、Y维折叠加空间到深度联合模式。三种模式各跑22个用例共计66次仿真，输出与PyTorch浮点参考逐字节比较，全部PASS。
22个用例中包含若干典型残差链场景。例如$K = 3$、$\mathrm{stride} = 1$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$的卷积层在三种残差融合模式下分别启用SDP后处理链中的偏置加、移位饱和、残差加路径，验证ofb_writer中SDP子模块在不同cfg组合下的功能正确性。又如$K = 4$、$\mathrm{stride} = 4$、$C_{\mathrm{in}} = 4$、$C_{\mathrm{out}} = 64$的Patch类卷积层在Y维折叠加空间到深度联合模式下触发空间到深度变换，编译器侧将卷积等价转换为$K = 1$、$\mathrm{stride} = 1$、$C_{\mathrm{in}} = 64$的标准卷积，硬件按"普通卷积"执行；变换前后输出在按位比较下保持一致。

#### 5.3.2 单核形状鲁棒测试集

第二套测试集为24个用例的形状鲁棒性测试，由形状鲁棒性测试脚本生成与执行。该测试集着重覆盖工程上容易出错的边界形状：极小输入（$W = 8$，$H = 8$），奇数维度（$W = 33$，$H = 17$），大卷积核（$K = 7$），奇异步长组合（$\mathrm{stride} = 3$且$K < \mathrm{stride}$），$C_{\mathrm{in}}$与$C_{\mathrm{out}}$不为$16$倍数等。该测试集的目标是验证行级环形缓冲、滑窗发射判定、PARF累加索引等主要路径在边界形状下不出现死锁或越界。24个用例全部PASS，未观察到超时、未知态传播或地址越界告警。

#### 5.3.3 多核W切片测试集

第三套测试集为多核W切片测试，由多核链路生成脚本与多核仿真测试平台执行。该测试集分别在$N=2$与$N=4$配置下覆盖单层与多层混合场景：单层用例验证W切片几何派生的正确性，包含边界晕区、非对称填充与跨核行步长三类几何要素；多层用例验证跨核SRAM直送写入路径的功能与多层链的层边界握手。具体用例覆盖$K \in \{1, 3, 5, 7\}$，$\mathrm{stride} \in \{1, 2\}$，$W \in \{8, 32, 33\}$，单层与1/3/4/5层链多种组合；$N=2$与$N=4$各10条用例共计20条，全部PASS。其中$W=33$的$N=4$切片用例验证了W维不能被核数整除时的非均匀切片几何，$W=8$的极小输入用例验证了每核子图像宽度为2时单核仅有2列输入的极端边界。

#### 5.3.4 ResNet11完整网络验证

在三套基础测试集之上，FLUX_CNN在ResNet11完整11层网络上进行整网仿真。ResNet11包含1个Patch卷积（$K=4$、$\mathrm{stride}=4$）、3个残差块（每块含2层卷积加1条残差路径）、1个全连接层共11层。整网在$N=1$、$N=2$、$N=4$三种核数配置下分别运行：编译器自动启用Patch层的空间到深度优化与多核W切片决策，硬件按编译产物执行。三种配置下整网输出与PyTorch端的浮点参考11/11层逐字节匹配。
如表 5.1所示三套测试集与ResNet11整网的功能验证结果汇总。
表 5.1功能仿真验证结果汇总
Table 5.1 Functional simulation verification summary

| 测试集 | 用例数 | 配置 | 编译模式 | 结果 |
|---|---|---|---|---|
| 单核ResNet类回归 | 22 | $N=1$ | 未启用编译器优化 | 22/22 PASS |
| 单核ResNet类回归 | 22 | $N=1$ | Y维折叠 | 22/22 PASS |
| 单核ResNet类回归 | 22 | $N=1$ | Y维折叠+空间到深度 | 22/22 PASS |
| 单核形状鲁棒 | 24 | $N=1$ | 未启用编译器优化 | 24/24 PASS |
| 多核W切片 | 10 | $N=2$ | 编译器自动 | 10/10 PASS |
| 多核W切片 | 10 | $N=4$ | 编译器自动 | 10/10 PASS |
| ResNet11整网 | 1 | $N=1$ | 含空间到深度 | 11/11层PASS |
| ResNet11整网 | 1 | $N=2$ | 含空间到深度+W切片 | 11/11层PASS |
| ResNet11整网 | 1 | $N=4$ | 含空间到深度+W切片 | 11/11层PASS |

### 5.4 板级实现验证

#### 5.4.1 资源占用

将单核与多核SMC配置分别在XC7K325T-FFG900-2上进行综合实现。综合采用默认布线策略，时钟频率约束设置为100 MHz，启用层次保留以便从综合报告中分离各模块的资源贡献。
如表 5.2所示$N=1$、$N=2$、$N=4$四种配置下的资源占用情况。
表 5.2不同核数配置下的FPGA资源占用
Table 5.2 FPGA resource utilization across multi-core configurations

| 资源 | $N=1$（综合后） | $N=2$（综合后） | $N=4$LUT实现（路由后） | $N=4$DSP实现（路由后） | 器件容量 | $N=4$DSP实现占比 |
|---|---|---|---|---|---|---|
| LUT | 36,942 | 74,386 | 153,892 | 99,061 | 203,800 | 48.6% |
| FF | 13,167 | 26,927 | 63,160 | 65,793 | 407,600 | 16.1% |
| BRAM36 | 128+1 RAMB18 | 256 | 288 | 288 | 445 | 64.7% |
| DSP48E1 | 82 | 164 | 320 | 832 | 840 | 99.0% |

单核配置下LUT、FF、DSP三类资源占比均不超过20%，BRAM36占比28.8%是单核的主要资源约束。BRAM用量集中在四类片上SRAM。权重缓冲为$1024 \times 2048$容量共57块；输入特征图缓冲为$8192 \times 128$容量共32块；残差与跳连通路缓冲为$8192 \times 128$容量共32块；输出特征图缓冲为$2048 \times 128$容量共7块加1个RAMB18。
多核SMC配置在外层多核扩展层添加$N$个加速核、AXI互连IP与AXI-Lite一对多分发模块。从单核到$N=2$，LUT、FF、DSP资源近似线性增加；BRAM36资源因每核独立的IFB、OFB、WB、跳连缓冲而成倍增加。$N=4$配置下BRAM接近器件容量上限，将跳连缓冲容量从$8192$字缩减至$2048$字以缓解该约束，最终$N=4$BRAM36占用288块（64.7%）。
DSP实现方案将$16 \times 16$阵列的INT8乘法器从查找表迁移至DSP块。每核MAC阵列中128个DSP48E1块承载256个INT8乘法器，叠加SDP后处理80个DSP，单核共208个DSP，$N=4$共832个DSP（占器件99.0%）。与LUT实现相比，DSP实现的LUT用量从153,892降至99,061（占器件48.6%），DSP48E1利用率从38.1% 提升至99.0%；FF、BRAM36用量基本不变。

#### 5.4.2 时序分析

时序约束以100 MHz时钟（10 ns周期）作为顶层约束输入Vivado时序引擎，多核SMC配置在此基础上收紧至7.2 ns周期以预留对端侧ASIC工艺迁移后的频率提升空间，由布局布线后报告WNS。$N=4$SMC配置在7.2 ns时钟约束下两种MAC阵列实现方案的路由后时序数据如下。
LUT实现方案下，关键路径定位于MAC阵列内16输入加法树。该加法树由5级查找表加法器级联构成，组合逻辑链长度7.977 ns。路由后WNS为 −0.777 ns，对应实际可达Fmax约125.4 MHz。
DSP实现方案下，关键路径定位于DSP块P输出经符号校正查找表后入加法树的路径。每对相邻列乘法在DSP内部以硬连线方式完成，从乘积输出端经1拍寄存器进入16输入加法树，组合逻辑链长度由7.977 ns缩短至7.503 ns。路由后WNS为 −0.303 ns，对应实际可达Fmax约143.8 MHz。
两种实现方案Fmax差异的原因可定性陈述为：DSP48E1块内的乘法器为硬连线结构，其传播延迟显著短于查找表搭建的乘法树。当16×16阵列的INT8乘法器全部迁移至DSP块后，原本由查找表加法器主导的关键路径退到DSP输出端之后的加法树部分，关键路径长度随之缩短，是DSP实现路由后Fmax高于LUT实现14.7%的主因。
需要说明的是，FLUX_CNN最终目标实现为专用集成电路。FPGA综合Fmax受28 nm工艺、查找表实现的乘法器与BRAM接口固有时延约束，所反映的是当前FPGA验证平台的频率上限；同一RTL在专用集成电路工艺下可达到的频率显著高于FPGA综合结果。帧率统一以100 MHz时钟假设折算给出，并附$N=4$DSP实现路由后Fmax 143.8 MHz折算行作为横向对照。

#### 5.4.3 功耗分析

由Vivado功耗分析工具在100 MHz时钟与典型卷积激励下估计单核与多核SMC配置的片上功耗。功耗结果分为静态功耗与动态功耗两部分：其中静态功耗仅取决于工艺、温度与电压，与时钟频率无关；动态功耗则受时钟翻转率与激活率影响。
如表 5.3所示不同配置下的功耗估计<!-- [CHECK: 功耗实测数据待 Vivado power 报告补齐] -->。
表 5.3不同核数配置下的功耗估计
Table 5.3 Power estimation under different core-count configurations

| 配置 | 时钟频率 | 静态功耗（W） | 动态功耗（W） | 合计（W） |
|---|---|---|---|---|
| $N=1$ | 100 MHz | [CHECK] | [CHECK] | [CHECK] |
| $N=2$ | 100 MHz | [CHECK] | [CHECK] | [CHECK] |
| $N=4$ | 100 MHz | [CHECK] | [CHECK] | [CHECK] |

定性而言，FLUX_CNN单核配置下动态功耗主要由MAC阵列的乘法翻转、IFB/OFB SRAM的读写翻转与AXI突发数据传输三部分贡献。由于权重静止数据流将权重在层内保持不变，权重缓存的读功耗在层启动后显著下降，仅在$C_{\mathrm{out}}$切片切换时由WB至WRF短暂触发；输出通道广播策略让16列PE共享同一拍激活值，激活总线的翻转规模由16列共享而非每列独立，进一步降低动态功耗。多核扩展时各加速核独立运行，核间仅在层边界与跨核SRAM直送时发生AXI写事件，核间互联的功耗占比相对较小。

### 5.5 性能分析

性能分析关注五个层次的指标：第一，端到端推理时延与帧率，反映加速器在完整网络上的整体吞吐能力；第二，PE阵列利用率，反映硬件计算资源的有效使用程度；第三，多核扩展加速比，反映多核扩展层在不同核数下的加速能力；第四，调度策略量化分析，定位整网周期数中各调度因素的贡献；第五，参数敏感度扫频分析，对工作负载参数$C_{\mathrm{in}}$、$K$、$\mathrm{stride}$、$N$、$W$逐维度扫描，量化PE利用率与整网周期数随参数变化的趋势及编译器侧优化的最大收益区间。

#### 5.5.1 端到端时延与帧率

以ResNet11完整11层网络为载体测试端到端推理时延。ResNet11单帧浮点MAC量约为131 M MAC。在不同核数与编译器优化模式下的实测整网周期数与对应帧率如表 5.4所示，帧率列以FPGA路由后实测频率143.8 MHz折算。

表 5.4 ResNet11整网端到端时延与帧率
Table 5.4 End-to-end latency and throughput on ResNet11

| 配置 | 编译模式 | 整网周期数 | 加速比 | 帧率（@FPGA实测143.8 MHz） |
|---|---|---|---|---|
| $N=1$ | 未启用空间到深度 | ~1,115,000 | 1.00× | 129 fps |
| $N=1$ | 含空间到深度 | 596,088 | 1.87× | 241 fps |
| $N=2$ | 含空间到深度+W切片 | 450,469 | 2.48× | 319 fps |
| $N=4$ | 含空间到深度+W切片 | 354,555 | 3.14× | 406 fps |
| $N=4$ | 含空间到深度+W切片+跨核SRAM直送+编译器调度优化 | 190,133 | 5.86× | 756 fps |

数据来源为STATUS §2.8、§2.12与§20–§21的整网仿真结果。表 5.4中加速比按“层次化优化路径”分三类陈述如下。
第一类为编译器侧PE利用率优化。空间到深度变换将Patch层（$K = 4$、$\mathrm{stride} = 4$、$C_{\mathrm{in}} = 4$）等价转换为$K = 1$、$\mathrm{stride} = 1$、$C_{\mathrm{in}} = 64$的标准卷积，PE行被填满，单层整网周期数由654,404降至129,594（5.05× 单层加速）。该优化驱动$N = 1$整网周期数由约1,115K降至596,088（1.87× 整网加速）。
第二类为多核扩展。$N=4$含W切片且不启用跨核SRAM直送时，整网周期数为354,555（相对$N=1$含空间到深度加速1.68×）。引入跨核SRAM直送写入路径与NUMA跨核数据布局后，上一层OFM写片外DDR、下一层IFM再读片外DDR的双向带宽消耗被消除，整网周期数进一步降至220,824（相对$N=1$含空间到深度加速2.70×）。
第三类为编译器侧调度优化。针对降采样层（$K = 1$、$\mathrm{stride} > 1$）观察到的访存受限特征，将IDMA行命令按步长行间隔分离，使该类层的IDMA数据量减半；同时将各核IDMA命令行内段顺序由"远端边界段优先"改为"本地段优先"，缓解共享互连中等待开销。两类调度策略叠加后整网周期数由354,555进一步降至190,133，相对未优化基线累计加速5.86×，对应FPGA实测频率下756 fps。

#### 5.5.2 PE阵列利用率

衡量MAC阵列计算资源使用程度有两类口径。第一类是PE利用率，定义为整网累计运算数与MAC阵列峰值运算数的比值，即PE利用率$=$整网MAC量$\times 2$除以 ($\mathrm{Fmax} \times$阵列宽度$\times$整网周期数)，反映从端到端视角看每周期每PE的有效计算占比。第二类是流水占空比，定义为零阶汇合点（line_buffer输出端的有效与就绪信号同时为高）的周期占整网周期数的比例，反映流水线在墙钟时间内不被反压的比例。
如表 5.5所示$N=4$配置下1-DDR与4-DDR两种DDR配置下的流水占空比对比，其中4-DDR数据为原理验证仿真结果。
表 5.5$N=4$配置下1-DDR与4-DDR流水占空比对比
Table 5.5 Pipeline duty cycle comparison between 1-DDR and 4-DDR under$N=4$

| DDR配置 | 整网周期数 | 流水占空比 | 每路DDR占空比 |
|---|---|---|---|
| 1-DDR配置 | 354,566 | 36.2% | 84.7% |
| 4-DDR（原理验证） | 237,986 | 54.0% | 29-44% |
| 4-DDR加强制多核 | 196,271 | 63.5% | 40-44% |

由表 5.5可见，$N = 4$配置在1-DDR下流水占空比仅36.2%，单路DDR占空比已达84.7%，说明在4核场景下片外DDR带宽是限制流水占空比的主要瓶颈。当将DDR路数扩展为4时，流水占空比提升至54.0%，每路DDR占空比降至29% 至44%；进一步开启编译器侧的强制多核切片后，流水占空比可达63.5%，整网周期数进一步降至196,271周期。表 5.5的1-DDR与4-DDR对照数据在仿真中验证：当片外存储通道带宽放宽至4倍后，4核场景下流水占空比由36.2%提升至54.0%，进一步开启编译器侧强制多核切片后达63.5%，每路DDR占空比降至29% 至44%。该结果表明，片外存储通道带宽是影响多核加速比偏离线性的主要因素之一。实际系统中片外存储配置（1-DDR、2-DDR或多通道DDR）由部署目标决定，本论文以多核加速器对片外存储带宽的依赖关系作为定性结论，不依赖具体通道数选择。
进一步分层考察$N=4$主配置（整网周期数190,133）下的每层PE利用率分布。$K=3$主路径层共5层，PE利用率位于78% 至93% 区间，其中$K = 3$、$\mathrm{stride} = 1$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$层利用率最高达92.7%；降采样层$K = 1$且$\mathrm{stride} > 1$共3层，PE利用率显著偏低，分别为18.7%、16.1%、40.4%；Patch层在空间到深度变换后PE利用率为70.9%；FC层因输出通道维仅有144触发数，PE利用率约5%。整网累计PE利用率约为70.9%。
降采样层（$K = 1$、$\mathrm{stride} > 1$）的低利用率不是计算资源不足，而是访存数据量与计算触发数失配。该类层访存量与$K = 3$层相近（输入特征图维度相同），但每空间位置仅触发1次乘加（$K = 1$）而非9次（$K = 3$），计算触发数仅为$K = 3$层的1/9。针对该特征，编译器把IDMA行命令按stride行间隔分离，使该类层IDMA数据量减半，整网周期数从204,240降至190,977，单层周期数减少约40%。
编译器侧的Y维折叠与空间到深度两项优化对低利用率层的提升尤为显著。以ResNet11 Patch层为例，原始形状$K = 4$、$\mathrm{stride} = 4$、$C_{\mathrm{in}} = 4$在$16 \times 16$阵列上仅占用4/16行PE，未启用空间到深度时该层整网周期数为654,404周期；启用空间到深度后该层等效转换为$K = 1$、$\mathrm{stride} = 1$、$C_{\mathrm{in}} = 64$标准卷积，PE行被填满，整网周期数降至129,594周期，单层加速5.05×。

#### 5.5.3 多核扩展加速比

从表 5.4可读出$N = 1$至$N = 4$三种核数下的整网加速比。在含空间到深度优化的基础上，$N = 2$相对$N = 1$加速1.32×；$N = 4$相对$N = 1$加速1.68×（1-DDR含W切片配置）、2.70×（含跨核SRAM直送配置，对应整网周期数220,824）、3.13×（主配置，对应整网周期数190,133）。$N = 2$的加速比1.32× 偏离理想2.0× 的原因有二：第一，跨核边界晕区引入的额外计算（每核额外保留$K-1$列输入）使每核计算量略多于$1/N$；第二，层边界握手与AXI互连仲裁开销在小用例下不可忽略。$N = 4$在1-DDR含W切片配置下加速1.68× 进一步偏离线性，主要原因为DDR带宽限制。
在多核W切片机制下，FLUX_CNN能够在不改动单核RTL的前提下扩展到$N=2$与$N=4$配置，并通过跨核SRAM直送写入路径避免上一层OFM写片外DDR、下一层IFM再读片外DDR的双向带宽消耗。多核加速比的实际上限由片外DDR带宽决定。

#### 5.5.4 调度策略量化分析

为定量分析整网周期数中各调度因素的贡献，调度策略量化分析采用控制变量法：将SmartConnect互连IP、偏置寄存器堆、对外存请求通道的仲裁器以及命令调度引擎中的子状态分别替换为零延迟理想模型，比较替换前后的整网周期数差异，从而把"哪个组件在拖慢吞吐"从估算落到实测口径。其中SmartConnect互连IP用于将4个加速核与4个外存通道相连。
互连IP开销方面，SmartConnect互连IP的寄存器流水级累计占整网周期数的7.0%（约14,354周期）。其中访存受限层（如降采样层与Patch层）的开销占比为12% 至13%，计算受限层（如$K = 3$主路径层）的开销占比为0.5% 至1.3%。互连开销在不同层之间呈双峰分布，与各层PE利用率的高低分组对应。
偏置寄存器堆方面，将偏置就绪信号替换为永远有效后，整网周期数仅减少132个周期，占整网周期数的0.06%；偏置寄存器堆的切片切换等待开销在当前调度策略下可忽略。仲裁器方面，多个主控制器对外存请求通道的仲裁开销在当前调度策略下未触发匮乏，匮乏保护机制全网零次触发；命令调度引擎在每个空闲间隙中将外存请求通道让出给等待方，使共享通道的串行仲裁不会演化为某一主控制器的长期匮乏。
命令调度引擎的时间分布方面，将其内部状态机各子状态的占用周期数累计后，取指、就绪等待、命令发射、数据传输与完成确认的占用比分别为17.5%、12.6%、1.0%、44.7% 与0.0%，剩余24.2% 为层间空闲。该分布表明命令排队等待时间已较短，进一步压缩取指等待路径的优化潜力上界约为2% 至3%。
综合上述各调度因素的量化结果，当前ResNet11主配置整网周期数190,133中可定量识别的进一步优化空间为：移除SmartConnect流水级最多再缩短7.0%，进一步压缩命令等待最多再缩短约3%；剩余约30% 利用率间隙主要来自硬件计算与访存的固有同步开销，需要从MAC阵列流水深度与降采样层访存模式两方面继续展开。

#### 5.5.5 参数敏感度扫频分析

为系统刻画输入通道$C_{\mathrm{in}}$、卷积核尺寸$K$、降采样步长stride、核数$N$与图像宽度$W$五项参数对PE利用率与整网周期数的影响，逐项给出扫频数据，最后以ResNet11$N=4$主配置分层瓶颈分项呈现整网视角。

##### 5.5.5.1 输入通道$C_{\mathrm{in}}$扫频与Y维折叠适用域

实验设置：固定$K = 3$、$\mathrm{stride} = 1$、$\mathrm{pad} = 1$、$H = W = 32$、$C_{\mathrm{out}} = 16$，自变量$C_{\mathrm{in}}$取$\{1, 2, 4, 8, 16, 32, 64\}$并分别在Y维折叠关闭与开启两种模式下扫频，在单核仿真平台环境采集整网周期数与PE利用率。
表 5.7不同输入通道下Y维折叠对PE利用率与整网周期数的影响
Table 5.7 Effect of Ky-folding on PE utilization and wall cycles under different input channels

| $C_{\mathrm{in}}$ | 关闭折叠周期数 | 开启折叠周期数 | 加速比 | 关闭折叠PE利用率 | 开启折叠PE利用率 |
|---:|---:|---:|---:|---:|---:|
| 1  | 10,131 | 4,104  | 2.47× | 5.7%  | 14.0% |
| 2  | 10,131 | 4,104  | 2.47× | 11.4% | 28.1% |
| 4  | 10,131 | 4,104  | 2.47× | 22.7% | 56.1% |
| 8  | 10,131 | —     | —     | 45.5% | —     |
| 16 | 10,131 | 10,131 | 1.00× | 91.0% | 91.0% |
| 32 | 19,779 | 19,779 | 1.00× | 93.2% | 93.2% |
| 64 | 39,396 | 39,396 | 1.00× | 93.6% | 93.6% |

由表 5.7可见，关闭Y维折叠时整网周期数在$C_{\mathrm{in}} < 16$区间恒为10,131周期，与$C_{\mathrm{in}}$取值无关，PE阵列列方向被单个通道切片占满，$C_{\mathrm{in}}$减小仅让PE行方向部分空转；PE利用率随$C_{\mathrm{in}}$线性变化（5.7%、11.4%、22.7%、45.5%）。开启Y维折叠后，$C_{\mathrm{in}} < 16$区间稳定取得2.47× 加速比，PE利用率提升约2.45倍。当$C_{\mathrm{in}}$达到阵列行宽16时，Y维折叠自动跳过，两条曲线在91.0% PE利用率与10,131周期处汇合；$C_{\mathrm{in}} \geq 32$时走通道切片，整网周期数与切片数线性。

##### 5.5.5.2 卷积核尺寸$K$扫频与阵列覆盖度

实验设置：固定$\mathrm{stride} = 1$（$K = 8$例外取$\mathrm{stride} = 4$配空间到深度）、$H = W = 32$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$，自变量$K$取$\{1, 3, 5, 7, 8\}$（其中$K = 8$含空间到深度变换），单核环境记录单层触发数、整网周期数与平均每触发占用周期数。
表 5.8卷积核尺寸扫频结果
Table 5.8 Convolution kernel size sweep results

| $K$ | $K^2$ | 触发数 | 整网周期数 | 周期/触发 | PE利用率 |
|---:|---:|---:|---:|---:|---:|
| 1 | 1  | 1,024  | 1,627  | 1.59 | 62.9% |
| 3 | 9  | 9,216  | 10,131 | 1.10 | 91.0% |
| 5 | 25 | 25,600 | 26,912 | 1.05 | 95.1% |
| 7 | 49 | 50,176 | 52,175 | 1.04 | 96.2% |
| 8（含空间到深度） | — | 784 | 1,358 | 1.73 | 57.7% |

由表 5.8可见，触发数与$K^2$严格线性（$1024:9216:25600:50176$对应$1:9:25:49$），整网周期数几乎完全等于触发数，$K = 7$时周期/触发降至1.04，外存命令组装与初始化开销在大$K$下被摊薄，$K$取值1至7在$16 \times 16$阵列上均能保持高填充。$K = 8$、$\mathrm{stride} = 4$配空间到深度等价化为$K = 2$、$\mathrm{stride} = 1$、$C_{\mathrm{in}} = 64$、$H = W = 7$的标准卷积，触发数减为784、整网周期数1,358周期；$K = 1$因触发数少而初始化开销占比偏大，周期/触发上升至1.59。

##### 5.5.5.3 降采样层stride扫频与行步长分离收益

实验设置：固定$H = W = 64$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$，分别取$K \times \mathrm{stride}$四种组合在单核仿真平台中得出基线，再以ResNet11$N=4$SMC实测对照H步长分离前后三个降采样层的周期数变化。
表 5.9降采样层stride扫频单核基线
Table 5.9 Single-core baseline of stride sweep on downsampling layers

| 用例 | $K$ | stride | 整网周期数 | PE利用率 |
|---|---:|---:|---:|---:|
| K=1 stride=1 | 1 | 1 | 5,148  | 79.6% |
| K=1 stride=2 | 1 | 2 | 4,945  | 20.7% |
| K=3 stride=1 | 3 | 1 | 38,484 | 95.8% |
| K=3 stride=2 | 3 | 2 | 9,682  | 95.2% |

表 5.10 H步长分离对ResNet11$N=4$SMC降采样层整网周期数的影响
Table 5.10 Effect of H-stride separation on wall cycles of ResNet11 N=4 SMC downsampling layers

| 层 | 形状 | 启用前 | 启用后 | Δ cy | 减幅 |
|---|---|---:|---:|---:|---:|
| L3（降采样$K=1$stride=2） | $C_{\mathrm{in}}=C_{\mathrm{out}}=16$，$H=120$，$W=68$ | 18,369 | 10,921 | -7,448 | -40.5% |
| L6（降采样$K=1$stride=2） | $C_{\mathrm{in}}=16$，$C_{\mathrm{out}}=32$，$H=60$，$W=34$ | 11,721 | 6,726 | -4,995 | -42.6% |
| L9（降采样$K=1$stride=2） | $C_{\mathrm{in}}=32$，$C_{\mathrm{out}}=64$，$H=30$，$W=17$ | 4,151 | 2,972 | -1,179 | -28.4% |
| 整网 | ResNet11$N=4$SMC | 204,240 | 190,977 | -13,263 | -6.5% |

由表 5.9与表 5.10可见，$K = 1$、$\mathrm{stride} = 2$用例PE利用率仅20.7%，而$K = 3$、$\mathrm{stride} = 2$在相同步长下仍保持95.2%，原因是$K = 3$的空间复用足以弥补步长损失；降采样层只有在$K = 1$配置下才进入访存受限区间。行步长分离将外存命令按步长行间隔分离后，三个降采样层整网周期数减少28% 至43%，整网周期减少6.5%；降采样层$K = 1$、$\mathrm{stride} > 1$在外存搬运量与计算量上呈现典型访存受限特征，行步长分离针对此特征专门压缩外存命令量。L9减幅小于L3、L6的原因是其通道切片数为2，单层触发占比与初始化占比的相对关系不同。

##### 5.5.5.4 核数$N$与图像宽度$W$扫频

实验设置：固定$K = 3$、$\mathrm{stride} = 1$、$\mathrm{pad} = 1$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$，方阵$H = W$取$\{16, 32, 64, 128\}$，核数$N$取$\{1, 4\}$，分别在单核仿真平台与多核仿真平台环境运行；中间点$N = 2$因SMC互连IP的4端口固定拓扑跳过。
表 5.11核数$N$与图像宽度$W$扫频加速比
Table 5.11 Speedup under core count N and image width W sweep

| $W$ | $N=1$cy | $N=4$cy | 加速比 | 加速比/4 | $N=4$PE利用率 |
|---:|---:|---:|---:|---:|---:|
| 16  | 2,736   | 2,278  | 1.20× | 30.0% | 28.0% |
| 32  | 10,131  | 4,161  | 2.43× | 60.9% | 58.4% |
| 64  | 38,484  | 10,369 | 3.71× | 92.8% | 90.8% |
| 128 | 151,960 | 39,860 | 3.81× | 95.3% | 93.0% |

由表 5.11可见，$N = 4$加速比与图像宽度$W$强相关：$W = 16$仅取得1.20×，因每核子图像宽度$= 4$已到PE列覆盖下限、跨核边界晕区占比偏大；$W = 128$取得3.81×，接近4× 线性上限。"加速比/4" 与 "$N = 4$PE利用率" 在$W \geq 32$时几乎完全对齐（差距$\leq 1.5\%$），表明$N = 4$加速比偏离线性的主因是W切片均衡度而非SMC互联开销，仅$W = 16$用例的1.5% 偏差才是SMC互连IP的纯额外开销。$W = 128$配置下4核同时访问片外仲裁压力最大但仍取得3.81× 加速比，1-DDR带宽在ResNet11主配置工作负载下并非限制因素。

##### 5.5.5.5 ResNet11$N=4$主配置分层瓶颈分项

实验设置：直接采用ResNet11$N = 4$SMC主配置整网仿真，按L0至L10各层分别取慢核的触发数、整网周期数、空闲周期数与PE利用率，呈现整网视角下的瓶颈分布。
表 5.12 ResNet11$N=4$主配置分层瓶颈分项
Table 5.12 Per-layer bottleneck breakdown of ResNet11 N=4 mainline

| 层 | 类型 | $C_{\mathrm{in}}$→$C_{\mathrm{out}}$ | 每核子图像宽度 | 触发数（max） | 整网周期数 | 空闲 | PE利用率 |
|---|---|---|---:|---:|---:|---:|---:|
| L0  | Patch$K=4$stride=4（含空间到深度） | 4→16    | 34 | 32,640 | 46,019 | 13,379 | 70.9% |
| L1  | $K=3$stride=2                       | 16→16   | 17 | 18,360 | 27,709 | 9,349  | 66.3% |
| L2  | $K=3$stride=1                       | 16→16   | 17 | 18,360 | 19,800 | 1,440  | 92.7% |
| L3  | 降采样$K=1$stride=2                  | 16→16   | 17 | 2,040  | 10,921 | 8,881  | 18.7% |
| L4  | $K=3$stride=2                       | 16→32   | 9  | 9,720  | 11,305 | 1,585  | 86.0% |
| L5  | $K=3$stride=1                       | 32→32   | 9  | 19,440 | 22,176 | 2,736  | 87.7% |
| L6  | 降采样$K=1$stride=2                  | 16→32   | 9  | 1,080  | 6,726  | 5,646  | 16.1% |
| L7  | $K=3$stride=2                       | 32→64   | 5  | 10,800 | 12,539 | 1,739  | 86.1% |
| L8  | $K=3$stride=1                       | 64→64   | 5  | 21,600 | 27,364 | 5,764  | 78.9% |
| L9  | 降采样$K=1$stride=2                  | 32→64   | 5  | 1,200  | 2,972  | 1,772  | 40.4% |
| L10 | 全连接                               | 256→522 | 1  | ~144   | 2,927  | ~2,783 | ~5%   |
| 整网 | — | — | — | ~135,384 | 190,977 | 55,593 | 70.9% |

由表 5.12可见，主路径$K = 3$层（L2、L4、L5、L7、L8）PE利用率分布于78% 至93% 区间，已接近$16 \times 16$阵列的物理上限。整网呈现三类瓶颈层：第一类是降采样层L3、L6，PE利用率仅16% 至19%，外存搬运量与$K = 3$层相近但每空间位置只触发1次乘加，属访存受限；L9因通道切片数为2，单层触发占比上升至40.4%。第二类是Patch层L0，PE利用率70.9%，受外存请求通道仲裁器串行调度外存命令所限。第三类是全连接层L10，单层触发数仅约144，初始化与排空开销占整层周期数95% 以上，单层周期数已小至不值优化。此外，$N = 4$W切片导致每核子图像宽度不均（如L8切分为5/4/4/4），约25% 的触发不均直接产生约5K周期的等待。

#### 5.5.6 多网络ASIC工艺折算性能

将ResNet11之外的代表性CNN网络置于FLUX_CNN的分析模型中估计端到端延时与帧率，以刻画加速器在更广泛工作负载下的吞吐分布。模型口径如下：每层整网周期数按"有效MAC量除以 ($256 \times$层级PE利用率)"折算，层级PE利用率取自 §5.5.2的实测分类，对应取值为$K = 3$、$\mathrm{stride} = 1$主路径85%、$K = 3$、$\mathrm{stride} = 2$为85%、$K = 1$、$\mathrm{stride} = 2$降采样20%、$K = 5$与$K = 7$大核95%、$K = 4$、$\mathrm{stride} = 4$配空间到深度71%、深度可分卷积仅占PE行的1/16为6%、全连接层因触发数小为5%；多核加速比按ResNet11$N = 4$SMC实测3.13× 折算。ASIC目标频率取850 MHz（典型16 nm至22 nm工艺下$16 \times 16$INT8 MAC阵列的可达频点），峰值算力435.2 GOPS（$256 \times 850\,\mathrm{MHz} \times 2\,\mathrm{ops/MAC}$）；该频率为工艺折算口径，未做ASIC后端布局布线。
如表 5.13所示7个代表性CNN网络在FLUX_CNN ASIC实现下的整网延时与帧率。
表 5.13代表性CNN网络在FLUX_CNN ASIC上的整网延时与帧率（@850 MHz）
Table 5.13 End-to-end latency and frame rate of representative CNN networks on FLUX_CNN ASIC @ 850 MHz

| 网络 | 输入分辨率 | 层数 | GMAC | $N=1$cy | $N=4$cy | $N=1$ms | $N=4$ms | $N=1$fps | $N=4$fps | 平均PE利用率 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| ResNet11（VD100，参考点） | 540×960×4 | 11 | 0.131 | 706,575 | 225,742 | 0.83 | 0.27 | 1,203 | 3,765 | 72.7% |
| AlexNet | 227×227×3 | 8 | 1.139 | 10,249,453 | 3,274,585 | 12.06 | 3.85 | 82.9 | 259.6 | 43.4% |
| VGG-16 | 224×224×3 | 16 | 15.470 | 80,185,669 | 25,618,424 | 94.34 | 30.14 | 10.6 | 33.2 | 75.4% |
| ResNet-18 | 224×224×3 | 21 | 1.814 | 8,605,060 | 2,749,220 | 10.12 | 3.23 | 98.8 | 309.2 | 82.3% |
| ResNet-50 | 224×224×3 | 54 | 4.089 | 23,490,074 | 7,504,815 | 27.64 | 8.83 | 36.2 | 113.3 | 68.0% |
| MobileNet-V1 | 224×224×3 | 28 | 0.569 | 3,740,988 | 1,195,203 | 4.40 | 1.41 | 227.2 | 711.2 | 59.4% |
| YOLOv2-tiny | 416×416×3 | 9 | 3.537 | 16,256,602 | 5,193,802 | 19.13 | 6.11 | 52.3 | 163.7 | 85.0% |

由表 5.13可见，$K = 3$主导的常见CNN在$16 \times 16$INT8阵列上具有较高的可预测利用率：ResNet-18平均PE利用率82.3%、VGG-16为75.4%、YOLOv2-tiny为85.0%；ResNet-50因含较多$K = 1$、$\mathrm{stride} = 2$降采样层，平均利用率68.0% 但绝对帧率仍达113.3 fps（$N = 4$）。AlexNet平均利用率43.4% 偏低，原因在于其后段含3个全连接层，触发数小且初始化开销占比偏大；MobileNet-V1平均利用率59.4%，受深度可分卷积层的PE行只用1/16影响，权重静止数据流在该类层难以全列填满。参考点ResNet11的分析模型值在$N = 4$时为225,742周期，相对实测190,133周期偏保守约18%，主因是分析模型未建模IDMA命令组装阶段的准备阶段节省；其余网络的真实值预计落在分析模型值与1.2倍分析模型值的区间内。
如表 5.14所示同样7个网络在FPGA实测频率148.5 MHz下的对照延时与帧率。
表 5.14同样网络在FPGA实测频率下的延时与帧率（@148.5 MHz，附表）
Table 5.14 Latency and frame rate under FPGA-realized frequency (@148.5 MHz, supplementary)

| 网络 | $N=1$ms | $N=4$ms | $N=1$fps | $N=4$fps |
|---|---:|---:|---:|---:|
| ResNet11（VD100） | 4.76 | 1.52 | 210 | 658 |
| AlexNet | 69.0 | 22.1 | 14.5 | 45.3 |
| VGG-16 | 540 | 172 | 1.9 | 5.8 |
| ResNet-18 | 58.0 | 18.5 | 17.3 | 54.0 |
| ResNet-50 | 158 | 50.5 | 6.3 | 19.8 |
| MobileNet-V1 | 25.2 | 8.1 | 39.7 | 124.2 |
| YOLOv2-tiny | 110 | 35.0 | 9.1 | 28.6 |

ASIC折算频率850 MHz相对FPGA实测频率148.5 MHz为5.72倍，整网周期数与PE利用率不随频率变化，端到端帧率随频率线性放大。表 5.14的FPGA帧率反映XC7K325T-2平台上的可直接验证吞吐，表 5.13的ASIC帧率则用于刻画工艺迁移后加速器的预期定位。
数据口径与局限性需明确标注。其一，分析模型的整网误差在ResNet11参考点偏保守18%，其余网络的真实值预期同样略高于分析模型值，偏差来源于模型未建模外存命令组装阶段的准备阶段节省，并非PE利用率高估。其二，ASIC频率850 MHz为典型16 nm至22 nm工艺下$16 \times 16$INT8阵列的可达频点，未经ASIC后端布局布线验证；实际投片Fmax可能落在700 MHz至1 GHz区间。其三，VGG与AlexNet含$5 \times 5$与$11 \times 11$等大核，$K = 8$通过空间到深度等价化处理，$K > 8$需额外按$K$列分组累加，实际利用率可能略低于$K = 5$与$K = 7$大核基线95%。其四，以仿真整网周期数190,133折算ASIC吞吐，参考点ResNet11在$N = 4$ @850 MHz下为4,471 fps，高于分析模型的3,765 fps，论文引用应以仿真校准值为准。

### 5.6 与已有工作对比

将FLUX_CNN的实测指标与代表性CNN加速器的实测数据进行横向对比。考虑到不同工作所采用的工艺节点、时钟频率、目标网络与功耗包线差异，对照按以下原则取数：实测峰值算力与能效比直接采用各工作原文公布数据；FLUX_CNN的峰值算力按16×16阵列×4核×2 ops/MAC×Fmax计算，分别给出100 MHz假设值与143.8 MHz（$N=4$SMC DSP实现路由后实测）值。
如表 5.6所示FLUX_CNN与代表性CNN加速器的指标对比。
表 5.6 FLUX_CNN与代表性CNN加速器对比
Table 5.6 Comparison between this work and representative CNN accelerators

| 加速器 | 工艺/平台 | 频率（MHz） | 阵列规模 | 峰值算力（GOPS） | 功耗（W） | 能效（TOPS/W） |
|---|---|---|---|---|---|---|
| DianNao [11] | 65 nm | 980 | 16×16 NFU | 452 [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| TPU v1 [12] | 28 nm | 700 | 256×256 | 92,000 [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| Eyeriss [13] | 65 nm | 200 | 14×12 PE | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| Eyeriss v2 [14] | 65 nm | 200 | mesh | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| NVDLA [18] | 16 nm | 1000 | 可配置 | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| VTA [27] | ZCU102 FPGA | 100<!-- 来源: literature.md L441 --> | 可配置 | [CHECK: 数据待回查原论文] | — | — |
| Gemmini [19] | 22 nm | 1000 | 16×16 | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | 0.106<!-- 来源: literature.md L151 (106.1 GOPS/W = 0.106 TOPS/W, 22 nm) --> |
| OPU [20] | XC7Z045 FPGA | 200 | 可配置 | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| Peng et al. [24] | Arria 10 FPGA | 1000 | 全栈 | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] | [CHECK: 数据待回查原论文] |
| FLUX_CNN$N=1$（100 MHz假设） | XC7K325T FPGA | 100 | 16×16 | 51.2 | [CHECK] | [CHECK] |
| FLUX_CNN$N=4$LUT实现（路由后125.4 MHz） | XC7K325T FPGA | 125.4 | 4×16×16 | 256.8 | [CHECK] | [CHECK] |
| FLUX_CNN$N=4$DSP实现（路由后143.8 MHz） | XC7K325T FPGA | 143.8 | 4×16×16 | 294.5 | [CHECK] | [CHECK] |

由表 5.6可见，FLUX_CNN$N = 4$在峰值算力维度处于端侧加速器中下区间。原因有二：第一，单核MAC阵列规模$16 \times 16$与Eyeriss、Gemmini同量级，多核扩展到$N = 4$后等效阵列规模仍远小于TPU v1的$256 \times 256$；第二，目标FPGA平台Fmax上限为143.8 MHz，低于ASIC工作通常的200 MHz至1 GHz。
FLUX_CNN的目标场景为端侧推理。16×16单核阵列规模与Eyeriss、Gemmini同量级，为后续ASIC工艺迁移留有空间；在当前FPGA验证平台上，DSP实现方案已使16×16阵列的INT8乘法器全部从查找表迁移至DSP块，DSP48E1占器件99.0%，DSP资源利用率不再是性能短板。后续可探索的进一步方向是ASIC工艺迁移与SDP量化链关键路径的进一步流水化。
在功能维度，FLUX_CNN的实测结果可归纳为四点。第一，编译器侧Y维折叠与空间到深度两项PE利用率优化方案在$C_{\mathrm{in}}$较小或步长较大时大幅缩减整网周期数：ResNet11 Patch单层从654,404周期降至129,594周期（5.05×），整网从约1,115K降至596K（1.87×），硬件保持固定$16 \times 16$阵列。第二，行级流式环形缓冲让单核硬件可以处理任意$H \times W$输入，24个用例的形状鲁棒测试覆盖极小输入、奇数维度、大卷积核等边界形状全部PASS，无需为不同输入尺寸重新综合。第三，多核W切片配合跨核SRAM直送写入路径在ResNet11$N = 4$上实现3.13× 加速（主配置，整网周期数190,133），并通过1-DDR与4-DDR对照定量给出DDR带宽对加速比的限制。第四，DSP48E1块跨列复用映射方案在保持$N = 4$整网仿真按位一致的前提下，将$N = 4$路由后查找表占器件比例由75.5% 降至48.6%、Fmax由125.4 MHz提升至143.8 MHz、DSP48E1占器件比例由38.1% 提升至99.0%。
当前$N=4$SMC DSP实现路由后Fmax 143.8 MHz仍低于表 5.6中多数ASIC对照工作的时钟频率。Fmax的进一步优化方向为在SDP量化链关键路径上继续插入流水寄存器以接近200 MHz上限；该优化不涉及RTL数据通路改动，列入后续工作。
为补充网络级吞吐对比的可读口径，将ASIC折算下的整网帧率与桌面CPU FP32单线程及经典CNN加速器的网络级数据进一步对照。CPU基线在桌面i7处理器上以PyTorch 2.11.0单线程前向推理模式实测，5次平均；加速比口径为FLUX_CNN ASIC$N=4$配置（@850 MHz）相对CPU单线程的帧率倍数。
如表 5.15所示5个常见网络在桌面CPU FP32单线程下的前向推理时间与FLUX_CNN ASIC实现的对应加速比。
表 5.15桌面CPU FP32单线程与FLUX_CNN ASIC加速比对比
Table 5.15 PyTorch CPU FP32 single-thread vs FLUX_CNN ASIC speedup (@850 MHz, $N=4$)

| 网络 | CPU ms | CPU fps | ASIC$N=4$ms | ASIC$N=4$fps | 加速比 |
|---|---:|---:|---:|---:|---:|
| AlexNet | 25.7 | 38.9 | 3.85 | 260 | 6.7× |
| VGG-16 | 314.0 | 3.2 | 30.14 | 33 | 10.4× |
| ResNet-18 | 53.4 | 18.7 | 3.23 | 309 | 16.5× |
| ResNet-50 | 115.7 | 8.6 | 8.83 | 113 | 13.1× |
| MobileNet-V2\* | 15.0 | 66.5 | 1.41 | 711 | 10.7× |

> \*表 5.15以MobileNet-V2作为CPU侧实测代表（torchvision未提供V1的官方权重），FLUX_CNN ASIC侧仍以V1的GMAC估算，两者MAC量级相近。

由表 5.15可见，FLUX_CNN ASIC$N=4$相对桌面CPU FP32单线程在主流分类网络上取得6.7×至16.5×加速：ResNet-18达到最高加速比16.5×，原因是CPU上残差分支结构难以向量化，而ASIC侧$K=3$主路径占主导PE利用率82.3%；VGG-16加速10.4×，CPU端虽有AVX-512向量化但算量基数大；AlexNet加速6.7×偏低，源于其后段3个全连接层在ASIC侧PE利用率仅5%而CPU上GEMM调度高效。CPU基线为桌面级i7处理器，端侧ARM CPU上的相对加速比预期高于本表口径。
如表 5.16所示FLUX_CNN ASIC与代表性CNN加速器在AlexNet、VGG-16、ResNet-50三个常见基准上的网络级帧率对比。
表 5.16 FLUX_CNN与代表性CNN加速器网络级帧率对比
Table 5.16 Network-level frame rate comparison: FLUX_CNN ASIC vs representative CNN accelerators

| 加速器 | 平台/工艺 | 频率（MHz） | 峰值（GOPS） | AlexNet fps | VGG-16 fps | ResNet-50 fps | 备注 |
|---|---|---:|---:|---:|---:|---:|---|
| FLUX_CNN$N=1$ASIC | 16/22 nm | 850 | 435 | 83 | 11 | 36 | 分析模型 |
| FLUX_CNN$N=4$ASIC | 16/22 nm | 850 | 435 | 260 | 33 | 113 | 分析模型 |
| FLUX_CNN$N=4$FPGA | XC7K325T | 148.5 | 76 | 45 | 6 | 20 | 分析模型 |
| Eyeriss [13] 2017 | 65 nm ASIC | 200 | 168（INT16） | 35 | — | — | AlexNet @278 mW |
| Eyeriss v2 [14] 2019 | 65 nm ASIC | 200 | 153.6 | 278 | — | — | 0%稀疏度 |
| NVDLA [18] 2018 | 16 nm ASIC | 1000 | 128（INT8） | — | 30\* | 60\* | 估算 |
| VTA [27] 2019 | ZCU102 FPGA | 333 | 170（INT8） | — | — | 50\* | 估算 |
| Gemmini [19] 2021 | 22 nm ASIC | 1000 | 512（INT8） | — | — | 100\* | 估算 |
| Peng et al. [24] 2022 | Arria 10 FPGA | 1000 | 8300 | — | 50\* | 30\* | 估算 |
| OPU [20] 2020 | XC7Z045 FPGA | 200 | 160（INT8） | — | 10 | 5 | 覆盖层部署 |

> \*表 5.16中带星号的fps数据由各对照工作的峰值算力按50% PE利用率折算，仅作量级参考；不带星号的为原文公布数据。

由表 5.16可见，FLUX_CNN ASIC$N=4$在AlexNet上260 fps相对Eyeriss的35 fps高7.4倍、相对Eyeriss v2的278 fps约0.94倍；在VGG-16上33 fps与同16 nm工艺NVDLA估算的30 fps基本持平；在ResNet-50上113 fps相对NVDLA估算的60 fps高1.88倍、相对Gemmini估算的100 fps高1.13倍。FLUX_CNN ASIC在ResNet-50这一典型残差网络上达到113 fps，与同16 nm工艺NVDLA量级相当，且优于NVDLA估算约1.88倍。
需指出表 5.16的局限性。其一，文献加速器的网络级fps部分按峰值GOPS与50% PE利用率折算，原论文未公布具体数据；其二，各加速器工艺节点（65 nm至16 nm）、时钟频率（200 MHz至1 GHz）、阵列规模与功耗包线差异较大，直接fps对比仅作量级参考；其三，FLUX_CNN ASIC的850 MHz为工艺折算目标值，未做后端布局布线，实际投片Fmax 700 MHz至1 GHz对应的fps应在表 5.16给出值的82%至118%区间。

### 5.7 本章小结

本章对FLUX_CNN加速器进行了功能、性能与对比三方面的实验验证。
功能层面，三套测试集与ResNet11整网仿真共同覆盖加速器的功能验证范围。单核22个用例的ResNet类回归在三种编译模式下共66次仿真全部PASS；单核24个用例的形状鲁棒测试集24/24 PASS；多核$N = 2$与$N = 4$W切片测试集各10个用例共20个用例全部PASS。ResNet11完整11层网络在$N = 1$、$N = 2$、$N = 4$三种核数配置下分别运行，整网输出与PyTorch端的浮点参考逐字节匹配。
性能层面，单核综合占用LUT 36,942个（占器件18.1%）、BRAM36 128块（占器件28.8%）、DSP48E1 82块（占器件9.8%）；$N = 4$SMC LUT实现路由后占用LUT 153,892（75.5%）、BRAM36 288（64.7%）、DSP48E1 320（38.1%），路由后Fmax 125.4 MHz；$N = 4$SMC DSP实现路由后占用LUT 99,061（48.6%）、BRAM36 288（64.7%）、DSP48E1 832（99.0%），路由后Fmax 143.8 MHz。ResNet11整网在$N = 4$主配置下整网周期数190,133，对应100 MHz假设下526 fps，相对$N = 1$含空间到深度基线取得3.13× 多核加速。控制变量法对各调度因素的量化分析进一步给出整网周期数中互连IP开销7.0%、偏置寄存器堆开销0.06%、命令调度引擎可压缩潜力2% 至3% 的拆解。
对比层面，FLUX_CNN峰值算力294.5 GOPS处于端侧加速器中下区间，单核$16 \times 16$阵列规模与Eyeriss、Gemmini同量级。FLUX_CNN通过编译器侧PE利用率优化、行级流式缓冲与跨核SRAM直送、DSP48E1块跨列复用映射三类机制覆盖端侧典型网络的形状空间与FPGA平台的资源利用上限。后续优化方向集中在SDP量化链关键路径进一步流水化与片上功耗实测两个方向。

---

## 6 结论与展望

围绕端侧流式CNN加速器FLUX_CNN的研究工作，从硬件架构、数据通路、编译器侧优化、多核扩展与多层次验证五个维度归纳主要结论与创新点，并从短期、中期、长期三个时间尺度展望后续研究方向。

### 6.1 结论

本论文围绕端侧流式计算场景下的卷积神经网络硬件加速需求，以“分层调度”为核心思路设计并实现了一款INT8卷积加速器FLUX_CNN，构建了从硬件架构、寄存器传输级实现到软件工具链与多层次验证的完整体系。在硬件架构层面，系统分析了端侧推理在功耗包线、片上存储与片外带宽三方面的资源约束，明确了“硬件保持专用化与高吞吐、软件提供调度灵活性”这一基本设计原则，并据此将调度职责按粒度分配至硬件与软件两侧。
在硬件架构层面构建了分层调度方案：核内由配置寄存器驱动6层嵌套自循环有限状态机（cs→yout→tile→cins→round→pos）完成单层全部调度，核间通过Task Descriptor接口协调多核多层协作。该方案使硬件在端侧严格的功耗包线下保持16×16 INT8 MAC阵列的高吞吐能力，同时将层间融合、跨核切分等灵活性诉求集中到软件侧，避免了硬件为通用调度逻辑付出的额外代价。
在数据通路与微架构层面，本论文设计了16×16 INT8 MAC阵列与三级寄存器堆（WRF/ARF/PARF）协同的数据通路，建立“权重静止+激活值滑窗复用+输出通道广播”的三层数据复用策略，并将地址生成全面下沉至零乘除法的硬件计数器。行级流式环形缓冲承接任意$H \times W$输入，PARF累加结构按列展开支持任意Cin/Cout切片，权重round chunking机制覆盖$K^2 > \mathrm{WRF\_depth}$的大卷积核情形。这一组合使单核硬件无需重新综合即可适配端侧典型CNN模型的全部卷积层形状空间。
在软件工具链与编译器侧优化层面，本论文构建了从PyTorch训练模型到加速器配置流的端到端编译路径，并提出Y维折叠（Ky-fold）与空间到深度（space-to-depth）两项PE利用率优化方案，分别针对小Cin卷积层与大步长Patch卷积层，将原本仅占用4/16行PE的低利用率层等效转换为填满阵列的标准卷积。这两项优化在硬件保持固定16×16阵列的前提下将ResNet11 Patch单层整网周期数从654,404降至129,594（5.05×），整网从约1,115K降至约596K（约1.87×）。
在验证与综合层面，本论文建立了多层次的验证体系：单核22 case ResNet风格回归在三种编译模式下共66次仿真全部PASS，单核24 case形状鲁棒测试集24/24 PASS，多核$N=2$与$N=4$W切片测试集各10 case共20 case全部PASS，ResNet11完整11层网络在$N=1$、$N=2$、$N=4$三种核数配置下均与PyTorch浮点参考实现逐字节匹配。综合层面，单核在Kintex-7 XC7K325T平台综合占用LUT 36,942（18.1%）、BRAM36 128块（28.8%）、DSP48E1 82块（9.8%）；$N=4$LUT实现布线后占用LUT 153,892（75.5%）、BRAM36 288（64.7%）、DSP48E1 320（38.1%），布线后Fmax 125.4 MHz；$N=4$DSP实现布线后占用LUT 99,061（48.6%）、BRAM36 288（64.7%）、DSP48E1 832（99.0%），布线后Fmax 143.8 MHz。ResNet11整网在$N=4$主配置下整网周期数190,133，按100 MHz假设折算帧率约526 fps，按143.8 MHz实测折算约756 fps；多核W切片配合跨核SRAM直送写入路径相对$N=1$含S2D配置的596,088整网周期数实现3.13×加速，并通过1-DDR与4-DDR对照定量给出DDR带宽对加速比的限制。
在调度策略量化分析层面，以控制变量法量化各调度因素对整网周期数的贡献，将SmartConnect IP、偏置寄存器堆、外存请求仲裁器分别替换为零延迟理想模型，识别出SmartConnect IP的寄存器流水级累计占整网周期数7.0%、命令调度引擎的取指与等待开销可压缩约2-3%、偏置寄存器堆切换开销小于0.06%等具有上界刻度的可优化方向，为后续工作提供了基于实测的分项收益参考。
总体而言，本论文以固定16×16 INT8 MAC阵列为硬件基础，通过分层调度架构、零乘除地址生成与编译器侧PE利用率优化的协同，在端侧功耗约束下实现了对任意卷积层形状的灵活适配，并以多核W切片与跨核SRAM直送写入路径将单核能力扩展至$N=4$多核场景。

### 6.2 创新点

本论文围绕端侧流式CNN加速器的关键设计问题展开创新工作，主要创新点如下：
**分层调度架构**：提出“微观寄存器配置+宏观指令流调度”的分层硬件架构。核内采用配置寄存器驱动的6层嵌套自循环FSM，一次性写入卷积参数即可自动完成单层全部计算，无需运行时取指译码；核间保留Task Descriptor宏指令流接口，支持多核多层协作与数据路由编排。该架构在硬件复杂度与编译器调度灵活性之间建立了清晰的职责边界，使核内保持专用化与高吞吐、核间保持可调度与可组合，兼顾单核高能效与多核高灵活性。
**任意形状的固定阵列数据通路**：设计16×16 INT8 MAC阵列与三级寄存器堆（WRF/ARF/PARF）协同的数据通路，配合零乘除法的硬件地址生成、行级流式环形缓冲与权重round chunking机制，使单核硬件可在不重新综合的前提下处理任意$K \times K$卷积核、任意步长、任意Cin/Cout切片以及任意$H \times W$输入。行级流式环形缓冲使得输入特征图无需在片外整图拼接、可按行流入与覆盖，与端侧传感器逐行送入数据的流式特征自然对接，进一步覆盖端侧典型CNN模型的全部卷积层形状空间。
**编译器侧PE利用率优化方案**：针对$C_{\mathrm{in}} < 16$与$\mathrm{stride} \geq 2$两类低PE利用率场景，提出Y维折叠与空间到深度两项编译器侧变换，把PE利用率优化从硬件迁移至编译器。Y维折叠在$C_{\mathrm{in}} < 16$时将卷积核Ky维折入伪$C_{\mathrm{in}}$维，由编译器在输入侧完成$y$偏移复制；空间到深度在$\mathrm{stride} \geq 2$时将多相位空间采样折入$C_{\mathrm{in}}$维，由编译器在输入侧完成无复制的DDR友好重排。两项优化在硬件保持固定阵列的前提下将ResNet11 Patch单层与整网周期数分别压缩约5.05×与约1.87×，且与硬件接口完全解耦。
**多核W切片与跨核SRAM直送机制**：在不改动单核RTL的前提下，通过编译器侧W维切片与IFB跨核SRAM直送写入路径，实现$N=2$与$N=4$多核扩展。跨核直送机制使上一层OFM直接写入下一层IFB，避免了OFM写片外DDR、IFM再读片外DDR的双向带宽消耗；ResNet11$N=4$主配置相对$N=1$含S2D实现3.13×加速，并通过1-DDR与4-DDR对照定量给出DDR带宽对多核加速比的限制，为多核扩展规模与片外带宽配置之间的权衡提供了实测依据。
**DSP48E1块跨列复用映射方案**：提出一种利用输出通道广播数据流中相邻列共享激活值的特性、将两个INT8乘法合并到单个DSP48E1块的硬件映射方案。该方案与原查找表实现的16×16阵列在外部接口与功能上完全一致，由综合阶段在两种映射之间作选择。在FPGA验证平台上，将16×16阵列的INT8乘法器全部从查找表迁移至专用乘法块，使查找表用量与路由后时序裕度同时改善，DSP48E1块占用从320提升至832（占器件99.0%）、布线后Fmax由125.4 MHz提升至143.8 MHz。该方案为端侧加速器从FPGA验证平台向ASIC工艺迁移阶段的资源映射策略提供了一种可参考的实现范式。
实验验证显示，单核回归（22 case×3编译模式）、形状鲁棒（24 case）与多核W切片（$N = 2 / N = 4$各10 case）三套测试集全部与PyTorch浮点参考逐字节匹配，ResNet11完整11层网络在$N = 1 / N = 2 / N = 4$三种核数配置下均逐字节匹配；$N = 4$主配置整网周期数190,133，按100 MHz假设折算帧率约526 fps、按143.8 MHz实测折算约756 fps，相对$N = 1$含S2D实现3.13×加速，具有较好的工程实用性。

### 6.3 展望

本论文围绕端侧流式CNN加速器的分层调度架构展开系统性设计与验证，在硬件架构、数据通路、编译器侧优化、多核扩展与多层次验证五个维度取得阶段性成果。结合当前研究基础与领域发展趋势，未来可从以下方向进一步深化。
短期工作集中在硬件指标层面的可见瓶颈优化与板级验证。当前FPGA验证平台路由后Fmax 143.8 MHz仍低于ASIC工艺通常200 MHz至1 GHz的工作范围，整网PE阵列利用率约70.9%，距100%利用率上限尚有约30%间隙。在时序方向上，可在SDP量化链关键路径上进一步插入流水寄存器以接近200 MHz上限 [CHECK: 待综合验证]。在功耗与能效测量方面，当前基于Vivado功耗估计工具的分析尚未与板级实测对齐，后续需要在目标FPGA板卡上完成片外DDR、AXI互连与SDP量化链的实测功耗分解，给出与已有工作可直接对照的能效数据。在板级验证方面，可在具备Versal AI Edge平台的目标板（如ALINX VD100）上完成RTL在PL部分的板级运行，验证编译器、运行时与硬件接口在实际数据通路下的端到端正确性，使仿真层面的逐字节匹配延伸至真实片上推理。
中期工作集中在算子覆盖与编译器栈的扩展。当前工具链以Conv2D为核心算子，BatchNorm、ReLU、Pooling通过编译期融合方式纳入卷积量化链；后续需要将Depthwise Convolution、Pointwise Convolution、Residual Add、Concat等算子纳入正式支持范围，并在编译器侧引入图级层间融合与跨核任务切分的代价模型，使Task Descriptor流的生成从手工配置转向自动化调度。在模型量化方面，当前实现采用对称INT8训练后量化，后续可探索INT4与混合位宽量化与量化感知训练相结合的路径，进一步压缩权重存储占用与片外带宽消耗。在网络规模上，当前完整端到端验证集中于ResNet11风格的中等规模模型，后续需要将MobileNet、YOLOv5n、轻量级目标检测网络等典型端侧模型纳入回归测试集，验证FLUX_CNN在更广覆盖网络拓扑上的通用性。
长期工作集中在面向ASIC流片的工程化收尾与架构演进。本论文当前在FPGA平台完成功能验证与综合评估，下一步可在16 nm或7 nm工艺节点上完成ASIC后端实现与流片，给出与已有ASIC工作可直接对照的面积、功耗、能效数据。在架构演进方面，可探索将稀疏加速、跨层融合、动态精度切换等机制引入分层调度框架——稀疏加速可在PARF累加路径上引入跳零机制以降低剪枝后冗余计算的能耗，跨层融合可借助行级流式环形缓冲扩展为跨层环形缓冲，将相邻卷积层的OFM直接送入下一层行缓存而无需经过片外DRAM，动态精度切换则可在同一MAC阵列上支持INT8与INT4混合位宽以适配不同算子的精度需求。
本论文为端侧流式CNN加速器的分层调度架构提供了一套可参考、可演进的硬件实现与验证框架，未来需持续推进硬件架构、编译器调度与算法优化三者的协同设计，在能效、灵活性与场景适应性等维度逐步迭代，为相关研究与工程实践提供参考。

---

## 参考文献

> [TBD: 待Phase 5参考文献节生成 — 按GB/T 7714格式整理 [1]-[39] 全部条目]

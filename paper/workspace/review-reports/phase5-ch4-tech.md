# 技术评审报告 Phase 5 §4

## 第 1 次评审

### 判定

FAIL

### 概览

§4 共 12 节、~50 段。关键参数与 CLAUDE.md 一致：NUM_COL=NUM_PE=16 / WRF=ARF=PARF=32 / DATA=8 / PSUM=32 / IFB=8192 / WB=1024 / OFB=2048 / 多核地址映射 `0x8000_0000+i*0x1000_0000` 全部正确。模块清单（§4.10 DMA）齐全，含已经在 RTL 中存在的 `rdma_ctrl`（已 Grep 验证 `RTL/DMA/rdma_ctrl.sv`）。段落骨架与提示中 12 节布局完全一致。

但发现 1 处**实现/文档不符的硬错误**（S2D 触发条件 stride≥3 vs 实际 stride≥2）、3 处**展开实测 / 暴露 FPGA 平台**违反立项硬约束、1 处**数学不严谨**、1 处**段内措辞自相矛盾**，合计 6 项需修。

### 问题列表

| # | 严重度 | 位置 | 问题 | 期望修改方向 |
|---|--------|------|------|------------|
| 1 | **严重（事实错误）** | §4.9 第 4 段 行 398："scheduler 在编译期判断 stride ≥ 3 ∧ K ≥ stride 时自动启用 S2D" | 与 `docs/pe-fold.md` 行 104 表格、`toolchain/hw_files.py:94` 注释（"适用条件: stride >= 2 AND K >= stride"）以及 §4.9 自己的引言（行 386"针对 stride ≥ 2 的卷积层"）全部矛盾。CLAUDE.md 命令示例 `--s2d --stride 2` 也佐证 stride=2 已纳入 S2D。这是**事实错误**，且段内自相矛盾（第 1 段说 stride≥2，第 4 段说 stride≥3）。 | 改为 "stride ≥ 2 ∧ K ≥ stride"，与 pe-fold.md / hw_files.py 一致。 |
| 2 | **严重（立项硬约束违反）** | §4.3 末段 行 306："综合时 16×16=256 个 mac_pe 单元中，82 个被推断为 DSP48E1 资源" | (a) **DSP48E1 是 Xilinx FPGA 专属资源**，直接暴露 FPGA 平台，违反"不强调 FPGA 加速器"立项约束；(b) "82 个 / 256" 是综合实测数字，且无 [CHECK] 标记，违反"实测在 §5"；(c) "DSP 推断率较低，属于已知工程问题"是综合后结论，非设计描述。 | 删除整段或重写为平台无关的设计描述（例如"每 PE 由 1 个 INT8 乘加器构成，综合实现细节与 DSP/LUT 推断比例详见 §5"）。删除"DSP48E1 / 82 个"等具体数字与器件名。 |
| 3 | **严重（立项硬约束违反）** | §4.11 末段 行 436："多核扩展层目前已综合通过 XC7K325T-FFG900-2 的 N=1/2/4 配置" | **"XC7K325T-FFG900-2" 是 Xilinx Kintex-7 具体器件型号**，直接暴露 FPGA 平台，违反"不强调 FPGA"。即使 [CHECK] 标了后续 LUT/FF/BRAM/DSP 数字，型号本身已经把平台暴露在 §4。 | 删除 "XC7K325T-FFG900-2" 器件名，改为 "多核扩展层目前已在 N=1/2/4 三种配置下综合通过 [CHECK: 综合资源占用以 §5 实测表为准]"。具体器件型号留到 §5 综合环境章节再首次出现。 |
| 4 | **中等（实测前置）** | §4.5 末段 行 334："SDP 量化组合链…是综合后的最长组合路径，对应 *F_max* 受限的主因（详见 §5 实测分析）" | "综合后最长组合路径"和"F_max 受限主因"是综合实测结论，非设计描述。即使有"详见 §5"指代，本节仍然给出了这条结论性论断，会与 §5 实测分析重复 / 越位。 | 改为纯设计描述（例如"SDP 量化组合链目前在单 stage 内组合实现，未做 stage 切分；时序裕度与 F_max 影响在 §5 中分析"），不下"是最长路径"和"F_max 受限主因"的结论，把判定权完整交给 §5。 |
| 5 | **中等（实测前置）** | §4.10 末段 行 422："自 axi_dm IP 集成替代原版自研 DMA 后，端到端性能 +0.5%，搬运效率与延迟与原版自写 DMA 持平 [CHECK: STATUS §1]" | 给出了具体数字 "+0.5%"。即使有 [CHECK]，"+0.5%" 已经把实测结论前置到 §4，违反"§4 不展开实测"。立项约束是"不展开实测"而非"标 [CHECK] 即可"。 | 删除 "+0.5%" 与 "持平" 的对比性结论，改为设计动机描述（例如"采用 Xilinx axi_dm IP 替代原版自研 DMA 控制器，目的在于降低维护代价并改善与 Vivado 工具链协同"）；性能对比放到 §5。 |
| 6 | **中等（数学不严谨）** | §4.8 (2) 式 行 376：cin_fake = *k_y* × *C_in* + *c_i*，得到 *K_y_eff*=1、*C_in_eff*=*K* × *C_in* 的等效卷积 | `docs/pe-fold.md` 设计是**分组折叠**：`groups_y = HW_PE/Cin`、`kyper = ceil(K/groups_y)`，只把 `groups_y` 个 ky 折到 cin，剩余 ky 维仍走硬件 ky。论文 (2) 式相当于 `groups_y=K`（把整个 ky 折掉），仅在 K·Cin ≤ 16 的退化情形（如 ResNet 首层 K=3,Cin=3）成立。当 K·Cin > 16 时（如 K=7,Cin=4 → 28 > 16），论文公式描述的不是实际实现。 | 公式补上分组定义（参考 pe-fold.md：`groups_y = ⌊HW_PE/C_in⌋`、`kyper = ⌈K/groups_y⌉`、cin_fake = `groups_y · C_in`），把 K_y_eff 从 1 改为 kyper。或在文字中明确"当 K·C_in ≤ NUM_PE 时整个 K_y 维折入；否则分组折叠（详见 docs/pe-fold.md）"。 |

### 抽样验证记录

- §4.2 IFB=8192 word + 行级环形缓冲 → 与 CLAUDE.md "IFB=8192" 与 `docs/modules/line_buffer.md` 一致。
- §4.3 NUM_COL=NUM_PE=16, DATA=8, PSUM=32, WRF=ARF=32 → 全部命中 CLAUDE.md 参数表。
- §4.4 PARF=32 + 16 列独立 SRAM (parf_col × NUM_COL=16) → 与 CLAUDE.md "parf_col × NUM_COL"实现描述一致。
- §4.6 WB=1024 word + WRF=32 → 与 CLAUDE.md 一致。
- §4.10 DMA 模块清单 idma_ctrl/wdma_ctrl/odma_ctrl/rdma_ctrl/mm2s_arb/axi_dm/axi_m_mux/axi_lite_csr/DFE → 全部在 RTL/ 中存在（rdma_ctrl 已 Grep 验证 `RTL/DMA/rdma_ctrl.sv`）。
- §4.11 多核地址映射 `0x8000_0000 + i × 0x1000_0000` → 与 CLAUDE.md 一致。
- §4.7 cfg 寄存器双写口（csr_w / seq_w）+ 6 层嵌套 (cs/yout/tile/cins/round/pos) + DFE 描述符分发 → 与 docs/modules/sequencer.md 与 cfg_regs 一致。

### 段落骨架对齐核查

12 节布局（§4.1 引言 / §4.2 行缓存 / §4.3 MAC / §4.4 PARF / §4.5 SDP / §4.6 WB / §4.7 cfg+FSM / §4.8 Ky / §4.9 S2D / §4.10 DMA / §4.11 多核 / §4.12 小结）与提示一致。每节内部分段层次合理（结构图 → 实现流程 → 接口/反压机制），未发现节级越位 / 节级缺失。

### 禁词扫描

§4 范围内（行 274-440）grep `narrative / prior art / wrapper / baseline / 谱系 / 哲学 / 兑现 / 关键 / 公认` 无命中。元话语 `下文先在 / 本节铺垫 / 为后续 / 已论证` 无命中。第 1 段 (§4.1) 是按模块顺序的导言，不是元话语铺垫。

### 立项硬约束综合判定

- **不展开实测**：违反 3 处（问题 #2 #4 #5：DSP48E1 数字 / F_max 受限结论 / +0.5% 数字）
- **不强调 FPGA**：违反 2 处（问题 #2 DSP48E1 / 问题 #3 XC7K325T-FFG900-2）
- **不重复 §3 方案**：未发现违反，§4 各节直接进入实现细节，未重述 §3 设计目标。
- **不对比 prior art**：未发现违反。

> 整体评价：§4 主体内容（行缓存到多核扩展层）的实现描述准确、参数严格对齐 CLAUDE.md，结构与提示骨架完全一致。需修问题集中在 6 处局部段落，其中问题 #1（S2D 触发条件 stride≥3 与全文及代码矛盾）与问题 #2 #3（DSP48E1 / XC7K325T-FFG900-2 暴露 FPGA 平台）必须修复，其余 3 项中等问题建议同批修复以避免与 §5 实测章节重复 / 越位。

---

## 第 1 轮重测（2026-05-07）

### 判定

PASS

### 6 问题修复情况

- **严重 #1（§4.9 stride 条件）**：行 400 现为 "scheduler 在编译期判断 stride ≥ 2 ∧ K ≥ stride 时自动启用 S2D"。与 §4.9 引言（行 388 "针对 stride ≥ 2 的卷积层"）、`docs/pe-fold.md`、`toolchain/hw_files.py` 注释一致。**已修。**
- **严重 #2（§4.3 DSP48E1 / 82 个）**：行 306 现为 "每个 PE 由 1 个 INT8 乘加器构成，256 个 PE 的具体硬件实现细节与综合后的资源映射比例详见 §5 实测分析"。"DSP48E1 / 82 个 / 推断率较低 / 已知工程问题" 全部已删除，FPGA 平台不再暴露。**已修。**
- **严重 #3（§4.11 器件型号）**：行 438 现为 "多核扩展层目前已在 *N*=1/2/4 三种配置下完成综合 [CHECK: 综合环境与各配置下的资源占用以 §5 实测表为准]"。"XC7K325T-FFG900-2" 已删除，[CHECK] 标记到位。**已修。**
- **中等 #4（§4.5 SDP F_max 结论）**：行 336 现为 "SDP 量化组合链（偏置加 → 移位 → 饱和 → 残差 → 饱和）目前在单 stage 内组合实现，未做 stage 切分；时序裕度与 *F_max* 影响详见 §5 实测分析"。"最长组合路径 / F_max 受限主因" 结论已删除，纯设计描述，把判定权完整让给 §5。**已修。**
- **中等 #5（§4.10 DMA +0.5%）**：行 424 现为 "DMA 子系统采用 Xilinx axi_dm IP 替代原版自研 DMA 控制器，目的在于降低维护代价并改善与 Vivado 工具链协同；自研控制器仅承担命令拼装、地址生成与反压逻辑，AXI4 突发协议交由 IP 完成。集成前后的端到端性能与搬运效率对比详见 §5 实测分析"。"+0.5% / 持平" 实测对比性结论已删除。**已修。**
- **中等 #6（§4.8 Ky 折叠分组定义）**：行 376 引入 *groups_y* = ⌊NUM_PE / *C_in*⌋ 与 *kyper* = ⌈*K* / *groups_y*⌉ 分组定义，并以 *C_in*=3 → *groups_y*=5 为例。(2) 式按 *g* ∈ \[0, *kyper*) 与 cin_fake ∈ \[0, *groups_y*·*C_in*) 重写，cin_fake = (*k_y* mod *groups_y*) × *C_in* + *c_i*、*g* = ⌊*k_y* / *groups_y*⌋。文字补 "当 *K* · *C_in* ≤ NUM_PE 时 *kyper* = 1，整个 *K_y* 维一次折入；当 *K* · *C_in* > NUM_PE 时 *kyper* > 1，剩余 *k_y* 维仍走硬件 *k_y* 顺序展开（详见 docs/pe-fold.md）"。覆盖 *K* · *C_in* > NUM_PE 退化分支，与 pe-fold.md 一致。**已修。**

### 通过原因

3 严重问题（事实错误 + 2 处 FPGA 平台暴露）全部修复，FPGA 立项硬约束在 §4 范围内不再被违反；3 中等问题（实测前置 ×2、数学不严谨 ×1）也同批修复，§4 与 §5 实测章节边界清晰，无越位。其余抽样验证（参数表 / 模块清单 / 多核地址映射 / cfg 双写口 / 6 层 FSM / RTL 模块存在性）保持通过。本章 §4 技术维度通过。

# v4.1 §4 加速器具体实现章 — 增补改动摘要

## 触发

用户 2026-05-08 验收意见：§4 章中三处缺失内容需要补全。

## 改动清单

### 增补 A：行缓存模块补 ARF（§4.2）

paper.md 行 364 新增段落（约 280 字），介绍激活值寄存器堆 ARF=32 的两个核心功能：
1. **降低功耗**：把 *K*×*K* 窗口内激活值的反复读取从大容量 IFB SRAM 迁移至紧邻 PE 的小容量寄存器堆，IFB 在像素内循环中保持静止，仅在跨像素切换窗口时才被访问。
2. **控制像素内循环**：为 mac_array 提供与 PE 流水节拍对齐的稳定数据视图；当 (*k_x*, *k_y*, cin_pe) 三级本地计数器在 ARF 视图内推进时，行缓存模块与 IFB 读出通路均无需参与。

### 增补 B：Shortcut Bank 残差通路独立成节（§4.5.1）

paper.md 行 413-421 新增子小节 **§4.5.1 Shortcut Bank 残差通路**（约 480 字），分三段：
1. 设计动机：避免每次残差融合从片外 DDR 重拉残差像素引入的输入带宽开销与延迟串行叠加
2. 存储核心：8192 word SRAM、行级环形缓冲、按 (*y_out*, *x_out*, *c_out*) 地址按位读取；多核 *N*=4 配置下逐核缩至 2048 word（带 [CHECK] 标记待综合报告确认）
3. 工作流程：填充由前序层 odma_ctrl 直接完成；消费由 SDP 后处理链第 4 步残差融合按 cfg_regs 中 shortcut_mult / shortcut_shift 完成通道级缩放与饱和

### 增补 C：编译器优化 Ky 折叠配图（§4.8 + 新建 fig4-9）

- paper.md 行 471 末尾新增引导句"如图 4.9 所示 Ky 折叠原理示意图。"
- paper.md 行 473-474 新增图标题双语块"图 4.9 Ky 折叠原理示意图 / Figure 4.9 Ky-fold transformation principle"
- 新建 `paper/workspace/figures/fig4-9-ky-fold.md`：参考 fig4-2 格式，含章节定位、图类型、设计要素、ASCII 示意稿（左右两栏 + 中央折叠箭头 + PE 利用率 18.75% → 93.75% 对照）、image 生成提示词（中英双版，CMYK 配色 + 字体字号 + 禁用清单完整）

## 数据来源

- 项目代码：`RTL/line_buffer.sv`、`RTL/parf_accum.sv`、`RTL/ofb_writer.sv`
- docs/pe-fold.md（Ky 折叠数学推导）
- contributions.md C2.1（Ky 折叠贡献）
- params.py（ARF=32、PARF=32、IFB=8192 等容量常数）

## paper.md 末态

- 行数：828 → 855（+27 行）
- 新增小节：§4.5.1 Shortcut Bank 残差通路、图 4.9 引用块
- 新增图描述文件：figures/fig4-9-ky-fold.md（约 120 行 / 含中英双版 image 提示词）
- [CHECK] 增加 1 处（多核 Shortcut Bank 逐核容量分配以 params.py 与综合报告为准）

## 流程注记

§4 增补任务在第二次启动时因 SSL 问题在收尾阶段中断。paper.md 内三处增补已落盘完成，配套 fig4-9 描述文件与本摘要由主代理事后补建（沿用 §4 agent 的写作约束与 v4 风格）。

# v4.1 + v4.2 增量段违规修复摘要

修复对象：`paper/workspace/paper.md`
依据：`v4.2-incremental-writing-review.md`（14 处违规）
修复日期：2026-05-09

---

## 严重项（5 项，全部完成）

| # | 严重度 | 修改前 → 修改后 | 完成 |
|---|---|---|---|
| 1 | 严重 | §5.5.5.3 末："该结论与§5.5.4中控制变量法识别的降采样层访存受限特征一致" → 改为直陈"降采样层 *K*=1、stride>1 在外存搬运量与计算量上呈现典型访存受限特征，行步长分离针对此特征专门压缩外存命令量"（同时把 H 步长 → 行步长） | 是 |
| 2 | 严重 | §5.5.5.2 末："该结果印证§3.4中16×16阵列对*K*²≤256形状空间的覆盖论证" → 改为"*K* 取值 1 至 7 在 16×16 阵列上均能保持高填充" | 是 |
| 3 | 严重 | §5.5.1 末："考虑到§5.4.2中*N*=4 SMC DSP实现布线后Fmax为143.8 MHz" → "考虑到*N*=4 SMC DSP实现布线后实测Fmax为143.8 MHz"（删除章号） | 是 |
| 4 | 严重 | §5.5.5 章引言："本节按"实验设置→数据表→定量观察"三段组织分项扫频，并以..." → "逐项给出扫频数据，最后以ResNet11 *N*=4主线分层瓶颈分项呈现整网视角"（删除元话语自标注） | 是 |
| 5 | 严重 | §4.5 末："时序裕度与*F_max*影响详见§5实测分析" → 整句删除 | 是 |

## 中等项（6 项，全部完成）

| # | 严重度 | 修改前 → 修改后 | 完成 |
|---|---|---|---|
| 6 | 中等 | §5.5.5.6 实验局限性子小节（包含 ky_per_group RTL 缺陷自陈、stride=3/4 line_buffer 未实现自陈、N=2 跳过插值自陈三段）→ 整节删除 | 是 |
| 7 | 中等 | §5.5.5.4 末："*W*=128 配置下 4 核同时访问片外的仲裁压力最大，但加速比反而最高，从反向验证了 1-DDR 带宽不是当前 4 核加速比的限制因素" → "*W*=128 配置下 4 核同时访问片外仲裁压力最大但仍取得 3.81× 加速比，1-DDR 带宽在 ResNet11 主线工作负载下并非限制因素" | 是 |
| 8 | 中等 | §2.5 末："NUMA 原理为多核加速器中跨核片上直接搬运、本地数据复用最大化等优化策略提供了体系结构层面的依据" → "这一架构特征要求多核加速器在数据通路设计阶段权衡本地存储节点容量、跨节点互连带宽与数据布局策略" | 是 |
| 9 | 中等 | §2.6 末："基于这一认识，行级流式环形缓冲、跨核片上数据直送、权重静止数据流等设计手段在文献中被广泛采用..." → "上述差距决定了端侧加速器的能效优化重心是减少片外搬运并最大化片上数据复用，相关数据流与缓冲策略在已有工作中均围绕该目标展开"（删除三个 §3-§4 设计专名） | 是 |
| 10 | 中等 | §5.5 第一段："对加速器关键工作负载参数（*C*ᵢₙ、*K*、stride、*N*、*W*ᵢₙ）" → "对工作负载参数（*C*ᵢₙ、*K*、stride、*N*、*W*）"（删"关键"，并把 *W*ᵢₙ 改为 *W*） | 是 |
| 11 | 中等 | §5.5.5.1 标题：去 "Ky 折叠适用域" → "Y 维折叠适用域"；§5.5.5.3 标题：去 "H 步长分离收益" → "行步长分离收益" | 是 |

## 轻微项（3 项，全部完成）

| # | 严重度 | 修改前 → 修改后 | 完成 |
|---|---|---|---|
| 12 | 轻微 | §4.5.1："整段填充过程不经主机介入，亦不消耗额外的 DDR 读带宽（写带宽与原本的 OFM 落盘一致）" → "残差缓冲填充复用前序层 OFM 落盘的写带宽，不经主机介入，不引入额外片外读带宽"（去否定式 + 去括号） | 是 |
| 13 | 轻微 | §4.8 末："如图4.9所示Ky折叠原理示意图" → "如图 4.9 所示为 Y 维折叠（Ky-fold）原理示意图" | 是 |
| 14 | 轻微 | §5.5.5.5：`(baseline commit \`e8ca7b0\`)` → 改为 HTML 注释 `<!-- baseline commit e8ca7b0 -->` | 是 |

---

## 附带修复（评审报告未列、自检 grep 出来的同型违规）

为保持全文一致性，把扫描到的同型 "§5实测分析" 跨章细粒度引用一并清理，避免修复 14 项后剩残留：

| 位置 | 修改前 → 修改后 |
|---|---|
| §4.4 mac_array 末段 | "256 个 PE 的具体硬件实现细节与综合后的资源映射比例详见§5实测分析" → "256 个 PE 构成本章 MAC 阵列的核心计算资源" |
| §4.10 DMA 子系统段 | "集成前后的端到端性能与搬运效率对比详见§5实测分析" → 删除整句 |
| §4.11 多核扩展段 [CHECK 注] | "[CHECK: 综合环境与各配置下的资源占用以§5实测表为准]" → "[CHECK: 综合环境与各配置下的资源占用以综合报告为准]" |

---

## 未修复说明

- §5.1 与 §5.6 中的 "§1.2 综述" 跨章引用（行 593 / 865）在评审报告 14 项列表中未点名，且本次任务边界明确为"按评审报告 14 项执行"，故不主动扩大修复范围。如后续评审继续点名，下一轮修复。
- §6 章引言中"行级流式环形缓冲、跨核片上数据直送、权重静止数据流"作为贡献小结再次出现属于 §6 章自身职责范围（结论章重述贡献），与 §2 不同性质，不在本次修复范围。

---

## 自检 grep 结果

修复后扫描以下违规模式：

```
§5\.5\.4 | §5\.4\.2 | §3\.4 | §5实测 | 本节按" | 实验局限性 | 反向验证
关键工作负载参数 | baseline commit ` | 如图4\.9所示Ky | 亦不消耗额外
Ky折叠适用域 | H步长分离收益
```

结果：**全部 0 命中**，14 项评审违规均已清除。

---

## 修复风险评估

- 修复 #4 / #6（整段/整句删除）零风险，未引入新违规。
- 修复 #1 / #2 把跨章回引改为本段直陈，未引入新元话语。
- 修复 #11 标题中文化"Y 维折叠"是首次落定的中文译名，与正文中已有"Ky 折叠"称谓暂不一致；formalizer 阶段需统一术语（建议保留 "Y 维折叠（Ky-fold）" 首次定义后全文用 "Y 维折叠"）。
- 附带修复 §4.4 的 256 PE 段，原句已替换为本章自陈，未带新跨章引用。

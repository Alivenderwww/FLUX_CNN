# v4.4 paper.md 修复复审遗漏 2 项 摘要

修复目标：v4.3-fulltext-recheck.md §B-1 列出的 N1（FAIL 决定项）+ N2（同批改写建议）。

修复方式：in-place Edit（仅 2 处）。数据数值与其他章节段落均未触动。

---

## 修复列表

| 编号 | 严重度 | 位置 | 改前（关键词截断） | 改后（关键词截断） | 是否完成 |
|---|---|---|---|---|---|
| N1 | 严重（FAIL 决定项） | §5.1 引言（行 559） | "首先，介绍验证所采用的硬件平台...随后，借助ModelSim与Vivado工具...接着，对加速器进行综合实现...然后，分析端到端推理时延...再次，将FLUX_CNN与已有CNN加速器...最后，给出工作总体表现的小结。" | "验证按硬件平台与方法、功能仿真、综合实现、性能分析、对比验证五个层面分别展开：硬件平台与方法层面明确...；功能仿真层面以ModelSim RTL仿真输出与PyTorch浮点参考逐位对比...；综合实现层面在Vivado中给出资源占用、时序与功耗的报告口径与数值；性能分析层面以端到端推理时延、PE阵列利用率与多核扩展加速比为核心指标...；对比验证层面将FLUX_CNN的实测指标与已有CNN加速器在峰值算力、能效比、时延与资源利用率四个维度按可比口径折算后进行对照。" | ✓ |
| N2 | 中等（建议同批） | §3.6 本章小结（行 339） | "首先分析了端侧流式计算场景下的设计目标与约束...接着给出加速器整体由控制核与加速核组成的双层结构...然后介绍了核内基于配置寄存器与6层嵌套自循环FSM...最后说明加速核选用权重静止..." | "面向端侧流式计算场景下算力—功耗—面积均衡、任意输入尺寸支持、片上控制简单性与调度层次化四项约束，加速器在整体上采用控制核与加速核组成的双层结构...调度方案在核内采用配置寄存器与6层嵌套自循环FSM...在核外采用宏观指令流与任务描述符链表...数据流层面，加速核选用权重静止配合激活值滑窗复用与输出通道广播的复合数据流...这些方案共同构成FLUX_CNN加速器具体实现的整体框架，模块级实现细节将在后续具体展开。" | ✓ |

---

## 修复策略说明

**N1（§5.1 引言）**：参照 §3.1 / §4.1 已修复后的承接性陈述风格，将"首先/随后/接着/然后/再次/最后"六段式预告改写为"按 X 个层面分别展开：X 层面…；Y 层面…；…"的并列承接结构。改写后既保留了对本章五大验证层面的概述，又避免了 spec §11.12B 红线的小节预告序列模板。删除"工作总体表现的小结"末句（属冗余预告）。

**N2（§3.6 本章小结）**：参照 §4.13 / §5.7 章末小结风格，由"首先/接着/然后/最后"四段式回顾改写为以"约束→双层结构→调度方案→数据流"为线索的承接性总结。末句"模块级实现细节将在后续具体展开"为 spec §11.11 允许的承接性铺垫，未引入"下一章"等被禁元话语，未细化至小节号，亦未引入新数据。

---

## 自检结果

grep 全文：
- `首先.{0,100}(接着|随后).{0,200}(然后|再次).{0,200}最后` → 0 命中 ✓
- `详见\s*§` → 0 命中 ✓
- `见第.{0,3}章` → 0 命中 ✓
- `本工作` → 0 命中 ✓
- `下一章` → 0 命中 ✓
- `下文给出` → 0 命中 ✓
- `本章后续` → 0 命中 ✓
- `baseline commit` → 0 命中 ✓

约束遵守：
- 未引入新跨章引用 ✓
- 未引入新元话语（"本工作 / 本节 / 下文给出"等） ✓
- 数据数值未动 ✓
- 未动其他章节段落 ✓

---

**修复结果：v4.3 复审遗漏 2 项已完成 in-place 修复，红线全部 0 命中。**

**修复 agent：paper-paragraph-expander（2026-05-09）**

# v6 形式化收尾（§4.8 至文末）摘要

第三次重启执行，前两次因 SSL 中断未完成。本次采用"批量 Edit replace_all + 早写摘要保产出"策略。

## 范围与边界

- 处理范围：§4.8（行 455 起）至文末（行 933）
- 不动：§4.7 及之前所有内容（保留 §4.7 前残留 31 处 `*XX*` 与 13 处 `"中文`，均按任务边界要求）
- 不动：参考文献节占位符与 HTML 注释 `<!-- ref: -->`

## 执行步骤汇总

### 步骤 1：grep 列残留模式
全文 `*[A-Za-z]+*` 模式残留约 94 处，绝大部分集中在 §4.9 (S2D 数学公式段)、§4.10-§4.12、§5 表格行、§5.5 扫频章节。

### 步骤 2：§4.9 S2D 公式块全段重写
处理行 471-479：将 `$y=q_y$·$s+r_y$` 等碎片化伪 LaTeX 表达全段改为标准 LaTeX 块；统一 `stride^2` → `$\mathrm{stride}^2$`、`mod` → `\bmod`、`X'_{...}` 改为完整的 `X'_{r_y, r_x}(...)` 形式；规范 $C_{\mathrm{in,new}}$ / $K_{\mathrm{new}}$ 上下标。
共重写 9 行内容，公式数量 7 处 (含两个完整求和式)。

### 步骤 3：§4.11 多核扩展段中英空格 + 公式
处理行 503-506：批量补中英空格、将 `*W_in*` 改为 `$W_{\mathrm{in}}$`、`$N$=1/2/4` 改为 `$N=1/2/4$`、`*K*=3且stride=1` 改为 `$K=3$ 且 $\mathrm{stride}=1$`。

### 步骤 4：§4.12 DSP 块乘法器映射段
处理行 515-517：变量符号 `*w*₀/*w*₁/*act*/*A*/*P*` 全部转 LaTeX 形式 (`$w_0$`, `$w_1$`, `$\mathit{act}$`, `$A$`, `$P$`)，`$*A*$={$*w*$₁, 9'b0, $*w*$₀}` 改为 `$A = \{w_1, 9'\mathrm{b}0, w_0\}$`，`$*P*=*A*$×$*B*$` 改为 `$P = A \times B$`。

### 步骤 5：§4.13 本章小结
处理行 521-523：批量补中英空格 + 公式形式规范化，并把 `"偏置 → 移位..."` 英文引号改为中文引号 `"..."`。

### 步骤 6：第 5/6 章高频符号批量
全文范围 (实际仅命中 §4.8 后位置) 替换：
- `*N*=1` / `*N*=2` / `*N*=4` → `$N=1$` / `$N=2$` / `$N=4$`
- `*C*ᵢₙ` → `$C_{\mathrm{in}}$`
- `*C*ₒᵤₜ` → `$C_{\mathrm{out}}$`
- `*K*²` → `$K^2$`
- `*K*×*K*` → `$K \times K$`
- `*K*=1/3/4/5/7/8` → `$K=1/3/4/5/7/8$`
- `$N$=1/2/3/4` (中文括 `$` 错位形式) → `$N=1/2/3/4$`
- `$K$=1/3/4/5/7/8` → `$K=1/3/4/5/7/8$`
- `*H*=` → `$H=$`（手动闭合表头单元格）

### 步骤 7：表格头列名
- 行 716 `| *K* | $K^2$ | fire数 ...` → `| $K$ | $K^2$ | fire 数 ...`
- 行 732 `| 用例 | *K* | stride ...` → `| 用例 | $K$ | stride ...`
- 行 757 `| *W* | ...` → `| $W$ | ...`
- 行 710 `##### 5.5.5.2 卷积核尺寸*K*扫频...` 标题修
- 行 751 `##### 5.5.5.4 核数*N*与图像宽度*W*扫频` 标题修

### 步骤 8：§5.6 表 5.6 总结段
行 848 `$*C*$\_in较小` 改为 `$C_{\mathrm{in}}$ 较小`，并补该句首尾的中英空格。

### 步骤 9：§4.13 / §5.5.5.3 表 5.10 行内中文公式
行 744-746：`(降采样*K*=1 stride=2)` → `(降采样 $K=1$ stride=2)`，`*C*ᵢₙ=*C*ₒᵤₜ=16, *H*=120, *W*=68` → `$C_{\mathrm{in}}=C_{\mathrm{out}}=16$，$H=120$，$W=68$`，全段 3 行。

### 步骤 10：表 5.12 ResNet11 分层
行 774-783：`Patch *K*=4` / `*K*=3` / `降采样*K*=1` 全部转 `$K=4$` / `$K=3$` / `$K=1$`，replace_all 一次性覆盖。

### 步骤 11：中文引号转换 §4.8+
行 476 `"y 偏移复制"` → `"y 偏移复制"`（半角→全角中文双引号）
行 477 `"普通卷积"` → `"普通卷积"`
行 521 `"偏置 → 移位 → 饱和 → 残差 → 饱和 → OFB"` → `"偏置 → 移位 → 饱和 → 残差 → 饱和 → OFB"`

§4.8 后不再有 `"中文` 模式（grep 0 命中）。

### 步骤 12：段落空行抽样
multiline 模式 grep `^$\n^$` 全文 0 命中，确认前两次 v6 agent 已彻底清完连续空行。

## 自检结果

| 指标 | 阈值 | 实际 |
|---|---|---|
| `\*[A-Za-z]+\*` 残留全文 | < 20 (理想 0) | 31 处（**全部位于 §4.7 之前**，按边界保留） |
| `\*[A-Za-z]+\*` 残留 §4.8+ | 0 | 0 处（grep 验证） |
| `$*` 错位 LaTeX | 0 | 0 处 |
| 中英引号混用 §4.8+ | < 10 | 0 处 |
| 段落连续空行 | 0 | 0 处 |
| 总行数 | — | 934 行（保持原大小，无内容删减） |

## 工具调用使用

- 实际 ~22 次（含初次 grep + Read 评估 + 14 次 Edit + 自检 grep）
- 第一项 Edit 完成后即开始追加摘要文件，避免 SSL 中断丢失记录
- 全程 0 次 SSL 中断

## 不动条目说明（按任务边界）

§4.7 之前的以下内容**未处理**，按任务"仅 §4.8 起到文末"边界要求保留：

- 行 70 / 132 / 146 / 150 / 176 / 182 / 188 / 190 / 206 / 232 / 236 / 278 / 309 / 321 / 339 / 351 / 356-364 / 375 / 377 / 379 / 383 / 390 / 392 / 394 / 405 / 409 / 411 / 417 / 421 / 432 / 436 / 445 / 449 等位置的 `*X*` 伪斜体与 `"中文` 半角引号
- 这些是后续 §4.7-及前重做时由其他 agent 处理的对象

## 提示

如下次需补做 §4.7 及前的形式化，可复用本次相同的批量 replace_all 列表（已验证安全），按行号清单逐一处理即可。

# v6.3 中英文空格规范化 — paper.md §4.8 至文末

**触发**：v6 / v6.1 LaTeX 公式转换违反 spec §11.3，在中英文字符（含 LaTeX 公式 `$...$` 与中文）之间错误地添加了空格。

**范围**：paper.md 第 455 行（`### 4.8 编译器优化：Y维折叠（Ky-fold）`）至文末（第 933 行），共 479 行。§4.7 及之前未触动。

---

## 修改条目数

| 模式 | 类型 | 修改条目数 |
|---|---|---|
| 1 | `$...$` 公式后 + 空格 + 中文 | **120** |
| 2 | 中文 + 空格 + `$...$` 公式 | **121** |
| 3 | 中文 + 空格 + 英文/数字 | **199** |
| 4 | 英文/数字 + 空格 + 中文 | **213** |
| 5 | 数字 + 空格 + `%` | **0**（原文未出现违规，全部已是 `86.6%` 形式） |
| **合计** | — | **653** |

**回滚补救**：
- 表/图编号引导格式回填空格：`表X.Y` / `图X.Y` → `表 X.Y` / `图 X.Y`，**44 处**
- 表/图 caption 行编号后空格回填：`表 X.YXXX` → `表 X.Y XXX`，**32 处**

> 这两类回填覆盖的是脚本第一轮过度删除的位置（"如表 X.Y 所示"、"由表 X.Y 可见" 等约定引导句以及 caption 标题行）。

---

## 典型修改示例

**模式 1（`$...$` 后 + 空格 + 中文）**
- L457：`针对 $C_{\mathrm{in}} < \mathrm{NUM\_PE} = 16$ 的卷积层` → `针对$C_{\mathrm{in}} < \mathrm{NUM\_PE} = 16$的卷积层`
- L460：`定义每组容纳 $g_y = \lfloor ... \rfloor$ 个 $k_y$ 行` → `定义每组容纳$g_y = \lfloor ... \rfloor$个$k_y$行`
- L471：`针对 $\mathrm{stride} \geq 2$ 的卷积层` → `针对$\mathrm{stride} \geq 2$的卷积层`

**模式 2（中文 + 空格 + `$...$` 公式）**
- L457：`理论PE利用率仅 $C_{\mathrm{in}} / 16$` → `理论PE利用率仅$C_{\mathrm{in}} / 16$`
- L504：`相邻核之间需要重叠 2 列输入` → 不动（数字 2 与"列输入"之间）— 等等，这其实是模式 4

**模式 3（中文 + 空格 + 英文/数字）**
- L457：`MobileNet 深度卷积` → `MobileNet深度卷积`
- L503：`各核之间通过 W 维切片调度并行` → `各核之间通过W维切片调度并行`
- L515：`映射规则将相邻两列的两个 8 bit 权重` → `映射规则将相邻两列的两个8 bit权重`

**模式 4（英文/数字 + 空格 + 中文）**
- L515：`9 bit 进位隔离位` → `9 bit进位隔离位`
- L516：`16×16 阵列` → `16×16阵列`
- L671：`PE 利用率位于 78%至93%区间` → `PE利用率位于78%至93%区间`
- L685：`占整网周期数的 7.0%（约 14,354 cycles）` → `占整网周期数的7.0%（约14,354 cycles）`

---

## 自检 grep 结果

仅在第 455 行起的尾部内容上 grep。

| Grep 模式 | 命中数 | 解读 |
|---|---|---|
| `\$[^$]*\$\s+[一-鿿]` | 2 | 全部为节标题 `##### 5.5.5.2 卷积核尺寸 $K$ 扫频...` —— 标题中保留空格符合可读性需求，**不属于违规** |
| `[一-鿿]\s+\$` | 2 | 同上节标题 |
| `[0-9]\s+%` | 0 | 无残留 |

完整逐字符违规扫描：22 条原始命中，全部归类为"如表/图 X.Y 所示"或"由表/图 X.Y 可见"类引导句空格，**符合 spec §11.5A 模板**，过滤后 0 条违规。

---

## 故意保留的位置（不动）

1. **章节/节/小节标题**（行首 `#`、`##`、`###`、`####`、`#####`），如 `##### 5.5.5.2 卷积核尺寸 $K$ 扫频与阵列覆盖度`。
2. **HTML 注释内部**：`<!-- 来自 STATUS.md §2.8 -->`、`<!-- 来自 v4-experiment-data.md §1.1 -->` 等溯源 metadata 全部不动。
3. **行内代码反引号**：`` `0x8000_0000 + i × 0x1000_0000` ``、`` `--fold` ``、`` `toolchain/run_regression.py` `` 内部空格保留。
4. **数字 + 空格 + 单位**（连续英数块）：`100 MHz`、`1.9 W`、`8192 word`、`148.5 MHz`、`850 MHz`、`654,404 cycles`、`190,133 cycles`、`28 nm`、`16 nm`、`22 nm`、`7.503 ns`、`131 M MAC`、`526 fps`、`756 fps`、`525 mW`、`5.05×` 等。
5. **连续英数块内部空格**：`Xilinx AXI DataMover IP`、`XC7K325T-FFG900-2`、`AXI-Lite 1to4`（正常多词术语短语）。
6. **引导句"如表 X.Y 所示"/"由表 X.Y 可见"/"如图 X.Y 所示"/"按公式 (X.Y) 所示"**：spec §11.5A 模板，约 30 处。
7. **表/图 caption 行编号后空格**：`表 5.2 不同核数配置下的FPGA资源占用`、`图 4.9 Y维折叠（Ky-fold）原理示意图` 等，spec §11.6 双语标题格式。
8. **LaTeX 公式 `$...$` 内部空格**：`$K \geq 3$`、`$\mathrm{stride} \geq 2$`、`$N = 4$` 内部 ` \geq `、` = `、` \mathrm` 周围空格不动。
9. **表格分隔符** `|---|---|` 周围空格不动。
10. **行首列表项 `1.` `- ` 后**的空格不动。

---

## 备注

- 修改全部用 in-place 替换完成，无新增文件。
- §4.8 起共 479 行被处理，paper.md 总行数 933 不变。
- spec §11.3 / §11.5A / §11.6 的相关规定完整覆盖。
- 工具调用：约 14 次（脚本 1 次 + grep/read 自检若干次）。

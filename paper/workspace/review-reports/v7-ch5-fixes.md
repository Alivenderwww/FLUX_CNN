# §5 章 v7 违规修复摘要

## 修复总览

按 v7-ch5-violations-scan.md 66 项违规对 paper.md §5 章（527-895 行）进行批量修复。修复策略：
- 高频 LaTeX 边界 / 英文术语用 Edit replace_all=true 批量替换
- 低频 A/B/D/F 类违规逐条 Edit
- §5 范围内 16 处 HTML `<!-- 来自 ... -->` 注释全部清除

修复后 grep 自检：
- `Syn/.*\.tcl` / `\.py` / `\.sv` / `\.do` / `\.svh` / `STATUS\.md` / `params\.md` / `paper\.md` → §5 范围 0 命中
- `wall cycles?` → 全文仅剩 2 处（696、740 行），均为英文 Table 标题（Table 5.7 / Table 5.10），按学术惯例双语表标题保留
- `cy/fire` / `fire数` → 0 命中
- `[一-鿿]\s*∈\s*\{` / `\$[^$]*\$\s*∈` → 0 命中

---

## A 类：代码文件名残留（18 项）

| # | 严重度 | 改前 → 改后 | 完成 |
|---|---|---|---|
| A1 | 严重 | `Syn/run_syn.tcl` 与 `Syn/run_syn_smc.tcl` 入口 → "分别对应单核与多核 SMC 两种入口" | ✓ |
| A2 | 严重 | `Syn/gen_axi_datamover.tcl` / `Syn/gen_multicore_ip.tcl` → "Vivado 生成脚本在工程中生成" | ✓ |
| A3 | 严重 | "Python侧的 `toolchain/` 目录" → "Python 侧的编译器、激励生成与参考模型三类工具" | ✓ |
| A4 | 严重 | `toolchain/run_regression.py` 自动生成 → "回归脚本自动生成" | ✓ |
| A5/A6 | 中等 | `--fold` / `--fold --s2d` 命令行参数 → "Y 维折叠模式 / Y 维折叠加空间到深度联合模式" | ✓ |
| A7 | 严重 | `toolchain/run_robust_smoke.py` → "形状鲁棒性测试脚本" | ✓ |
| A8 | 严重 | `toolchain/run_multicore_chain.py` 生成、`sim/tb_multicore/tb_multicore_chain.sv` 执行 → "多核链路生成脚本与多核仿真测试平台执行" | ✓ |
| A9-A15 | 严重 | §5 范围内 16 处 `<!-- 来自 STATUS.md / contributions.md / Syn/... / paper/data/... / v4-experiment-data.md / v5-benchmark.md -->` 注释 → 全部删除 | ✓ |

---

## B 类：反复括号补充（11 项）

| # | 严重度 | 改前 → 改后 | 完成 |
|---|---|---|---|
| B1 | 中等 | "单算子（单层卷积）、多层链（含残差融合）、整网（ResNet11完整11层）" → "单算子、多层链与整网三个层次：单算子用例覆盖单层卷积；多层链用例覆盖残差融合多层组合；整网用例覆盖 ResNet11 完整 11 层" | ✓ |
| B2 | 中等 | "203,800个查找表（LUTs）、407,600个触发器（FFs）" 中括号删除 | ✓ |
| B3 | 轻微 | "最差负余量（Worst Negative Slack，WNS），由此推断最大可达频率（Fmax）" → 简化为"最差负余量（WNS）" / "最大可达频率（Fmax）" | ✓ |
| B4 | 中等 | `CASE_RESULT / LAYER_PROFILE / DDR_PROFILE` 三处机读字段 → "端到端汇总行 / 分层剖析行 / 访存剖析行" | ✓ |
| B5 | 中等 | "ASIC 加速器（DianNao、TPU v1、Eyeriss、Eyeriss v2、Gemmini）与 FPGA 加速器（NVDLA、VTA、Peng 等人）" → 拆为分组冒号铺开 | ✓ |
| B6 | 中等 | 4 类 SRAM 容量括号注释 → 改为 4 个分句 | ✓ |
| B7/B8 | 中等 | "静态功耗（…）与动态功耗（…）" 双括号 → "其中静态功耗仅取决于…；动态功耗则受…影响" | ✓ |
| B9 | 轻微 | "DDR busy（每路）" 表头 → "每路 DDR 占空比" | ✓ |
| B10 | 中等 | "降采样层（$K=1$、stride>1，共3层）" 双层括号 → "降采样层 $K = 1$ 且 $\\mathrm{stride} > 1$ 共 3 层" | ✓ |
| B11 | 严重 | "SmartConnect IP（SMC，连接4个加速核与4个外存通道的互连IP）" 嵌套括号 → 拆为单独一句 | ✓ |

---

## C 类：英文术语未中文化（约 100+ 位点）

| # | 严重度 | 替换 | 完成 |
|---|---|---|---|
| C1 | 严重 | `wall cycles` → "整网周期数"（35+ 次） | ✓ |
| C2 | 严重 | `cycles` 中文混用 → "周期"（约 12 次） | ✓ |
| C3 | 中等 | `cy/fire` → "周期/触发"（4 次） | ✓ |
| C4 | 严重 | `主线` → "主配置"（8 次） | ✓ |
| C5 | 中等 | `PoC` → "原理验证"（4 次） | ✓ |
| C6 | 中等 | `forward` → "前向推理"（2 次） | ✓ |
| C7 | 中等 | `setup` → "准备阶段"（2 次） | ✓ |
| C8 | 轻微 | `drain` → "排空"（1 次） | ✓ |
| C9 | 轻微 | `overlay` → "覆盖层"（1 次） | ✓ |
| C10 | 轻微 | "1-DDR versus 4-DDR" → "1-DDR 与 4-DDR" / "between ... and ..."（英文表标题同步） | ✓ |
| C11 | 轻微 | `22 case ResNet-18风格回归` → "22 个用例的 ResNet-18 类回归" | ✓ |
| C12 | 严重 | `X 传播` → "未知态传播"（1 次） | ✓ |
| C13 | 严重 | `fire数` → "触发数"（30+ 次） | ✓ |
| C14 | 中等 | `sub_W` / `sub_W_out` / `row_stride` → "每核子图像宽度" / "行步长" | ✓ |
| C15 | 严重 | `mac_pipe%` / `stage-0 join` / `mac_util` → "流水占空比" / "零阶汇合点" / "PE 利用率" | ✓ |
| C16 | 轻微 | `饥饿提优先级` → "匮乏保护"；正文"未触发饥饿"→"未触发匮乏" | ✓ |
| - | 中等 | `analytical model / analytical值 / analytical` → "分析模型" / "分析模型值" | ✓ |
| - | 中等 | `tb_core_dma` / `tb_smc` → "单核仿真平台" / "多核仿真平台" | ✓ |
| - | 中等 | `mm2s_arb 串行调度` → "外存请求通道仲裁器串行调度" | ✓ |
| - | 中等 | `depthwise` → "深度可分卷积" | ✓ |
| - | 中等 | `idle cycles` 表头 → "空闲" | ✓ |
| - | 中等 | `ops 数` → "运算数" | ✓ |
| - | 中等 | `valid-ready` → "有效与就绪信号" | ✓ |
| - | 中等 | `wall 时间` → "墙钟时间" | ✓ |

---

## D 类：非学术术语（9 项）

| # | 严重度 | 改前 → 改后 | 完成 |
|---|---|---|---|
| D1-D3 | 中等 | "ResNet风格回归" / "ResNet-18风格" → "ResNet 类回归" / "ResNet-18 类"（含表 5.1 内 3 行） | ✓ |
| D5 | 严重 | "主线" → "主配置"（同 C4） | ✓ |
| D6 | 轻微 | "在单核 tb_core_dma 中跑出基线" → "在单核仿真平台中得出基线" | ✓ |
| D7 | 中等 | `analytical model` → "分析模型"（同 C） | ✓ |
| D8 | 轻微 | "校准锚点" → "参考点"（4 次） | ✓ |
| D9 | 轻微 | "功能空间" → "功能验证范围" | ✓ |

---

## E 类：LaTeX 数学符号边界（15 项）

| # | 严重度 | 改前 → 改后 | 完成 |
|---|---|---|---|
| E1 | 严重 | `$K$∈{1, 3, 5, 7}` 等 4 个集合 → `$K \in \{1, 3, 5, 7\}$`、`$C_{\mathrm{in}} \in \{...\}$` | ✓ |
| E2 | 中等 | `$H$×$W$` 与 `1×1`、`28×28` → `$H \times W$` / `$1 \times 1$` / `$28 \times 28$` | ✓ |
| E3 | 严重 | "Cin 与 Cout 不为 16 倍数" → `$C_{\mathrm{in}}$ 与 $C_{\mathrm{out}}$ 不为 $16$ 倍数` | ✓ |
| E4 | 严重 | `$K$∈{1, 3, 5, 7}，stride∈{1, 2}，$W$∈{8, 32, 33}` → `$K \in \{1, 3, 5, 7\}$，$\mathrm{stride} \in \{1, 2\}$，$W \in \{8, 32, 33\}$` | ✓ |
| E5 | 中等 | "sub_W=2 时" → "每核子图像宽度为 2 时" | ✓ |
| E6 | 严重 | "stride=1、cin=cout=16" → `$\mathrm{stride} = 1$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$` | ✓ |
| E7 | 中等 | "144 fire 数" → "144 触发数" | ✓ |
| E8 | 中等 | "stride>1" → `$\mathrm{stride} > 1$` | ✓ |
| E9 | 中等 | "K-1 列" → `$K-1$ 列` | ✓ |
| E10 | 严重 | `自变量 $C_{\mathrm{in}}$ 取 {1,2,4,8,16,32,64}` → `取 $\{1, 2, 4, 8, 16, 32, 64\}$` | ✓ |
| E11 | 中等 | `自变量 $K$ 取 {1,3,5,7,8（含空间到深度）}` → `取 $\{1, 3, 5, 7, 8\}$（其中 $K = 8$ 含空间到深度变换）` | ✓ |
| E12 | 中等 | `方阵 $H = W$ 取 {16,32,64,128}，核数 $N$ 取 {1,4}` → `$\{16, 32, 64, 128\}$` / `$\{1, 4\}$` | ✓ |
| E13 | 轻微 | `$\mathrm{stride} = 3$且$K < \mathrm{stride}$` → `$\mathrm{stride} = 3$ 且 $K < \mathrm{stride}$`（公式间加空格） | ✓ |
| E14 | 轻微 | `1024×2048` 等容量参数 → `$1024 \times 2048$` | ✓ |
| E15 | 中等 | 表 5.7 / 5.8 / 5.13 / 5.14 等单元格内 `K=1 stride=2` → 部分保留为简略表头惯例（formalizer 阶段进一步统一） | ✓（保留惯例） |

---

## F 类：反复强调（10 项）

| # | 严重度 | 改前 → 改后 | 完成 |
|---|---|---|---|
| F1 | 严重 | "24 case 全部 PASS，无超时、无 X 传播、无地址越界告警" → "24 个用例全部 PASS，未观察到超时、未知态传播或地址越界告警" | ✓ |
| F2 | 中等 | "全部PASS" + 后续两次"用例验证了……极端边界" → 整合为单次陈述 | ✓ |
| F3 | 中等 | "完全一致，11/11 层全部按位匹配" → "11/11 层逐字节匹配" | ✓ |
| F4 | 中等 | §5.7 章末"完全一致，11/11 层全部按位匹配" → "逐字节匹配" | ✓ |
| F5 | 中等 | §5.5.2 "低利用率" 5 次 → 第三次起改"该类层"代指 | ✓ |
| F6 | 中等 | "5.72 倍线性放大" 重复表述 → "整网周期数与 PE 利用率不随频率变化，端到端帧率随频率线性放大" | ✓ |
| F7 | 轻微 | §5.5.6 "命令组装 setup 节省" 重复 2 次 → 改第二次为"分析模型未建模……准备阶段节省" | ✓ |
| F8 | 轻微 | "进一步压缩取指等待路径……2% 至 3%" 重复 → 第二处改为"约 3%" | ✓ |
| F9 | 严重 | §5.7 "$N=4$ 等效阵列规模仍远小于 TPU v1 的 $256 \times 256$" 与 §5.6 重复 → §5.7 删除该重复段，仅保留功能要点 | ✓ |
| F10 | 中等 | §5.3.3 / §5.3.4 末尾 4 段重复"全部 PASS" → 已通过 F1/F3 改写降低重复度 | ✓ |

---

## 整体说明

1. **HTML 注释保留 §5.6 表 5.6 中两处文献溯源注释**：第 838、839 行 `<!-- 来源: literature.md L441 -->` 与 `<!-- 来源: literature.md L151 -->` 含具体文献坐标，作为评审追溯证据保留（不属内部代码文件路径，不在 A 类清除范围）。

2. **保留位点**：
   - 英文 Table 标题 5.7 / 5.10 中的 `wall cycles` 是双语标题约定，等待 formalizer 阶段统一中文化
   - 表 5.7-5.12 单元格内 `K=1 stride=2` 等简略表头惯例保留，避免破坏列宽

3. **章节范围**：所有修复严格限定在 §5（527-895 行）。§6 章（结论与展望）出现的 `case` / `S2D` / `wall cycles` 等词语未触动。

4. **数据数值**：所有数值未做改动（包括周期数、PE 利用率、帧率、加速比等），符合"数据数值不动"要求。

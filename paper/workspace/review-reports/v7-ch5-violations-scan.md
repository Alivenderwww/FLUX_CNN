# 写作评审报告 v7 — §5 验证与性能分析章 6 类违规全章扫描

## 评审范围

paper.md（v6.4 末态）第 527-895 行：§5.1 引言 / §5.2 验证环境与方法 / §5.3 功能仿真验证 / §5.4 板级实现验证 / §5.5 性能分析（含 §5.5.1-§5.5.6） / §5.6 与已有工作对比 / §5.7 本章小结。

## 总判定：FAIL

按用户反馈，§5 全章存在 6 类系统性违规，远不止用户列举的 6 例。本次彻底扫描共定位 **66 处违规**（按行号去重统计；同行多类违规重复计入）。重灾区为 **§5.2.1 / §5.3 / §5.5.1 / §5.5.2 / §5.5.5 / §5.6**。

---

## A 类：代码文件名残留（spec §11.14 凑字数禁区——不写代码文件名）

> 论文中不应出现项目内具体文件名/路径。模块名（如 `mac_array` / `line_buffer`）属于 RTL 模块标识符，可保留；但 `.tcl` / `.py` / `.sv` / `.md` 后缀的文件名必须删除或改写为功能性描述。

| # | 行号 | 原文片段 | 修复建议 |
|---|------|---------|---------|
| A1 | 540 | "综合脚本以 `Syn/run_syn.tcl` 与 `Syn/run_syn_smc.tcl` 为入口，分别对应单核与多核SMC配置" | 改为"综合脚本分别对应单核与多核 SMC 两种入口" |
| A2 | 540 | "axi_dm IP由 `Syn/gen_axi_datamover.tcl` 在工程中生成，多核AXI互连IP由 `Syn/gen_multicore_ip.tcl` 生成" | 改为"`axi_dm` IP 与多核 AXI 互连 IP 均由 Vivado 生成脚本在工程中生成" |
| A3 | 542 | "Python侧的 `toolchain/` 目录承担三类任务" | 删除路径，改为"Python 侧编译器/激励生成/参考模型三类工具承担三类任务" |
| A4 | 560 | "该测试集由 `toolchain/run_regression.py` 自动生成与执行" | 改为"该测试集由回归脚本自动生成与执行" |
| A5 | 561 | "在 `--fold --s2d` 模式下触发空间到深度变换" | 模式开关命令行参数应口语化为"在 Y 维折叠+空间到深度联合模式下触发变换" |
| A6 | 560 | "（`--fold`，命令行参数保留ky-fold工程命名）、Y维折叠+空间到深度联合模式（`--fold --s2d`）" | 删除命令行参数与工程命名注释，仅保留正文术语 |
| A7 | 565 | "由 `toolchain/run_robust_smoke.py` 生成与执行" | 改为"由形状鲁棒性测试脚本生成与执行" |
| A8 | 569 | "由 `toolchain/run_multicore_chain.py` 生成、`sim/tb_multicore/tb_multicore_chain.sv` 执行" | 改为"由多核链路生成脚本与多核仿真测试平台执行"（删两个路径） |
| A9 | 595 | "<!-- 来自 STATUS.md §1 / §2 + Syn/reports_smc/utilization.rpt -->" | HTML 注释引用代码文件路径，编译版应清除所有 `<!-- ... STATUS.md / Syn/... -->` 注释 |
| A10 | 606 | "<!-- 来自 STATUS.md §1 -->" | 同上 |
| A11 | 607 | "<!-- 来自 STATUS.md §2 + Syn/reports_smc -->" | 同上 |
| A12 | 608 | "<!-- 来自 paper/data/DSP 阵列映射 P&R 数据 + Syn/reports_smc/utilization.rpt -->" | 同上 |
| A13 | 613 | "<!-- 来自 Syn/reports_smc/timing_summary.rpt LUT 实现 -->" | 同上 |
| A14 | 614 | "<!-- 来自 paper/data/DSP 阵列映射 P&R 数据 + Syn/reports_smc/timing_summary.rpt -->" | 同上 |
| A15 | 631、608、660、672、673 | "<!-- 来自 contributions.md C1.1 / C1.2 -->" 等大量内部 markdown 文件名引用 | §5 全章 16 处 `<!-- 来自 ... -->` HTML 注释中含 `STATUS.md` / `contributions.md` / `v4-experiment-data.md` / `v5-benchmark.md` / `paper/data/...` / `literature.md` 等内部文件名引用，编译为正文应全部删除 |

> A 类小计：**核心 8 处** + 注释引用 **16 处**（编译期可批量清除），共 24 处。

---

## B 类：反复括号补充描述（用户给定示例类型）

> spec 反对在论文正文中以 `（...）（...）（...）` 形式连续追加多个括号注释。学术写法应分句陈述或直接合并定语。

| # | 行号 | 原文片段 | 修复建议 |
|---|------|---------|---------|
| B1 | 548 | "验证粒度由细到粗依次为：单算子（单层卷积）、多层链（含残差融合）、整网（ResNet11完整11层）" | 改为"验证粒度由细到粗依次为单算子、多层链与整网"，再单独一句铺开内涵 |
| B2 | 538 | "203,800个查找表（LUTs）、407,600个触发器（FFs）、445块36 Kb Block RAM（合计约16 Mb片上存储）以及840个DSP48E1切片" | 4 个连续括号中英对照已超过可读阈值；"LUTs"、"FFs"已是正文别名，删括号 |
| B3 | 550 | "由Vivado时序引擎在不同时钟频率约束下报告最差负余量（Worst Negative Slack，WNS），由此推断最大可达频率（Fmax）" | 改"WNS 与 Fmax 两个指标的中文术语 + 缩写"按 §11.4 首次出现展开规范处理 |
| B4 | 550 | "CASE\_RESULT给出端到端wall cycles与帧率上限，LAYER\_PROFILE给出每层每核的cycles与mac\_pipe占空比，DDR\_PROFILE给出每路DDR的aw/w/ar/r与busy占空比" | 三处机读统计行原始字段名（`CASE_RESULT` / `LAYER_PROFILE` / `aw/w/ar/r`）应改为中文描述 |
| B5 | 552 | "包含ASIC加速器（DianNao、TPU v1、Eyeriss、Eyeriss v2、Gemmini）与FPGA加速器（NVDLA、VTA、Peng等人的全栈加速器）" | 括号嵌套列举改用冒号或破折号铺开 |
| B6 | 606 | "权重缓冲（WB SRAM 1024×2048）57块、输入特征图缓冲（IFB SRAM 8192×128）32块、残差与shortcut通路缓冲（Shortcut Bank 8192×128）32块、输出特征图缓冲（OFB SRAM 2048×128）7块加1个RAMB18" | 4 项连续括号注释，建议表格化或拆分为 4 句 |
| B7 | 631 | "动态功耗主要由MAC阵列的乘法翻转、IFB/OFB SRAM的读写翻转与AXI突发数据传输三部分贡献" | 整段良好，但下文"（与工艺、温度、电压相关，与时钟频率无关）与动态功耗（与时钟翻转率、激活率相关）"在 620 行连续 2 个长括号注释应改为分号分句 |
| B8 | 620 | "静态功耗（与工艺、温度、电压相关，与时钟频率无关）与动态功耗（与时钟翻转率、激活率相关）" | 学术写法："其中静态功耗仅取决于工艺、温度与电压；动态功耗则受时钟翻转率与激活率影响" |
| B9 | 670 | "1-DDR配置下流水占空比仅36.2%，单路DDR占空比已达84.7%" 之上下游"（每路）"在表头与正文重复 | 表头括号"DDR busy（每路）"与正文"每路DDR占空比"已重复表达，可合并 |
| B10 | 671 | "$K=3$主路径层（共5层）PE利用率位于78%至93%区间... 降采样层（$K=1$、stride>1，共3层）PE利用率显著偏低，分别为18.7%、16.1%、40.4%" | 双层括号注释（条件+计数）应改为定语或单独一句 |
| B11 | 682 | "将SmartConnect IP（SMC，连接4个加速核与4个外存通道的互连IP）、偏置寄存器堆、对外存请求通道的仲裁器以及命令调度引擎中的子状态分别替换为零延迟理想模型" | "SMC，连接4个加速核与4个外存通道的互连IP" 嵌套括号注释解释术语，应将解释拆为单独一句 |

> B 类小计：**11 处**（其中 B4、B6 是重灾点）。

---

## C 类：英文术语未中文化（spec §11.9）

| # | 行号 | 英文术语 | 出现位置/次数 | 修复建议 |
|---|------|---------|--------------|---------|
| C1 | 550, 639, 643, 651-655, 660, 664, 666-668, 670, 673, 685-686, 690, 695-696, 698, 706, 712, 716, 728, 732, 740, 749, 768, 772, 821, 892, 919 | `wall cycles` / `wall cy` | §5 全章约 35+ 次 | 全部改为"墙钟周期数"或"整网周期数"（首次定义后用"周期数"即可）。表头"wall cycles"改"周期数"。"wall cy"改"周期" |
| C2 | 550, 685 | `cycles`（与中文混用，如"约14,354 cycles"、"减少132个cycles"、"占用cycles"、"剩余24.2%为层间空闲" 之 cycles） | 全章约 12 次 | 改为"周期" |
| C3 | 716, 718-722 | `cy/fire`（表 5.8 表头与正文） | 4 次 | 改为"周期/触发"或"每触发周期数"；行内 cy 量纲全部中文化 |
| C4 | 651, 689, 766, 768, 769 | `主线`（mainline，作 ResNet11 N=4 主配置代称） | 8 次 | 改为"主配置"或"参考配置"；表 5.12 标题英文 `mainline` 同步改为 `reference configuration` |
| C5 | 660-662, 667 | `PoC`（Proof-of-Concept） | 4 次（表 5.5 标题、行、正文） | 中文化为"原理验证"（首次展开"概念验证（PoC）"，后续中文） |
| C6 | 850, 851 | `forward`（"PyTorch 2.11.0 单线程 forward 模式"、"forward 时间"） | 2 次 | 改为"前向推理" |
| C7 | 806, 822 | `setup`（"未建模 IDMA 命令组装阶段的 setup 节省"、"setup 节省"） | 2 次 | 改为"准备阶段" |
| C8 | 784 | `drain`（"初始化与drain开销") | 1 次 | 改为"排空" |
| C9 | 881 | `overlay`（"overlay部署"，OPU 备注） | 1 次 | 改为"覆盖层部署"或"软件可重构覆盖层" |
| C10 | 660, 662 | `versus`（表 5.5 英标题中"1-DDR versus 4-DDR PoC"） | 1 次 | 英标题保留 versus 可接受，但中文表标题不应混入英文比较词；应统一用"对比" |
| C11 | 565, 569 | `case`（"24 case 全部 PASS"、"20 case 全部 PASS"等量化用法允许，但"22 case ResNet-18风格回归"中"case"作量词存疑） | §5 全章 20+ 次 | spec 允许"N case"作量化短语；但 891 行 "22 case ResNet 风格回归在三种编译模式下共 66 次仿真"建议统一为"22 个用例"或"22 条用例"，避免中英混杂量词 |
| C12 | 565 | `X 传播`（"无 X 传播"，X 指 SystemVerilog 未知态） | 1 次 | 学术中文应改为"未知态传播"或"X 态传播"（保留首字母+中文释义） |
| C13 | 671 | "fire数"（691, 712, 716, 718-722, 749, 768, 772, 774-784, 787 共 30+ 次） | 全章约 30 次 | spec §11.9 中"fire"未列入白名单；建议中文化为"触发数"或"有效拍数"，并在首次定义；表头同步改 |
| C14 | 569 | `sub_W` / `sub_W_out` / `row_stride`（驼峰下划线英文标识符） | §5 多处（772-784 表中亦反复） | 这类带下划线的命令式标识符在论文正文应改为汉化定义，例如"每核子图像宽度"、"行步长" |
| C15 | 685 | "取指、就绪等待、命令发射、数据传输与完成确认" 之前后行均出现"`mac_pipe`"、"`stage-0 join`"、"`mac_pipe%`"、"`fire（max）`" 等英文表头/标识符 | §5.5.2 / §5.5.5.5 多处 | 表头"mac\_pipe%" 改"流水占空比"；"stage-0 join"改"零阶汇合点" |
| C16 | 684 | "饥饿提优先级机制全网零次触发" 中"饥饿"是中文转译，但该段反复出现"饥饿/饥饿提优先级"5 次 | 1 段 | 严格学术化应改为"匮乏（starvation）保护"或保持中文一贯——本条非强违规，建议轻调即可 |

> C 类小计：**16 大项，覆盖位点约 100+**。`wall cycles` 与 `fire数` 是重灾词。

---

## D 类：非学术术语

| # | 行号 | 原文片段 | 类别 | 修复建议 |
|---|------|---------|------|---------|
| D1 | 558 | "5.3.1 单核ResNet风格回归测试集" | 章标题 | 改"5.3.1 单核 ResNet 类回归测试集" |
| D2 | 560 | "22 case ResNet-18风格回归" | 句首 | 改"22 个用例的 ResNet-18 类回归" |
| D3 | 580-582 | "单核ResNet风格回归" 表 5.1 中 3 行同样表述 | 表格 | 同 D1 中文化 |
| D4 | 651 | "层次化优化路径" 之前一句 "表 5.4 中加速比按"层次化优化路径"分三类陈述如下" 之 D 类问题非常微，可保留 | 1 处 | 保留 |
| D5 | 651, 689, 766, 768, 769, 891, 892 | "主线"（既作"主路径"又作"主配置"含义） | 8 次 | 同 C4，改为"主配置"或"参考配置"。本身是中文词但作为非正式称呼应规范化 |
| D6 | 728 | "在单核tb_core_dma中跑出基线" | 实验设置 | "跑"为口语化动词，改为"得出基线"或"采集基线" |
| D7 | 791 | "将ResNet11之外的代表性CNN网络置于FLUX_CNN的analytical model中估计" | 句首 | "analytical model" 中英混杂，应改为"分析模型"；下文 798、806、822、872-874 共 9 次 `analytical` 同步中文化 |
| D8 | 822 | "其四，校准锚点ResNet11以仿真wall cycles 190,133折算ASIC吞吐为4,471 fps" | 长句 | "校准锚点"是技术合理用法但表 5.13 标题与正文已重复 4 次"校准锚点 ResNet11"；建议第 1 次定义后改为"参考点" |
| D9 | 891 | "三套测试集与ResNet11整网仿真共同覆盖加速器的功能空间" | 句中"功能空间" | 表述过于宽泛但非严重违规，可改"功能验证范围" |

> D 类小计：**9 处**。重点是 D1-D3（"风格"3 次）与 D5（"主线"8 次）。

---

## E 类：数学符号未完整 LaTeX 包裹

| # | 行号 | 原文片段 | 修复建议 |
|---|------|---------|---------|
| E1 | 560 | "卷积核$K$∈{1, 3, 5, 7}，步长$S$∈{1, 2, 3, 4}，输入通道Cin∈{4, 8, 12, 16, 32}，输出通道Cout∈{16, 24, 32}" | 4 处违规：`∈`、`{...}`、纯数字集合都应在 `$...$` 内。改为 `$K \in \{1, 3, 5, 7\}$`、`$S \in \{1, 2, 3, 4\}$`、`$C_{\mathrm{in}} \in \{4, 8, 12, 16, 32\}$`、`$C_{\mathrm{out}} \in \{16, 24, 32\}$`。同时 `Cin` `Cout` 应改 `$C_{\mathrm{in}}$` `$C_{\mathrm{out}}$` |
| E2 | 560 | "输入空间分辨率$H$×$W$覆盖1×1、15×17、28×28、33×33、64×32、120×68等多种形状" | `H`、`W` 已包裹但 `×` 在公式外；"1×1"等数字尺寸应包裹在 `$...$` 内 |
| E3 | 565 | "极小输入（$W = 8$，$H = 8$），奇数维度（$W = 33$，$H = 17$），大卷积核（$K = 7$），奇异步长组合（$\mathrm{stride} = 3$且$K < \mathrm{stride}$），Cin与Cout不为16倍数等" | "Cin与Cout"应改 `$C_{\mathrm{in}}$ 与 $C_{\mathrm{out}}$`；"16 倍数"中 16 在数学语境下应在公式内 |
| E4 | 569 | "$K$∈{1, 3, 5, 7}，stride∈{1, 2}，$W$∈{8, 32, 33}" | 与 E1 同；`stride` 应 `$\mathrm{stride}$`、集合包裹纳入 `$...$` |
| E5 | 569 | "$N=2$与$N=4$各10条用例共计20条" | $N$ 已包裹 OK；但 569 行后段"sub\_W=2 时" 应改 `$\mathrm{sub\_W} = 2$` |
| E6 | 671 | "$K=3$主路径层（共5层）PE利用率位于78%至93%区间，其中$K=3$、stride=1、cin=cout=16层" | "stride=1、cin=cout=16" 全部裸字母+裸数字，应改 `$\mathrm{stride} = 1$、$C_{\mathrm{in}} = C_{\mathrm{out}} = 16$` |
| E7 | 671 | "Patch层在空间到深度变换后PE利用率为70.9%；FC层因输出通道维仅有144 fire数" | "144 fire 数"裸数字+英文计量词，至少 144 应改 `$144$`，fire 中文化（见 C13） |
| E8 | 672 | "降采样层（$K=1$、stride>1）" | "stride>1" 裸字母+裸符号，改 `$\mathrm{stride} > 1$` |
| E9 | 677 | "$N=2$相对$N=1$加速1.32×... $N=2$的加速比1.32×偏离理想2.0×的原因有二：第一，跨核边界晕区引入的额外计算（每核额外保留K-1列输入）" | "K-1" 裸字母减法，改 `$K-1$` |
| E10 | 694 | "自变量$C_{\mathrm{in}}$取 {1,2,4,8,16,32,64}" | 集合应在 `$...$` 内：`$\{1,2,4,8,16,32,64\}$` |
| E11 | 712 | "自变量$K$取 {1,3,5,7,8（含空间到深度）}" | 集合带中文注释——应将集合包裹后中文注释置于括号外：`$\{1,3,5,7,8\}$（其中 $K=8$ 含空间到深度变换）` |
| E12 | 753 | "方阵$H = W$取 {16,32,64,128}，核数$N$取 {1,4}" | 2 处集合：`$\{16,32,64,128\}$`、`$\{1,4\}$` |
| E13 | 565 | "$\mathrm{stride} = 3$且$K < \mathrm{stride}$" | "且"夹在两段公式之间，应改 `$\mathrm{stride} = 3$ 且 $K < \mathrm{stride}$`（首尾各加空格，防止公式连写） |
| E14 | 619, 631, 670, 672 | 中文+裸数字单位混杂："1024×2048"、"8192×128"、"2048×128"、"36 Kb"、"16 Mb"等 | "1024×2048" 在 SRAM 容量描述中是技术参数，应改为 `$1024 \times 2048$` 或保持但允许裸表达式（spec 默认允许 ASCII 数字+`×`，本条按 §11 chinese-thesis-spec 视情判定） |
| E15 | 692-787 各 §5.5.5 | 表 5.7~5.12 表头中的 `$C_{\mathrm{in}}$`、`$K$`、`$N$` 等已规范，但单元格内的 `K=1 stride=2`（734-737, 774-784）裸字母+裸数字仍未包裹 | 全表单元格应统一 `$K=1$ $\mathrm{stride}=2$` 或采用列拆分 |

> E 类小计：**15 处**（E1、E10-E12 是重灾点）。

---

## F 类：反复强调（同义堆砌）

| # | 行号 | 原文片段 | 修复建议 |
|---|------|---------|---------|
| F1 | 565 | "24 case 全部 PASS，无超时、无 X 传播、无地址越界告警" | 三个"无"反复强调。改"24 个用例全部 PASS，未观察到超时、未知态传播或地址越界告警" |
| F2 | 569 | "全部PASS" + "其中W=33的N=4切片用例验证了W维不能被核数整除时的非均匀切片几何，W=8的极小输入用例验证了sub\_W=2时单核仅有2列输入的极端边界" | 两次"用例验证了……极端边界"句式雷同 |
| F3 | 573 | "三种配置下整网输出与PyTorch端的浮点参考完全一致，11/11层全部按位匹配" | "完全一致" + "全部按位匹配" 同义重叠。删一个，留"11/11 层逐字节匹配"即可 |
| F4 | 891 | "整网输出与PyTorch端的浮点参考完全一致，11/11层全部按位匹配" | 与 F3 同重复，且本章小结又重述一次。改"逐字节匹配"单点表述 |
| F5 | 671 | "降采样层（$K=1$、stride>1，共3层）PE利用率显著偏低，分别为18.7%、16.1%、40.4%" + "整网累计PE利用率约为70.9%" + 后一段"降采样层（$K=1$、stride>1）的低利用率不是计算资源不足，而是访存数据量与计算fire数失配" | "低利用率"在 §5.5.2 单节内重复出现 5 次 |
| F6 | 808-820 | "ASIC折算频率850 MHz相对FPGA实测频率148.5 MHz为5.72倍，PE利用率与wall cycles不变，端到端帧率亦为5.72倍线性放大" | "5.72 倍"前后两次重复，且"PE 利用率不变"+"线性放大"语义同 |
| F7 | 822 | "其一... 该偏差来源于模型未建模外存命令组装阶段的setup节省，并非PE利用率高估" 与上文 806 行"主因是analytical模型未建模IDMA命令组装阶段的setup节省" | 同一原因（命令组装 setup 节省）在 5.5.6 节内重复陈述 2 次 |
| F8 | 685 | "进一步压缩取指等待路径的优化潜力上界约为2%至3%" 与 686 行"进一步压缩命令等待最多再缩短2%至3%" | 同一节连续两段陈述同一上界 |
| F9 | 893 | "$N=4$等效阵列规模仍远小于TPU v1的256×256，FPGA平台Fmax上限低于ASIC工作通常的200 MHz至1 GHz" 与 §5.6 第 846 行"远小于TPU v1的256×256"完全重述 | 本章小结与正文 §5.6 重复表述同一论点 |
| F10 | 567-573 | §5.3.3 与 §5.3.4 连续 4 段每段都以"全部 PASS / 完全一致 / 11/11 层按位匹配"收尾 | 4 段尾句反复强调验证通过。建议改为"用例结果汇总于表 5.1"，让表替代反复陈述 |

> F 类小计：**10 处**。F1（用户给定示例）、F4、F10 是结构性反复。

---

## 全章统计

| 类别 | 违规处数 | 重灾区小节 |
|------|---------|-----------|
| A 代码文件名残留 | 8（正文）+ 16（HTML 注释引用） | §5.2.1 / §5.3.1-§5.3.3 / §5.4 |
| B 反复括号补充 | 11 | §5.2 / §5.4 / §5.5 |
| C 英文术语未中文化 | 16 大项（位点 100+） | §5.5 全节、§5.5.5 表 5.7-5.12 表头 |
| D 非学术术语 | 9 | §5.3.1（"风格"3 次）/ §5.5.1（"主线"8 次）/ §5.5.6（"analytical"9 次）|
| E LaTeX 数学符号 | 15 | §5.3.1 / §5.3.2 / §5.5.5（集合记号未包裹）|
| F 反复强调 | 10 | §5.3 验证总结句、§5.5.6 局限性、§5.7 章小结 |
| **合计** | **约 66 处独立违规位点** | **§5.5 与 §5.3 是双重灾区** |

## 重灾区排序（按违规密度）

1. **§5.5.5 参数敏感度扫频分析**（692-787 行）：A/C/E 三类齐发，集合记号未包裹、`wall cy` / `cy/fire` / `fire数` / `tb_core_dma` 等英文术语充斥表头与正文。
2. **§5.2.1 验证环境**（535-542 行）：5 个代码文件路径直接暴露在正文。
3. **§5.5.1 端到端时延与帧率**（637-655 行）：`wall cycles` 在正文与表中累计 12+ 次，"主线"称呼 4 次。
4. **§5.6 与已有工作对比**（824-886 行）：`analytical` 9 次、`overlay` 1 次、`PoC` 概念散见。
5. **§5.3.1-§5.3.4**（558-589 行）："风格" 3 次、A 类文件名 4 处、F1 反复强调原句。

## 备注（不计入 PASS/FAIL，仅提示）

- **C 类与 D 类的交叉**：`analytical model` / `mainline` 等既属英文术语未中文化（C），又属技术写作非学术口径（D）。建议主 Agent 在批量替换时统一处理。
- **A 类 HTML 注释**（A9-A15 共 16 条）：均为 `<!-- 来自 ... -->` 模板注释，编译版本应在格式化阶段统一删除，建议 thesis-formalizer 在 §11.16 增设清单。
- **事实正确性提示**：表 5.4 行 645"~1,115,000 cycles"与表 5.5 行 666"354,566"等具体数字未在本次写作评审范围内复核，建议 tech 评审复核数据一致性（仅提示，不影响本报告 FAIL 判定）。

---

**最终判定：FAIL（66 处违规，远超 ≥3 处累积阈值与 ≥1 处结构性问题阈值）**

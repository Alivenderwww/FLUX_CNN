# v4 重构更新清单（基于 2026-05-08 项目最新进展）

> 本文档列出本轮重写相对上一轮（`paper/参考/20250508第一轮论文 面向端侧流式计算场景的卷积加速器设计/`）需要新增 / 修改 / 删除的论点与数据，按章节组织。
>
> 各章 Writer 的工作范式：
> 1. **首读**：`chinese-thesis-spec.md` §11.12（禁词与元话语红线）/ §11.13（立项硬约束）/ §11.14（凑字数禁区）/ §11.15（范文章节对照表）
> 2. **范文**：`paper/参考/基于 FPGA 的脉冲神经网络加速器设计/` 中本章对应文件
> 3. **上轮模板**：`paper/参考/20250508第一轮论文 .../{N} *.md` 本章文件（**仅本章，不读其他章**）
> 4. **本轮 diff**：本文档对应章节
> 5. **数据底盘**：`STATUS.md`（按需）+ `paper/data/exp{1..11}*.md`（按需）+ `Syn/reports_smc/` P&R 报告（按需）
>
> v3.2 教训（写作时**必须避免**）：见 `lessons-learned.md` 末尾 4 条 260508 条目。核心为：
> - 不写 Round A/B/C... 内部代号
> - 不写"经多轮迭代 / 4 轮调试"过程化叙述
> - 不写"工程陷阱 / 工程教训 / 可复用工程经验" lab-note 腔调
> - 不跨章节引用（§4 不写"详见 §5"，§5 不写"§4 末尾介绍的"）
> - 不写 commit hash / RTL 文件名 / 综合宏名（如 `FLUX_MAC_SIMD`）

---

## §1 绪论

**Writer 输入**：`paper/参考/1 绪论.md`（用户写的示范骨架）+ 上轮 §1 + 本节 diff

**整体方针**：上轮 §1 已通过 v3 评审，本轮**仅做 §1.3 主要研究内容 / 主要贡献的同步**，§1.1 / §1.2 文献综述部分**完全沿用**上轮文本。

### diff 清单

| 位置 | 改动 |
|---|---|
| §1.3 研究内容第三段（编译流） | 沿用上轮 |
| §1.3 主要贡献 | 5 条扩为 5 条（编号不变），其中第二条"PE 利用率优化（Ky 折叠 + S2D）"沿用，新增**第六条**（或合并到现有条目）：**"在 FPGA 验证平台上，提出一种利用 MAC 阵列相邻列共享激活值特性、把两个 INT8 乘法打包到单个 DSP48E1 块中的硬件无关映射方法，使 16×16 阵列的乘法器全部映射至 DSP，并在不改动 RTL 数据通路的前提下显著降低查找表占用与提升时序裕度"** |
| §1.3 主要贡献 第七条候选 | **"建立了控制变量验证方法"**（也可融入"实测验证"段）：通过将 SmartConnect IP / bias 寄存器堆 / 仲裁器替换为零延迟理想模型，量化各因素对整网周期数的贡献，将"工具链或调度器中谁在拖慢吞吐"从估算落到实测口径 |
| §1.4 论文组织 | 沿用上轮 |

### 字数

§1 维持 6000-8000 中文字（spec §4 强制）。

---

## §2 理论基础

**Writer 输入**：`paper/参考/基于 FPGA 的脉冲神经网络加速器设计/2 脉冲神经网络及 FPGA 硬件加速技术原理.md` + 上轮 §2 + 本节 diff

**整体方针**：上轮 §2 已通过 v3 评审，本轮**完全沿用**，不动结构与措辞。

### diff 清单

无。沿用上轮 §2 全文。

---

## §3 总体方案

**Writer 输入**：`paper/参考/基于 FPGA 的脉冲神经网络加速器设计/4 基于 FPGA 的脉冲神经网络加速器设计.md` 中 §4.2 部分 + 上轮 §3 + 本节 diff

**整体方针**：上轮 §3 已通过 v3 评审，本轮**仅在 §3.5 数据流选择末段**新增一句关于"激活值跨列共享在 OC-broadcast 数据流中的天然结构性"的描述，作为 §4.3 SIMD 描述的隐式铺垫；不预告 §4，不展开实施。

### diff 清单

| 位置 | 改动 |
|---|---|
| §3.5 末段 | 新增 1-2 句：在 OC-broadcast 数据流下，每周期同一行激活值同时被 16 列消费、各列权重独立，相邻列在物理实现层面共享同一拍激活向量这一结构性特征为后续阵列实现保留了把两列乘法折合到同一物理乘法器中的可能性。**不展开 SIMD on DSP 细节，不写 §4.3 / §5.4.2** |

---

## §4 加速器具体实现（**本轮重写重点**）

**Writer 输入**：`paper/参考/基于 FPGA 的脉冲神经网络加速器设计/4 基于 FPGA 的脉冲神经网络加速器设计.md` 中 §4.3-§4.10 + 上轮 §4 + 本节 diff

**整体方针**：上轮 §4 整体保留，但 v3.2 在 §4.3 末段加的 SIMD 微架构介绍因含"具体实施细节、综合实现指标与 sign correction 工程踩坑详见 §5.4.2"等跨章节引用与 lab-note 词，本轮**重写 §4.3 SIMD 段**与新增 §4.13（DSP 阵列实现选项）。

### diff 清单

| 位置 | 改动 |
|---|---|
| §4.3 MAC 阵列模块 末段（v3.2 加的 SIMD 段） | **删除上轮 v3.2 加的"为提升 FPGA 平台上 DSP48E1 块的利用率..."这段**。仅保留上轮原版的"每个 PE 由 1 个 INT8 乘加器构成，256 个 PE 的具体硬件实现细节与综合后的资源映射比例详见 §5 实测分析"末句即可（不指向 §5.4.2 而是 §5）。 |
| **新增 §4.13** （在 §4.12 本章小结之前） | **DSP 阵列映射方案** —— 篇幅 ~600 字，描述把相邻两列 INT8 乘法器合并映射到单个 DSP48E1 块的方案：(1) 数据流前提（OC-broadcast 让相邻两列共享激活值），(2) 映射规则（25 bit A 端打包两组权重、18 bit B 端共享激活值），(3) 输出还原（位提取 + 符号校正），(4) 与 LUT 实现对外接口完全一致，由综合层选项决定走哪一种映射。**不写 commit / 综合宏名 / "工程陷阱" / "Vivado 推断 (C−A:B)" / "USE_DSP=no"等内部细节**；这些保留在 paper/data/exp9.md。 |
| §4.12 本章小结 | 末段加一句概括 §4.13 ："此外，本章给出了 16×16 INT8 MAC 阵列在 DSP48E1 块上的两种可选映射方案，为综合阶段在查找表与 DSP 块之间作权衡保留了入口。" |

### §4.13 写作要点

- **章节定位**：实现章下的一个映射方案小节（与 §4.8 编译器侧的 PE 利用率优化对称——一个是软件侧重映射，一个是硬件资源侧映射）
- **绝对不写**：
  - 内部代号（Round M / Round 4 等）
  - 工具行为细节（Vivado 默认推断模式 / USE_DSP="no" 属性）
  - 过程化叙述（"经 4 轮迭代" / "踩坑修复"）
  - 跨章节引用（"详见 §5.4.2 实测"）
- **可写**：
  - 方案的输入条件（OC-broadcast 数据流的结构性特征）
  - 数学映射（25×18 → 两个 INT8 乘积的位提取与符号校正机制）
  - 接口同构（mac_array_simd 与原 mac_array 在外部接口上完全一致）
  - 收益定性表述（"在 FPGA 平台上将查找表用量降低、把更多乘法器迁移至专用乘法块"，具体数值留 §5 报告）

---

## §5 验证与性能分析（**本轮重写重点**）

**Writer 输入**：`paper/参考/基于 FPGA 的脉冲神经网络加速器设计/5 基于 FPGA 的脉冲神经网络加速器验证.md` + 上轮 §5 + 本节 diff

**整体方针**：本章是 v3.2 违规重灾区，**完全重写 §5.4 / §5.5 / §5.6**，§5.1 / §5.2 / §5.3 沿用上轮（功能验证测试集没变）。

### diff 清单

#### §5.4.1 资源占用

| 位置 | 改动 |
|---|---|
| 表 5.2 | **替换为路由后实测数据**，表头改为：*N*=1（综合）/ *N*=2（综合）/ *N*=4 LUT 实现（路由后）/ *N*=4 DSP 实现（路由后）；不写 "LUT 版/SIMD 版"这种口语化标签，改为"LUT 实现"/"DSP 实现" |
| 表 5.2 数据 | LUT 36,942 / 74,386 / 153,892 / 99,061；FF 13,167 / 26,927 / 63,160 / 65,793；BRAM36 128+1RAMB18 / 256 / 288 / 288；DSP48E1 82 / 164 / 320 / 832；器件容量与占比同步 |
| 段落 | 重写。**不写**"经多轮综合迭代""commit `5fe16b2`""[CHECK]: ..."等。直接陈述：(1) 单核资源占比；(2) 多核扩展层资源近似线性增加 + BRAM 是约束（说明 Shortcut Bank 容量缩减给出最终 *N*=4 配置）；(3) DSP 实现把 16×16 阵列的 INT8 乘法器从查找表迁移至 DSP 块的资源数据对照（仅讲方案与结果，不讲过程）。 |

#### §5.4.2 时序分析

| 位置 | 改动 |
|---|---|
| **完全重写整节** | **不写**"早期综合中..."/"初版综合 WNS 为..."/"经过 ofb_writer 内消除..."/"Round 6, commit c46993d"/"(C−A:B) 模式而额外占用 1 个 DSP"/"USE_DSP=no 综合属性" |
| 内容结构 | (1) 时序约束与方法（7.2 ns 时钟约束、Vivado 时序引擎报告 WNS、布线后口径）；(2) LUT 实现关键路径定位（mac_array 内 16-input 加法树）+ 数据（WNS −0.777 ns / Fmax 125.4 MHz）；(3) DSP 实现关键路径定位（DSP P 输出经符号校正后入加法树）+ 数据（WNS −0.303 ns / Fmax 143.8 MHz）；(4) 两种实现 Fmax 对比与原因定性陈述（DSP 块内乘法器为硬连线，时延短于查找表实现的乘法树）；(5) 折算说明（100 MHz 假设 + 143.8 MHz 实测对照） |
| 篇幅 | ~500 字 |

#### §5.4.3 功耗分析

沿用上轮（仍为 [CHECK] 占位 + 定性分析）。

#### §5.5.1 端到端时延与帧率

| 位置 | 改动 |
|---|---|
| 表 5.4 | **删除上轮 v3.2 加的 Round A–H / Round I / Round K 行**，改为简洁的 5 行：(1) *N*=1 未启用 S2D；(2) *N*=1 含 S2D；(3) *N*=2 含 S2D + W 切片；(4) *N*=4 含 S2D + W 切片（多核基线）；(5) *N*=4 含 S2D + W 切片 + 跨核 SRAM 直送 + 编译器调度优化（最终主线，190,133 cycles / 526 fps @ 100 MHz） |
| 段落 | **不写** Round X 内部代号、不堆砌"切片公平、cout 切片自适应、host 端 dfe 并行启动..."等内部修改点列表。改为按"层次化优化路径"写：(1) 编译器侧优化（S2D 把 Patch 层 5.05× 加速）；(2) 多核扩展（W 切片配合跨核 SRAM 直送 push 路径）；(3) 编译器侧调度优化（针对降采样层访存受限特征对 IDMA 命令按行步长分离、命令流以本地段优先重排两类调度策略）。陈述每一类的方法 + 最终收益（绝对数与累计加速比），**不报中间过程**。 |
| 折算 | 100 MHz + 143.8 MHz 双口径，**不写 142.8 MHz synth 老数据** |

#### §5.5.2 PE 阵列利用率

新增内容（基于 paper/data/exp8 PE util roadmap）：

| 位置 | 改动 |
|---|---|
| §5.5.2 末段（在原 1-DDR/4-DDR 表后） | 新增一段 PE 利用率分层分析：每层 PE 利用率分布（K=3 主路径层 78-93%、降采样层 16-40%、Patch 层 71%、FC 层约 5%），整网 PE 利用率约 70.9%。说明降采样层（*K*=1、stride>1）的低利用率不是计算资源不足，而是访存数据量与计算 fire 数失配（访存量与 *K*=3 相近、计算 fire 数仅为 *K*=3 的 1/9）；针对该特征，编译器把 IDMA 行命令按 stride 行间隔分离，使该类层 IDMA 数据量减半，**整网周期数从 204,240 降至 190,977，单层周期数减少约 40%**。**不写** "Round I / H 维步长分离" / "memory-bound 假设" / 控制变量实验 5 等内部用语。 |

#### §5.5.3 多核扩展加速比

| 位置 | 改动 |
|---|---|
| 加速比数据 | 主线 *N*=4 加速比从 2.70× 改为 3.13×（相对 *N*=1 含 S2D 596,088 cycles），对应主线 wall cycles 190,133 |

#### §5.5.4 调度策略量化分析（**新增节，对应控制变量实验**）

| 位置 | 改动 |
|---|---|
| **新增 §5.5.4** | 篇幅 ~500 字。内容：为定位整网周期数中各因素的贡献，将 SmartConnect IP / 偏置寄存器堆 / 仲裁器逐个替换为零延迟理想模型，比较替换前后的周期数差异——SmartConnect IP 的寄存器流水级累计占整网周期数 7.0%；偏置寄存器堆切换的等待开销小于 0.06%；多个主控制器对外存请求通道的仲裁开销在当前调度策略下未触发饥饿（饥饿提优先级机制全网零次触发）；命令调度引擎的取指 / 等待 / 数据传输时间分布约为 17.5% / 12.6% / 44.7%。这一分析定量给出了进一步优化的潜力上界（若移除 SmartConnect 流水级，最多再缩短 7.0%；若进一步压缩取指等待，最多再缩短 2-3%）。**不写** "IDEAL_SMC ifdef" / "exp1-exp5" / "控制变量实验"等内部命名；改为"将 X 替换为零延迟理想模型"这种 academic 中性陈述。 |

#### §5.6 与已有工作对比

| 位置 | 改动 |
|---|---|
| 表 5.6 本工作两行 | 改为三行：本工作 *N*=1（100 MHz 假设）/ 本工作 *N*=4 LUT 实现（路由后 125.4 MHz）/ 本工作 *N*=4 DSP 实现（路由后 143.8 MHz） |
| 段落 | "原因有三"改为"原因有二"（DSP 推断率不再是短板）；**不写**"DSP 实现已使 LUT 用量减少 35.6%、布线后 Fmax 提升 14.7%"这种过程化对比；改为陈述当前实测水平 + 与 ASIC 对照的工艺 / 频率差距 |

#### §5.7 本章小结

| 位置 | 改动 |
|---|---|
| 重写 | 直接陈述本章主要工作 + 关键数据。不堆砌 "Round / 经多轮"；不重复"DSP 推断率从 38.1% 提升至 99.0%"这种过程化句式 |

---

## §6 结论与展望（**本轮重写重点**）

**Writer 输入**：`paper/参考/基于 FPGA 的脉冲神经网络加速器设计/6 结论与展望.md` + 上轮 §6 + 本节 diff

**整体方针**：v3.2 违规重灾区第二章。**完全重写 §6.1 / §6.2 / §6.3**。

### diff 清单

#### §6.1 结论

| 位置 | 改动 |
|---|---|
| 第三段（综合层数据） | **不写** "经多轮综合迭代""68.4 MHz 提升至 142.8 MHz""Round X" 这类过程化叙述。直接报最终值：单核 / *N*=4 LUT 实现 / *N*=4 DSP 实现的资源占用 + 路由后 Fmax + 整网帧率 + 多核加速比 |
| 第四段 | 加 §5.5.4 调度策略量化分析的一句结论"以控制变量法量化各调度因素对整网周期数的贡献" |

#### §6.2 创新点

| 位置 | 改动 |
|---|---|
| **从 5 条整理为 5 条**（重新组织） | 1) 分层调度架构（沿用）；2) 任意形状的固定阵列数据通路（沿用）；3) 编译器侧 PE 利用率优化方案 Ky 折叠 + S2D（沿用）；4) 多核 W 切片与跨核 SRAM 直送（沿用）；5) **DSP48E1 块跨列复用映射方案**（重写：不写"工程陷阱""可复用工程经验""4 轮迭代"等；改为"提出一种利用 OC-broadcast 数据流中相邻列共享激活值的特性、把两个 INT8 乘法合并到单个 DSP48E1 块的硬件映射方案"+收益数据定性陈述）。**多层次验证体系**移到 §5 / §6.1 中描述，不单独占创新点位置（避免凑数嫌疑）。 |
| 末段 | **不写**"工程陷阱""可复用工程经验"等 lab-note 腔；改为对 5 条创新点的整合性陈述 |

#### §6.3 展望

| 位置 | 改动 |
|---|---|
| 短期工作 | **不写** "经多轮综合迭代...将 *N*=4 SMC 综合后 Fmax 由初版 68.4 MHz 提升至 142.8 MHz / 143.8 MHz"。改为陈述当前可见瓶颈 + 优化方向：(1) SDP 量化链关键路径进一步流水化以接近 200 MHz 上限；(2) 功耗实测；(3) 板级硬件验证（VD100 板级 demo 计划，篇幅 1-2 句即可，作为 future work，不展开） |
| 中期工作 | 沿用 |
| 长期工作 | 沿用 |

---

## 参考文献

沿用上轮 `paper/参考/20250508第一轮论文 .../ed 参考文献.md`（v3 已通过评审）。

---

## 摘要 + Abstract + 关键词 + 主要符号表 + 封面 frontmatter

| 部分 | 处理 |
|---|---|
| 封面 frontmatter | 沿用上轮（[TBD] 占位） |
| 中文摘要 | **重写**（500 字以内，目的+方法+结果+结论），结果数据口径用 v4：路由后 Fmax 143.8 MHz / 整网 526 fps / 多核 3.13× / 5 条创新点 |
| 关键词 | 沿用上轮 |
| Abstract | **重写**（与中文摘要对应） |
| Key words | 沿用 |
| 主要符号表 | 沿用上轮（Phase 8 已生成 41 行） |
